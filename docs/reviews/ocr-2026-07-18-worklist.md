# OCR Remediation Worklist — 2026-07-18

> Phase 0 deliverable. Generated tracked worklist derived from the OpenCodeReview dump.
> Downstream Phases 1–4 consume this file to land verified, atomic fixes.

**Source:** `ocr_full-2026-07-18.md` (377 files reviewed, 3077 finding blocks)  
**Generated:** 2026-07-18  
**Method:** ANSI-stripped, parsed by `─── file:lines ───` header, deduplicated by {file, theme} grouping, filtered (path + verified-FP), tiered P0–P3.

## Totals & Reconciliation

| Bucket | Count |
| ------ | ----- |
| **P0** (critical / security·high) | 54 |
| **P1** (bug·high / security·medium) | 210 |
| **P2** (bug·medium / security·low) | 258 |
| **P3** (maintainability/style/docs/perf/test + uncategorized) | 681 |
| **Retained total** (P0+P1+P2+P3) | 1203 |
| Path-excluded (`.claude/**` + `.design-sync/previews/**`) | 1859 |
| Verified-excluded (false positives) | 15 |
| **Grand total** | 3077 |

**Reconciliation:**

```
path-excluded + verified-excluded + retained  ==  total
1859 + 15 + 1203  ==  3077
(1203 = P0 54 + P1 210 + P2 258 + P3 681)
RECONCILES: YES — nothing silently dropped (== 3077)
```

## Phase 1 — P0 Theme Index

The 12 Phase 1 P0 remediation themes, each cross-referenced to its primary file(s) and the tier/section where its worklist findings landed.

| # | Theme | Primary file(s) | Landed in | Findings |
| - | ----- | --------------- | --------- | -------- |
| 1 | SSRF hardening (model adapters) | `src/server/models/anthropic.ts`, `src/server/models/openai.ts`, `src/server/models/google.ts`, `src/server/models/cloudflare.ts` | P0, P1, P2, P3 | 46 |
| 2 | Error-message leakage | `src/server/routes/api/models.ts`, `src/server/routes/api/repos.ts`, `src/server/routes/api/vcs-credentials.ts`, `src/server/routes/api/stats.ts` | P0, P1, P2, P3 | 38 |
| 3 | Log-redaction gaps | `src/server/core/logger.ts` | P0, P2, P3 | 6 |
| 4 | OAuth / CSRF | `src/server/core/oauth.ts`, `src/server/routes/auth.ts`, `src/server/core/github-oauth.ts`, `src/server/core/bitbucket-oauth.ts`, `src/server/middleware/csrf.ts` | P0, P1, P2, P3 | 35 |
| 5 | Weak KDF | `src/server/core/crypto.ts`, `src/server/core/llm-crypto.ts` | P0, P1, P2, P3 | 12 |
| 6 | Prompt-injection fencing | `src/server/prompts/file-review.ts`, `src/server/prompts/summary.ts`, `src/server/core/review.ts` | P0, P1, P2, P3 | 39 |
| 7 | Job-mutation TOCTOU | `src/server/routes/api/jobs.ts`, `src/server/db/jobs.ts` | P0, P1, P2, P3 | 30 |
| 8 | VCS-provider source-of-truth | `src/server/core/webhook-ingest.ts`, `src/server/db/repositories.ts`, `src/server/services/vcs.ts`, `src/server/core/bitbucket.ts` | P0, P1, P2, P3 | 28 |
| 9 | Session / input validation | `src/server/core/sessions.ts`, `src/server/middleware/auth.ts`, `src/server/routes/api/stats.ts`, `src/server/core/verify.ts` | P0, P1, P2, P3 | 20 |
| 10 | DB-layer secret handling | `src/server/db/vcs-credentials.ts`, `src/server/db/model-configs.ts` | P0, P1, P2, P3 | 31 |
| 11 | Client correctness / data integrity | `src/client/pages/repos.tsx`, `src/client/components/features/job-detail/file-finding.tsx` | P1, P2, P3 | 15 |
| 12 | Low-effort hardening | `src/server/core/verify.ts`, `src/server/middleware/csrf.ts`, `src/server/core/sessions.ts` | P0, P1, P2, P3 | 10 |

## P0 — Critical / security·high

### `.github/workflows/ci.yml`
- [ ] `.github/workflows/ci.yml:28-32` — [security · high] Hardcoded credentials in the workflow env block: `POSTGRES_PASSWORD`, `GITHUB_APP_WEBHOOK_SECRET`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `AUTH_CALLBACK_URL`, `APP_URL`, and similar values are e…

### `LICENSE`
- [ ] `LICENSE:1-8` — [documentation · critical] Pre-scan focus area #5: AGPL v3 must be distributed in the official, unaltered text published by the FSF — the preamble of the license explicitly states 'changing it is not allowed' for verbatim copi…
- [ ] `LICENSE:288-299` — [bug · critical] Section 5 heading is missing and the Section 6 heading is incorrectly inserted mid-paragraph inside Section 5's Installation Information prose. The sentence beginning 'But this requirement does not a…

### `index.html`
- [ ] `index.html:7-10` — [security · high] Missing critical security meta tags for a web app served over a network: no Content-Security-Policy, no `X-Content-Type-Options`-equivalent guidance (can't be set via meta, but worth noting), no `Ref…

### `scripts/fix-max-files.mjs`
- [ ] `scripts/fix-max-files.mjs:33-37` — [security · high] This script performs a hardcoded bulk update against whichever database URL is selected, including a production URL from environment files, with no explicit target/environment guard or confirmation.…

### `scripts/render-design.mjs`
- [ ] `scripts/render-design.mjs:176-176` — [security · high] Only `/api/auth/session`, `/api/stats**`, and `/api/jobs**` are intercepted. Any additional endpoint the dashboard fetches (job detail by id, comments, repository metadata, repos list, user/profile)…

### `scripts/test.mjs`
- [ ] `scripts/test.mjs:56-61` — [security · high] `env: process.env` forwards the entire process environment to both `migrate.mjs` and `vitest`, including `TEST_DATABASE_URL` and any other secrets loaded from `.env` files. Vitest output, stack trace…

### `src/client/components/features/job-detail/job-meta-cards.tsx`
- [ ] `src/client/components/features/job-detail/job-meta-cards.tsx:138-141` — [security · high] Untrusted hrefs bound to anchors: `commitUrl(job, job.commitSha)` and `reviewUrl(job, job.prNumber, job.reviewId)` are rendered directly into `<a href={...}>`. If the underlying `repositoryUrl`/`vcsP…

### `src/client/components/features/stats/overview-stats.tsx`
- [ ] `src/client/components/features/stats/overview-stats.tsx:12-18` — [bug · critical] **Mock data presented as real metrics (data integrity)** — `generateMockTrend` fabricates sparkline values via `Math.random` and they are rendered to users as if they were real trend data for the ove…

### `src/client/components/motion/shared-layout-bg.tsx`
- [ ] `src/client/components/motion/shared-layout-bg.tsx:73-112, 84-109` — [bug · critical] Children cloning discards original siblings and re-mounts the child as a sibling of the background. The third argument to `cloneElement(el, props, <>...background...{el}</>)` replaces the child's own…  _(+1 related in cluster: bug·critical)_

### `src/client/components/motion/stepped-slider.tsx`
- [ ] `src/client/components/motion/stepped-slider.tsx:116-117` — [bug · critical] Pre-scan focus #3: `width: left` is being set to a `useMotionTemplate` string like `"42.5%"`, not a transform — and even if it were a transform, `width` isn't a transformable property here. At the ex…

### `src/client/components/ui/dropdown-menu.tsx`
- [ ] `src/client/components/ui/dropdown-menu.tsx:149-152` — [bug · critical] Positioning effect dependency on `ctx.triggerRef` is ineffective — refs are stable so the effect won't re-run when the trigger mounts. On first render, the trigger ref is null and `update` bails out,…

### `src/client/pages/repos/add-bitbucket.tsx`
- [ ] `src/client/pages/repos/add-bitbucket.tsx:16-17` — [security · high] Sensitive credentials (access token and webhook secret) are stored in plain React state for the lifetime of the page. With React DevTools, browser back-button state restoration, or component re-mount…

### `src/server/app.ts`
- [ ] `src/server/app.ts:54-54` — [security · high] The `/health` SPA fallback requires a valid session, but health endpoints are typically probed by infrastructure (Kubernetes liveness/readiness probes, load balancers, uptime monitors, Cloudflare hea…

### `src/server/core/bitbucket.ts`
- [ ] `src/server/core/bitbucket.ts:18-31` — [security · high] Error body is retained on `BitbucketError.body` with only an inline comment forbidding direct logging. This is fragile: nothing structurally prevents a future caller from `console.log(err)` or serial…

### `src/server/core/crypto.ts`
- [ ] `src/server/core/crypto.ts:28-29` — [security · high] Weak key derivation: deriving an AES-GCM key from a passphrase via a single unsalted SHA-256 is not appropriate for a passphrase-derived key. A low-entropy secret is vulnerable to offline brute force…

### `src/server/core/logger.ts`
- [ ] `src/server/core/logger.ts:25-27` — [security · high] The redaction heuristic only recognizes strings beginning with `Bearer ` or any string whose entire value has exactly three dot-separated parts. Secrets embedded in larger strings (for example, an au…
- [ ] `src/server/core/logger.ts:58-60` — [security · high] `runWithContext` stores arbitrary context values, but `...store` and `...this.context` are written directly into the output without `redact()`. A caller can place credentials in context (including ne…
- [ ] `src/server/core/logger.ts:82-86` — [security · high] The `Error` branch passes `message` and `stack` directly to `log`, so they bypass `redact()`. Error messages and stack traces can contain bearer tokens, credentials, request data, or sensitive filesy…

### `src/server/core/oauth.ts`
- [ ] `src/server/core/oauth.ts:59-71, 73-81, 83-92` — [bug · critical] TOCTOU/state-reuse race in consumeOAuthState: a GET followed by a DELETE is not atomic against Cloudflare KV. Two concurrent callbacks presenting the same state will both observe a valid token and bo…  _(+2 related in cluster: security·high, bug·critical)_

### `src/server/core/sessions.ts`
- [ ] `src/server/core/sessions.ts:80-82` — [security · high] This assertion treats untrusted/persisted JSON as a valid session without runtime validation. Malformed data or schema drift can inject missing or incorrectly typed identity/authorization fields into…

### `src/server/core/webhook-ingest.ts`
- [ ] `src/server/core/webhook-ingest.ts:65-86` — [bug · critical] Two sources of truth for VCS provider on the inserted job: `findExistingJobForHead` / `supersedeOlderJobs` use `effectiveProvider`, but `insertJob` receives `vcsProvider` only when `reviewRequest.rep…

### `src/server/db/jobs.ts`
- [ ] `src/server/db/jobs.ts:895-911` — [security · high] `findExistingJobForHead` and `mostRecentJobForPullRequest` scope only by `(vcs_provider, owner, repo, pr_number)` — no `installation_id`/`workspace`-derived tenant key. If two GitHub Apps ever instal…

### `src/server/db/model-configs.ts`
- [ ] `src/server/db/model-configs.ts:85-93` — [security · high] `listLlmProviders` SELECTs `encrypted_api_key` from the DB even though the resulting `LlmProvider` only exposes `hasApiKey: boolean`. The secret material is fetched but discarded; any future change t…
- [ ] `src/server/db/model-configs.ts:160-165` — [security · high] `updateLlmProvider` conflates `undefined` (do not touch column) with `null` (clear the key) — but only `undefined` is checked: when `encryptedApiKey === undefined` the SQL skips updating the column,…

### `src/server/middleware/csrf.ts`
- [ ] `src/server/middleware/csrf.ts:13-15` — [security · high] This is not a complete CSRF check: it accepts any unsafe request whose caller supplies `X-Requested-With: XMLHttpRequest`. A custom header blocks simple cross-origin form submissions, but it does not…

### `src/server/models/catalog.ts`
- [ ] `src/server/models/catalog.ts:229-233` — [security · high] Error bodies may leak API keys/credentials. When the upstream provider echoes parts of the request (some proxies/dashboards do), the truncated body can include the `Authorization: Bearer ...` or `x-a…

### `src/server/models/cloudflare.ts`
- [ ] `src/server/models/cloudflare.ts:87-91` — [security · high] Stringly-typed error classification is fragile. `isLocalWorkersAiBindingError` and the quota check rely on substring matching (`'binding ai'`, `'run remotely'`, `'4006'`, `'daily free allocation'`) a…

### `src/server/models/google.ts`
- [ ] `src/server/models/google.ts:58-69` — [security · high] SSRF bypass via empty/whitespace/relative `baseUrl`: The guard `if (config.baseUrl && !isValidPublicUrl(config.baseUrl))` only runs when `baseUrl` is truthy and is a string. Then `(config.baseUrl ||…
- [ ] `src/server/models/google.ts:99-99` — [security · high] API key exposure via URL query string: The Gemini API key is placed in the query string (`?key=...`) rather than an `x-goog-api-key` header. URL query strings are commonly logged by proxies, CDNs, se…

### `src/server/models/openai.ts`
- [ ] `src/server/models/openai.ts:34-45, 52-58` — [security · critical] SSRF guard is incomplete. The `isPrivateIP` regex only matches dotted-quad IPv4 and the literal `::1`. It misses: (a) decimal/hex/octal IPv4 encodings (e.g. `2130706433`, `0x7f000001`), (b) IPv6 priv…  _(+1 related in cluster: security·critical, security·high)_

### `src/server/prompts/file-review.ts`
- [ ] `src/server/prompts/file-review.ts:58-58` — [security · high] Prompt-injection via unsanitized custom rules. `config.custom_rules` (sourced from `RepoConfig['review'].custom_rules`) is concatenated verbatim into both the system-affecting context and a 'Custom r…
- [ ] `src/server/prompts/file-review.ts:103-111` — [security · high] Prompt-injection via unescaped diff content. `hunk.header`, `line.content`, file paths, and `originalLineCount`/`lineCount` are concatenated directly into the user prompt with no escaping or fencing.…

### `src/server/routes/api/auth.ts`
- [ ] `src/server/routes/api/auth.ts:13-13` — [security · high] Session data exposure risk: The entire `sessionUser` object is returned verbatim to the client. If it contains sensitive fields (e.g., password hash, internal flags, roles, tokens), these will leak.…

### `src/server/routes/api/jobs.ts`
- [ ] `src/server/routes/api/jobs.ts:147-154` — [bug · critical] `retry/rerun/stop/delete` all perform a non-transactional read-then-act on the same job: `getJobForProcessing` -> mutate. Two near-simultaneous retries on the same id both pass the "not found" and "B…

### `src/server/routes/api/models.ts`
- [ ] `src/server/routes/api/models.ts:150-160` — [security · high] Sync endpoint exposes per-provider internal error messages to clients: `syncErrors[].error` contains the raw error message from `listProviderModels`/`decryptLlmApiKey`. Downstream HTTP/network errors…
- [ ] `src/server/routes/api/models.ts:211-222` — [security · high] Same exposure risk in `/sync` and provider create/update error paths: `error.message` from the encryption layer or DB drivers is forwarded verbatim. In particular, `isEncryptionConfigError` lets the…
- [ ] `src/server/routes/api/models.ts:363-369` — [security · high] Sensitive provider error messages may be leaked to clients: downstream review adapters (Cloudflare/OpenAI/Anthropic/Gemini) commonly include request URLs, token fragments (e.g. "Bearer xxx"), or stac…

### `src/server/routes/api/stats.ts`
- [ ] `src/server/routes/api/stats.ts:9-11` — [security · high] No range/length validation on `days` — callers can request `?days=999999999` or `?days=-1`. A negative or absurdly large value is forwarded straight into `getStats` and likely translated to a SQL `LI…

### `src/server/routes/api/vcs-credentials.ts`
- [ ] `src/server/routes/api/vcs-credentials.ts:71-73` — [security · high] Raw `error.message` is forwarded to the client (`jsonError(error.message, 400)`). The current contract relies on encryptSecret never embedding key material or secret contents in its error copy. There…

### `src/server/routes/auth-bitbucket.ts`
- [ ] `src/server/routes/auth-bitbucket.ts:7-10` — [security · high] Error message reflection in redirect (Pre-scan #2). `redirectToLogin` builds a query parameter `error=` from values that originate either from Bitbucket's `error` query string (attacker-controlled up…
- [ ] `src/server/routes/auth-bitbucket.ts:19-19` — [security · high] `c.env.BITBUCKET_AUTH_CALLBACK_URL` is taken from configuration and forwarded verbatim to Bitbucket's authorize and token endpoints. If the operator misconfigures this to a third-party URL, an attack…

### `src/server/routes/auth.ts`
- [ ] `src/server/routes/auth.ts:16-21, 17-21` — [security · critical] CSRF / login-CSRF risk: `consumeOAuthState` only validates the `state` parameter but does not bind it to the user's existing session cookie. Because the OAuth flow here is also used to *log a user in…  _(+1 related in cluster: security·critical, security·high)_
- [ ] `src/server/routes/auth.ts:46-50, 52-54` — [security · high] Allow-list uses only `profile.login` (GitHub username) without also checking a stable identifier such as `profile.id`. Usernames can be renamed on GitHub; if an allowed user renames their login, or i…  _(+1 related in cluster: security·high)_

### `src/server/routes/webhook-bitbucket.ts`
- [ ] `src/server/routes/webhook-bitbucket.ts:100-129` — [security · high] Critical trust-ordering bug: identity-projection parse happens BEFORE HMAC verification, and the parsed {workspace, repo_slug} is then used to look up the secret to verify. An unauthenticated attacke…

### `src/server/services/formatter.ts`
- [ ] `src/server/services/formatter.ts:67-67` — [security · critical] XSS: `comment.title` and `comment.body` are interpolated directly into HTML (`<strong>${title}</strong>` and the body string) without escaping. A malicious or malformed `ParsedReviewComment` (or stal…

### `src/server/services/github.ts`
- [ ] `src/server/services/github.ts:7-9` — [security · high] installationId is accepted as a free-form string with no validation (empty/invalid check). A misconfigured installationId silently authenticates against the wrong GitHub App installation, potentially…

### `worker-configuration.d.ts`
- [ ] `worker-configuration.d.ts:12-20` — [security · high] Sensitive secrets (`APP_PRIVATE_KEY`, `GITHUB_APP_WEBHOOK_SECRET`, `LLM_CONFIG_ENCRYPTION_KEY`, `CF_API_TOKEN`, `GITHUB_APP_ID`) are exposed on the `Env` interface, which is fine for server-side Work…

## P1 — bug·high / security·medium

### `.gitignore`
- [ ] `.gitignore:68-72` — [security · medium] The negation pattern `!.env.test.example` is correct, but if a developer ever creates a file like `.env.production.local` or `.env.staging`, the broad `.env.*` rule will silently exclude it from vers…

### `db/migrations/001_initial.sql`
- [ ] `db/migrations/001_initial.sql:306-313` — [bug · high] Silent non-hex re-encoding for commit_sha and base_sha: any value that is NOT pure even-length hex (e.g. an abbreviated SHA, a ref name, or input from a non-Git client) is silently UTF-8 encoded and…

### `db/migrations/002_jobs_async_review.sql`
- [ ] `db/migrations/002_jobs_async_review.sql:14-16, 23-25` — [bug · high] This migration mixes irreversible column drops with later statements that can fail, so a partial migration can permanently lose rpm/tpm/rpd data while leaving the schema only partially upgraded. Wrap…  _(+1 related in cluster: bug·high)_

### `db/migrations/003_vcs_provider_foundation.sql`
- [ ] `db/migrations/003_vcs_provider_foundation.sql:2-5` — [security · medium] CHECK constraint claims to be 'trivially extended' but actually requires a constraint swap. Adding a new provider (e.g. gitlab, gerrit) requires `ALTER TABLE repositories DROP CONSTRAINT ..._check, A…
- [ ] `db/migrations/003_vcs_provider_foundation.sql:20-32` — [bug · high] Partial-failure state on non-transactional DDL. Postgres autocommits each top-level DDL statement, so if the runner does not wrap the whole file in a transaction (and this file contains a DO block, w…

### `db/migrations/004_vcs_credentials.sql`
- [ ] `db/migrations/004_vcs_credentials.sql:18-18, 25-26` — [bug · high] **Focus area #1: Both `encrypted_access_token` and `encrypted_webhook_secret` are independently nullable, allowing a row with BOTH NULL.** The comment justifies partial rotation but never rules out t…  _(+2 related in cluster: security·medium, bug·high)_

### `db/migrations/006_review_comment_confidence.sql`
- [ ] `db/migrations/006_review_comment_confidence.sql:14-14` — [bug · high] No CHECK constraint enforcing the documented 0..1 range. The column comment and prompt contract both say confidence is `0.0 to 1.0`, and the rest of the code (model-output.ts clamps, Zod schema `.min…

### `index.html`
- [ ] `index.html:9-10, 14-14` — [bug · high] Module script uses an absolute path (`/src/client/main.tsx`) that depends entirely on the build tool/dev server (Vite) to resolve. If `index.html` is ever served as a static file (or the deployment t…  _(+1 related in cluster: security·medium, bug·high)_

### `scripts/apply_migrations.js`
- [ ] `scripts/apply_migrations.js:7-8, 8-9, 12-12, 13-14` — [bug · high] Concurrent invocations can both read the same maximum version and execute the same pending migrations. A unique constraint may only make one invocation fail after both have already attempted DDL, and…  _(+3 related in cluster: bug·high)_

### `scripts/fix-max-files.mjs`
- [ ] `scripts/fix-max-files.mjs:28-29` — [bug · high] If no `DATABASE_URL` is available, `postgres(null)`/`postgres(undefined)` can fail during client construction before `run()` is entered. That failure is not handled by the `try/catch`, and `sql.end()…
- [ ] `scripts/fix-max-files.mjs:39-43` — [bug · high] Errors are caught and logged but not rethrown and `process.exitCode` is never set, so a failed update can still make the process exit with status 0 and appear successful to CI or cron. Preserve the f…

### `scripts/migrate.mjs`
- [ ] `scripts/migrate.mjs:52-52` — [security · medium] The script reads `.dev.vars`, `.env.local`, and `.env` and uses the first `DATABASE_URL` it encounters, regardless of file permissions or whether the file is intended for this environment. In a share…
- [ ] `scripts/migrate.mjs:136-144` — [bug · high] This hand-written splitter does not implement PostgreSQL escape-string semantics. For a migration containing an `E'...'` literal with an escaped quote (or a backslash-escaped semicolon/quote), it can…

### `scripts/render-design.mjs`
- [ ] `scripts/render-design.mjs:113-114` — [bug · high] `stats.totals.jobs` is 1284 with `done: 1268` and `running: 16` while only 4 fixture jobs are provided. The dashboard will render aggregate numbers (totals, status counts) that contradict the visible…
- [ ] `scripts/render-design.mjs:148-151` — [bug · high] Vite server is spawned before the try block, but `server` is only killed in `finally`. If `spawn` itself throws (e.g., ENOENT for `node_modules/.bin/vite`), the error propagates without cleanup. Wrap…
- [ ] `scripts/render-design.mjs:210-212` — [bug · high] `server.kill('SIGTERM')` in the finally block is called unconditionally even when spawn failed and `server` may be undefined or the process already exited. Also, on Windows `kill('SIGTERM')` behaves…

### `scripts/setup-cloudflare.js`
- [ ] `scripts/setup-cloudflare.js:33-36` — [bug · high] Hyperdrive ID extraction regex `[a-f0-9]{32}` doesn't match actual Hyperdrive IDs, which Cloudflare typically returns as UUIDs (e.g., `a1b2c3d4-e5f6-...`, 36 chars with hyphens). If wrangler prints a…
- [ ] `scripts/setup-cloudflare.js:130-132` — [bug · high] `handleHyperdrive` only assigns the create-result of `extractId` if the create succeeds. If wrangler prints the new Hyperdrive ID on stdout but doesn't match `[a-f0-9]{32}` (see above), the function…

### `scripts/test.mjs`
- [ ] `scripts/test.mjs:14-14, 16-26` — [bug · high] Env parser does not strip inline comments or unescape values. A line like `KEY=foo # comment` will store `foo # comment` verbatim, and escape sequences other than `\\n` (e.g. `\\"`, `\\t`, leading `\…  _(+1 related in cluster: security·medium, bug·high)_

### `src/client/app.css`
- [ ] `src/client/app.css:1-1, 7-7` — [bug · high] The `@custom-variant dark (&:where(.dark, .dark *))` selector causes `dark:` utilities to match every descendant of any `.dark` element. This cascades into Radix portals (toasts, dialogs, popovers re…  _(+1 related in cluster: security·medium, bug·high)_

### `src/client/components/features/job-detail/constants.ts`
- [ ] `src/client/components/features/job-detail/constants.ts:5-9` — [bug · high] Tailwind JIT purge risk on dynamic opacity-suffixed classes: strings like `'bg-warning-bg/40'`, `'border-warning/20'`, `'text-warning/80'`, `'text-warning/60'`, `'bg-danger-bg'`, `'bg-warning-bg'`, `…

### `src/client/components/features/job-detail/job-header.tsx`
- [ ] `src/client/components/features/job-detail/job-header.tsx:118-125` — [bug · high] Re-run button is only gated by `isRerunning`, not by job status. Per the inline comment this is intentional, but the UI does not block re-runs while a job is `queued` or `running`, so a user can spaw…
- [ ] `src/client/components/features/job-detail/job-header.tsx:142-160` — [bug · high] No guard against double-submission in the confirm dialogs. `ConfirmDialog` (src/client/components/ui/confirm-dialog.tsx) does not expose a `pending`/`loading` prop and its confirm button stays enable…

### `src/client/components/features/job-detail/job-review-overview.tsx`
- [ ] `src/client/components/features/job-detail/job-review-overview.tsx:0-0` — [security · medium] `stripHeader` regex uses `^` without the multiline flag and removes only the first occurrence, which is fine — but the function is applied to `content` which itself was produced by `replace(...).trim…
- [ ] `src/client/components/features/job-detail/job-review-overview.tsx:39-40` — [bug · high] No null guard on `job.commitSha` and `job.files` before use. `hasOverview` only checks `summaryMarkdown`, `overallCorrectness`, and `overallConfidenceScore` — it does not validate `commitSha` or `fil…
- [ ] `src/client/components/features/job-detail/job-review-overview.tsx:57-61` — [bug · high] Inline `style` is chosen based on `job.overallCorrectness.toLowerCase().includes('incorrect')`. This is a substring match against a free-form string field whose casing/whitespace/exact phrasing is no…

### `src/client/components/features/models/model-chain.tsx`
- [ ] `src/client/components/features/models/model-chain.tsx:73-81` — [bug · high] Provider state can become stale when the value's model is missing or removed. Initial state uses `currentModel?.providerId ?? providers[0]?.value ?? ''`, but the sync effect only updates when a match…
- [ ] `src/client/components/features/models/model-chain.tsx:178-179` — [bug · high] Index-based React key can misalign component state when fallbacks change. `key={`${fb}-${i}`}` uses the array index. Removing or reordering fallbacks shifts indices and causes the inner `ModelSelecto…
- [ ] `src/client/components/features/models/model-chain.tsx:335-341, 345-354` — [bug · high] `max_lines` input accepts non-integers, negatives, and zero. `Number(e.target.value) || 1` coerces empty string, `0`, negatives, and floats to `1`. Although `min={1}` is set, browsers allow typing ne…  _(+1 related in cluster: bug·high)_

### `src/client/components/features/reviews/live-review-stepper.tsx`
- [ ] `src/client/components/features/reviews/live-review-stepper.tsx:39-41` — [bug · high] Unhandled status values (`cancelled`, `stopped`, or any future enum member) silently produce an empty `activeLabel`. The pill still renders — with the `queued` fallback colour — showing a blank chip…

### `src/client/components/features/stats/overview-stats.tsx`
- [ ] `src/client/components/features/stats/overview-stats.tsx:32-57, 36-42` — [bug · high] **Unstable array references cause needless re-renders / flicker** — `mockTrends` is memoized on `days`, but the `items` array (and the `Array(days).fill(0)` fallbacks) is rebuilt on every render, pro…  _(+1 related in cluster: bug·high)_

### `src/client/components/features/stats/stats-grid.tsx`
- [ ] `src/client/components/features/stats/stats-grid.tsx:34-36` — [bug · high] Using array index as React key can cause incorrect DOM reuse and stale state when items are reordered, inserted, or removed. Use a stable, unique identifier from `StatsItem` (e.g., add an `id` field…

### `src/client/components/layout/page-header.tsx`
- [ ] `src/client/components/layout/page-header.tsx:12-20` — [security · medium] Spreading arbitrary HTMLAttributes onto <header> lets callers inject props like onClick, onMouseOver, or unsafe aria-* values without validation. If this is intended as a pure presentational header,…

### `src/client/components/motion/shared-layout-bg.tsx`
- [ ] `src/client/components/motion/shared-layout-bg.tsx:67-82, 83-111` — [bug · high] Incorrect props typing with `any`. The cast `child as ReactElement<{ className?: string; onMouseEnter?: (e?: any) => void; children?: ReactNode }>` lies about the child's prop types and uses `any` on…  _(+1 related in cluster: bug·high)_

### `src/client/components/motion/smooth-scroll.tsx`
- [ ] `src/client/components/motion/smooth-scroll.tsx:105-109` — [bug · high] LenisBridge: useLenis() returns the Lenis instance itself, not the result of the selector callback. The selector here is being passed but its return value is discarded, which is a no-op side-effect.…
- [ ] `src/client/components/motion/smooth-scroll.tsx:194-197` — [bug · high] SmoothScroll: the `api` object is created via useMemo with deps [scrollY, progress, velocity, scrollTo], but its `lenis` field reads `lenisRef.current` at memoization time. Because `lenisRef.current`…
- [ ] `src/client/components/motion/smooth-scroll.tsx:263-268` — [bug · high] useSmoothScroll (fallback): this hook is meant to be used by consumers either inside or outside the provider, but the fallback unconditionally re-creates the `scrollTo` callback and motion values on…

### `src/client/components/motion/stepped-slider.tsx`
- [ ] `src/client/components/motion/stepped-slider.tsx:65-65` — [bug · high] Division by zero when `min === max`. `percent = ((current - min) / (max - min)) * 100` produces `NaN`/`Infinity` and `valueFromX` similarly divides by zero, breaking all position math. `clamp` will a…
- [ ] `src/client/components/motion/stepped-slider.tsx:143-151` — [bug · high] Pre-scan focus #1: Pointer capture is never released on unmount or when `disabled` flips mid-drag. If the component unmounts (or `disabled` becomes true) while a pointer is captured, the captured poi…
- [ ] `src/client/components/motion/stepped-slider.tsx:161-171, 173-190` — [bug · high] `releasePointerCapture` is called with optional chaining on the track div but `setPointerCapture` in `onPointerDown` is not gated similarly — if `setPointerCapture` is unsupported (older browsers, JS…  _(+1 related in cluster: bug·high)_

### `src/client/components/motion/tabs.tsx`
- [ ] `src/client/components/motion/tabs.tsx:103-108` — [bug · high] ARIA tabs pattern incomplete: triggers only handle `onClick`. The WAI-ARIA tabs pattern requires Left/Right (and Home/End) arrow-key navigation to move focus between tabs, a roving `tabIndex` (only t…
- [ ] `src/client/components/motion/tabs.tsx:156-164` — [bug · high] Tabs (`role="tab"`) is not associated with any `tabpanel`. Each `TabsContent` renders a plain `motion.div`/`div` with no `role="tabpanel"`, `id`, `aria-labelledby`, and no linking back to its trigger…

### `src/client/components/shared/sparkline.tsx`
- [ ] `src/client/components/shared/sparkline.tsx:10-24` — [bug · high] Gradient id collision across multiple instances: The hardcoded linearGradient id `colorValue` is a module-level literal. When the consumer (`StatsGrid`) renders multiple `<Sparkline>` components side…

### `src/client/components/ui/alert.tsx`
- [ ] `src/client/components/ui/alert.tsx:0-0` — [bug · high] Runtime safety: `variants[variant]` will be `undefined` if an invalid variant string reaches the component (e.g., from untyped external input, a typo, or browser extensions), causing a TypeError when…

### `src/client/components/ui/button.tsx`
- [ ] `src/client/components/ui/button.tsx:7-7` — [bug · high] Disabled semantics are broken when `asChild` is used with an anchor (`<a>`). HTMLAnchorElement does not honor the `disabled` attribute, so `disabled:pointer-events-none disabled:opacity-50` leaves a…

### `src/client/components/ui/card.tsx`
- [ ] `src/client/components/ui/card.tsx:50-50` — [bug · high] CardTitle is declared and given a displayName but is missing from the export list. Consumers cannot import it from this module even though it is clearly intended to be a public Card sub-component. Ad…

### `src/client/components/ui/confirm-dialog.tsx`
- [ ] `src/client/components/ui/confirm-dialog.tsx:36-39, 47-53` — [bug · high] onConfirm error swallowing & premature close: `onConfirm()` is invoked synchronously and `onOpenChange(false)` is called immediately after, regardless of whether onConfirm throws synchronously or ret…  _(+1 related in cluster: security·medium, bug·high)_

### `src/client/components/ui/dropdown-menu.tsx`
- [ ] `src/client/components/ui/dropdown-menu.tsx:138-146` — [bug · high] Focus restoration on close can fire after `AnimatePresence` has unmounted the panel, so `ctx.panelRef.current` is null and `getItems()` yields nothing; also `wasOpen.current` is read on every render…
- [ ] `src/client/components/ui/dropdown-menu.tsx:207-219` — [bug · high] Keyboard navigation does not implement WAI-ARIA menu pattern fully: Tab is not handled (should close the menu and return focus to the trigger), and there is no typeahead (character keys should move f…

### `src/client/components/ui/select.tsx`
- [ ] `src/client/components/ui/select.tsx:162-200` — [bug · high] Enter/Space on the trigger when closed re-opens the listbox instead of selecting the active option, but on browsers where Space dispatches a synthetic `click` on the same element, both `onClick` and…
- [ ] `src/client/components/ui/select.tsx:282-290, 284-287` — [bug · high] Portaled listbox DOM is rendered every render (even when closed). The `tabIndex={-1}` on options does not keep them out of programmatic focus, and a screen reader walking subtrees can still encounter…  _(+1 related in cluster: bug·high)_

### `src/client/components/ui/switch.tsx`
- [ ] `src/client/components/ui/switch.tsx:13-22` — [bug · high] Prop spread `{...props}` is placed AFTER `ref` and `onChange`, so any consumer passing `onChange` or `ref` via `...props` will silently override the component's internal wiring (in particular, the `o…  _(+1 related in cluster: bug·high)_

### `src/client/hooks/use-is-dark-mode.ts`
- [ ] `src/client/hooks/use-is-dark-mode.ts:4-6` — [bug · high] The lazy state initializer dereferences `document` during render. In SSR or non-DOM test environments this throws `ReferenceError: document is not defined`, preventing the component tree from renderi…

### `src/client/hooks/use-job-detail.ts`
- [ ] `src/client/hooks/use-job-detail.ts:59-59` — [bug · high] Stale-closure / race risk in the polling effect: `getPollDelay` and `schedulePolling` are recreated each render, and `setTimeout` captures whichever `fetchJob` was in scope when the timer was schedul…
- [ ] `src/client/hooks/use-job-detail.ts:71-74` — [bug · high] The status/nextRetryAt effect has no cleanup, so a rapid state change can fire two timers back-to-back. `schedulePolling` does call `stopPolling()` first, which mitigates the double-fire, but combine…

### `src/client/hooks/use-polling.ts`
- [ ] `src/client/hooks/use-polling.ts:11-14, 11-18` — [bug · high] Initial invocation ignores the `delay === null` contract: when `delay` is `null` the early return on line 15 skips the interval, but the initial `savedCallback.current()` call on line 13 still runs u…  _(+1 related in cluster: bug·high)_

### `src/client/lib/theme.tsx`
- [ ] `src/client/lib/theme.tsx:44-55, 57-59` — [bug · high] FOUC risk: module-level `applyTheme(initial)` runs *before* `ThemeProvider` mounts, setting `<html data-theme="...">` and the `dark` class. If anything in the import graph (CSS loaders, font scripts,…  _(+1 related in cluster: bug·high)_
- [ ] `src/client/lib/theme.tsx:77-87` — [bug · high] System theme listener does not sync DOM: the media-query `change` handler updates React state via `setThemeState` but never calls `applyTheme`. So when the user has no stored preference and the OS th…

### `src/client/lib/vcs.ts`
- [ ] `src/client/lib/vcs.ts:18-24, 31-33` — [bug · high] When a Bitbucket repository has neither `repositoryWorkspace` nor `owner`, `encodeURIComponent(undefined)` produces the literal string `undefined`, so callers receive a plausible-looking but invalid…  _(+1 related in cluster: bug·high)_

### `src/client/main.tsx`
- [ ] `src/client/main.tsx:56-66` — [bug · high] ErrorBoundary has no reset mechanism — once an error is caught, the boundary stays in error state for the lifetime of the mount. Because the same ErrorBoundary instance is reused across route changes…
- [ ] `src/client/main.tsx:79-85, 87-114` — [bug · high] withSuspense is called inside route definitions which is fine, but the resulting ErrorBoundary instances are not keyed by location, so a thrown error in `/jobs/:id` persists when navigating to `/dash…  _(+1 related in cluster: bug·high)_

### `src/client/pages/dashboard.tsx`
- [ ] `src/client/pages/dashboard.tsx:26-42` — [bug · high] Race condition / stale data in `load()`: the manual refresh button (`load(true)`) can fire concurrently with an in-flight poll cycle from `usePolling`. Because there is no in-flight guard, request se…

### `src/client/pages/job-detail.tsx`
- [ ] `src/client/pages/job-detail.tsx:24-42` — [bug · high] Error is never surfaced when the initial fetch fails. If `useJobDetail` errors out, `job` stays null and this component returns the skeleton indefinitely — the `<Alert>` on the success branch is unre…

### `src/client/pages/job-logs.tsx`
- [ ] `src/client/pages/job-logs.tsx:167-168` — [bug · high] `job.commitSha.slice(0, 7)` (line 167) is called unconditionally, but `commitSha` is typed as nullable elsewhere in the schema (e.g., `headRef`, `baseSha` patterns) and may be missing for queued/pend…

### `src/client/pages/jobs.tsx`
- [ ] `src/client/pages/jobs.tsx:27-47` — [bug · high] Race condition between manual refresh and polling: there is no request sequencing guard. If a polling tick fires while `load(true)` is in flight (or vice versa), the later-resolving response can over…

### `src/client/pages/login.tsx`
- [ ] `src/client/pages/login.tsx:43-53` — [bug · high] Unhandled promise rejection in session probe. `api.probeSession()` has no `.catch` handler; if it rejects, an unhandled rejection is thrown and the user gets no feedback and no redirect. Wrap in try/…

### `src/client/pages/repos.tsx`
- [ ] `src/client/pages/repos.tsx:255-262` — [bug · high] Modal's `useEffect` resets the form whenever `globalRouteKey` changes. If the user has unsaved edits in the modal and the global config refreshes (e.g., via `handleSync` → `loadRepos()`), the in-flig…
- [ ] `src/client/pages/repos.tsx:434-440` — [bug · high] `handleToggleEnabled` does an optimistic-but-not-really update: it only calls `mergeRepo` after the API call succeeds, and on failure it never surfaces the error to the page-level `error` state nor r…

### `src/client/pages/repos/add-bitbucket.tsx`
- [ ] `src/client/pages/repos/add-bitbucket.tsx:27-27` — [security · medium] Input validation gaps: `canSubmit` only checks that trimmed strings are non-empty. Workspace and repo slug should be validated against Bitbucket's allowed character set (lowercase letters, digits, hy…

### `src/client/pages/settings.tsx`
- [ ] `src/client/pages/settings.tsx:370-371` — [bug · high] The debounced autosave can leave an older request in flight while a newer edit starts another request. If the older `updateGlobalConfig` resolves last, it updates `savedGlobalConfig` with the stale r…
- [ ] `src/client/pages/settings.tsx:504-508` — [bug · high] Clearing a provider key only disables the provider locally and deletes its credential; it does not refresh or invalidate the model catalog. The existing `configs` and route selections can therefore c…

### `src/server/app.ts`
- [ ] `src/server/app.ts:19-21, 27-28` — [security · medium] `serveIndex` derives the base URL from `c.req.url`, which contains the attacker-controllable `Host` header. `new URL('/index.html', c.req.url)` will resolve the asset fetch against that tainted origi…  _(+1 related in cluster: security·medium)_

### `src/server/core/bitbucket-oauth.ts`
- [ ] `src/server/core/bitbucket-oauth.ts:18-23, 31-39` — [bug · high] Missing fetch error / timeout handling: both `exchangeBitbucketOAuthCode` and `fetchBitbucketOAuthProfile` rely on `fetch` but do not handle network rejections (e.g. DNS, connection reset) or aborts/…  _(+1 related in cluster: security·medium, bug·high)_
- [ ] `src/server/core/bitbucket-oauth.ts:49-51` — [security · medium] Hardcoded external API endpoint and User-Agent: 'https://api.bitbucket.org/2.0/user' and 'codra-app' are hardcoded. Business-related URL should be configured via env; the User-Agent string 'codra-app…

### `src/server/core/bitbucket.ts`
- [ ] `src/server/core/bitbucket.ts:57-66` — [bug · high] Retry classification uses `catch (error: any)` and string-sniffs for 'timeout' in the message. This is overly broad: any Error whose message contains the substring 'timeout' (e.g., a non-transient va…

### `src/server/core/config.ts`
- [ ] `src/server/core/config.ts:51-54` — [bug · high] Global config update is not atomic with revision bump: `updateGlobalConfig` first writes the new global value and then bumps the revision key in a separate KV `put`. A concurrent reader can observe t…
- [ ] `src/server/core/config.ts:70-73` — [bug · high] Unsafe cast of cached JSON payload: After a KV cache hit, `cached as CachedConfig` is returned without any schema validation. A malformed, stale, or poisoned KV value (e.g., missing `parsedJson` or `…
- [ ] `src/server/core/config.ts:95-102` — [bug · high] Side-effect (DB write) inside a read path on cache miss: `loadRepoConfig` is documented/used as a read, but on a cache miss with no existing DB record it performs `syncRepoConfig` (a write) before re…

### `src/server/core/crypto.ts`
- [ ] `src/server/core/crypto.ts:54-64` — [security · medium] Missing IV length validation in decrypt: `fromBase64(ivBase64)` is passed directly to AES-GCM without checking it decodes to the expected 12 bytes. A malformed/truncated/oversized IV produces a confu…

### `src/server/core/diff.ts`
- [ ] `src/server/core/diff.ts:102-106` — [bug · high] When a diff uses a mode-only 'new file mode' or 'deleted file mode' line without a preceding 'diff --git' header (which can occur in some git outputs, e.g. when only mode/permission changes are repor…

### `src/server/core/github-oauth.ts`
- [ ] `src/server/core/github-oauth.ts:51-51` — [security · medium] `access_token` is returned as a bare `string` and passed across module boundaries. There is no helper to redact it from logs/serialized errors, and the token can leak via thrown errors if upstream co…
- [ ] `src/server/core/github-oauth.ts:63-63` — [bug · high] `fetchGitHubOAuthProfile` does not validate required profile fields (`id`, `login`). The cast `as GitHubOAuthProfile` is a structural lie — if GitHub returns a partial/empty object (e.g., due to a ne…
- [ ] `src/server/core/github-oauth.ts:74-74` — [security · medium] `signedInAt` is generated from the local wall clock via `new Date().toISOString()`. For security-relevant session timestamps (replay window checks, audit, etc.) the value should come from a trusted/a…

### `src/server/core/github.ts`
- [ ] `src/server/core/github.ts:26-28` — [bug · high] The retry predicate dereferences `error.message` for every non-`GitHubError` value. JavaScript/TypeScript code can throw strings, `null`, or other values, so a non-retryable primitive/null rejection…
- [ ] `src/server/core/github.ts:212-216` — [bug · high] A cached installation token is returned solely because it has a truthy `token`; the KV record's `expiresAt` is not validated here. A stale, malformed, or manually corrupted KV record can therefore be…
- [ ] `src/server/core/github.ts:536-537` — [bug · high] If inline comments are rejected with 422, this fallback posts a different review that omits every comment but still returns success. Callers cannot distinguish a complete review from one where all re…

### `src/server/core/job-recovery.ts`
- [ ] `src/server/core/job-recovery.ts:11-21` — [bug · high] Requeue loop has no per-iteration isolation: if `env.REVIEW_QUEUE.send` throws after some jobs were already requeued, the outer `try/catch` logs once and the loop exits, leaving later `requeuedJobIds…
- [ ] `src/server/core/job-recovery.ts:91-92, 91-101` — [bug · high] `statusRef` can still be `''` and reach `vcs.updateStatusCheck`. The early `continue` only fires when *both* `check_run_id` and `status_check_ref` are null, but the `??` chain here collapses `null` c…  _(+1 related in cluster: bug·high)_

### `src/server/core/model-output.ts`
- [ ] `src/server/core/model-output.ts:304-318` — [bug · high] findReviewObject priority-3 fallback can silently pick an unrelated object. When none of the array elements contain a 'findings' array, the third-tier fallback picks the first object containing ANY o…
- [ ] `src/server/core/model-output.ts:329-339` — [bug · high] confidence_score normalization assumes a 0-10 scale whenever the value exceeds 1, which is wrong for percentage (0-100) and arbitrary scales. A value of 95 becomes 0.95 (acceptable), but 95 also work…

### `src/server/core/oauth.ts`
- [ ] `src/server/core/oauth.ts:22-27` — [security · medium] No size cap on the allowlist input: parseAllowedUsersByProvider accepts an arbitrarily large string, allocates a Set with unbounded membership, and is documented as environment-sourced. Even from a t…
- [ ] `src/server/core/oauth.ts:45-56` — [bug · high] Coercion via String(value) silently turns non-string JSON members (numbers, booleans, nested objects/arrays) into numeric strings or the literal '[object Object]', which then enter the Bitbucket set…

### `src/server/core/review.ts`
- [ ] `src/server/core/review.ts:1396-1402` — [bug · high] Numeric `reviewId` guard in `runFinalizePhase` will throw for any provider (today: Bitbucket) whose `submitReview` returns a non-numeric `ref`. The comment claims this is intentional and will be "fix…
- [ ] `src/server/core/review.ts:1461-1465` — [bug · high] Wait -- the labelMap on line 1461 maps `comment` to `labels.p2` (green/'approve' color) and `approve` to `labels.p1` (orange/'comment' color). These are SWAPPED relative to what the names suggest --…

### `src/server/core/sessions.ts`
- [ ] `src/server/core/sessions.ts:36-39` — [security · medium] Creating a new session leaves the session referenced by an existing cookie active in KV until its TTL. This permits concurrent stale credentials (especially problematic after login or privilege chang…
- [ ] `src/server/core/sessions.ts:75-80` — [security · medium] Validate the attacker-controlled cookie before using it in a KV key. Tokens generated here are exactly 64 lowercase hexadecimal characters; rejecting anything else avoids needless reads for malformed…

### `src/server/core/timeout.ts`
- [ ] `src/server/core/timeout.ts:22-24` — [bug · high] Caller-initiated aborts are misclassified as TimeoutError. The `err?.name === 'AbortError'` branch is too broad: if the caller (or `fn` itself) aborts the controller for a reason unrelated to the tim…

### `src/server/core/token-tracker.ts`
- [ ] `src/server/core/token-tracker.ts:95-99, 104-106` — [bug · high] merge() double-counts tokens and miscounts calls. merge() iterates `other.getBreakdown()` and calls `record()` for each entry, but `record()` appends the aggregated token totals and increments `calls…  _(+1 related in cluster: bug·high)_

### `src/server/core/verify.ts`
- [ ] `src/server/core/verify.ts:9-21` — [security · medium] Hex validation accepts arbitrarily short signatures (down to a single byte). `crypto.subtle.verify` will process any length, so a 2-char hex input becomes a 1-byte HMAC that passes the parser and is…
- [ ] `src/server/core/verify.ts:41-54` — [security · medium] Silently swallowing all errors from `crypto.subtle.importKey` / `verify` and returning `false` masks real runtime failures (e.g., `secret` containing unencodable characters in some environments, or p…

### `src/server/core/webhook-ingest.ts`
- [ ] `src/server/core/webhook-ingest.ts:47-47, 51-65` — [bug · high] Operator precedence bug on `effectiveProvider`: the `??` operator binds tighter than `as`, so this parses as `(input.provider ?? reviewRequest?.repositoryVcsProvider ?? 'github') as const`. That work…  _(+1 related in cluster: bug·high)_
- [ ] `src/server/core/webhook-ingest.ts:88-116` — [bug · high] Orphan job on queue-send failure: `insertJob` and `supersedeOlderJobs` have already mutated durable DB state before `env.REVIEW_QUEUE.send` is awaited. If the queue send throws (network blip, produce…

### `src/server/db/client.ts`
- [ ] `src/server/db/client.ts:31-34` — [bug · high] This does not provide normal nested-transaction semantics. If `innerFn` throws and its caller catches that error, statements already issued by `innerFn` remain part of the outer transaction and may s…

### `src/server/db/file-reviews.ts`
- [ ] `src/server/db/file-reviews.ts:27-30` — [bug · high] Concurrent upserts for the same `(job_id, file_path)` can interleave after the row-level conflict update: one transaction may delete comments inserted by the other, or both may insert their own sets,…
- [ ] `src/server/db/file-reviews.ts:160-163` — [bug · high] The conflict update is unconditional, so an older async result or retry can overwrite a newer terminal/pending state and its request identifiers. In particular, defaulting omitted `asyncRequestId`/`a…

### `src/server/db/jobs.ts`
- [ ] `src/server/db/jobs.ts:1221-1226` — [bug · high] `supersedeOlderJobs` silently swallows the GitHub branch when `installationId` is omitted: a caller passing `vcsProvider: 'github'` with `installationId: undefined` returns 0 with no signal. This is…

### `src/server/db/model-configs.ts`
- [ ] `src/server/db/model-configs.ts:95-95` — [security · medium] All functions that return secrets (`getLlmProvider`, `listLlmProviderSecrets`, `getResolvedModelConfig`, `upsertDiscoveredModelConfigs` via its CTEs) must be gated by authorization at the caller laye…
- [ ] `src/server/db/model-configs.ts:105-105` — [bug · high] `getLlmProvider` returns `LlmProviderSecret` (which includes `encryptedApiKey`), but the public `listLlmProviders` / `findLlmProviderByName` return `LlmProvider` (which does not). This asymmetry is i…
- [ ] `src/server/db/model-configs.ts:186-202` — [bug · high] TOCTOU race in `deleteLlmProvider`: between the `SELECT COUNT(*)` against `model_configs` and the `DELETE FROM llm_providers`, a concurrent transaction can insert a `model_config` referencing this pr…
- [ ] `src/server/db/model-configs.ts:331-358` — [bug · high] `upsertDiscoveredModelConfigs` slug uniqueness check is racy: the `usedModelIds` set is built from a snapshot read and candidate ids are reserved only in memory. Two concurrent calls for the same pro…

### `src/server/db/stats.ts`
- [ ] `src/server/db/stats.ts:15-21` — [bug · high] Promise.all is awaited without per-query error handling. If any one of the 10 parallel queries rejects (e.g. schema drift, missing column like `overall_confidence_score`, transient DB hiccup, type-ca…

### `src/server/db/vcs-credentials.ts`
- [ ] `src/server/db/vcs-credentials.ts:0-0` — [bug · high] omittedCount miscomputation: it's computed as reviewedComments.length - Math.min(finalComments.length, effectiveMaxComments), but reviewedComments is the pre-filter set while finalComments is post-se…

### `src/server/db/webhook-deliveries.ts`
- [ ] `src/server/db/webhook-deliveries.ts:23-30, 23-42` — [bug · high] Silent wrong-row attribution if (owner, repo) is not unique in `repositories`. The destructuring `[repoRow] = ...` picks an arbitrary row when the predicate matches multiple. If `repositories` has du…  _(+1 related in cluster: bug·high)_

### `src/server/env.ts`
- [ ] `src/server/env.ts:4-6` — [security · medium] Loose `any` return type leaks untyped data throughout the codebase. Workers AI responses are JSON with known shapes per model; using `Promise<any>` forces every caller to cast or `as`-assert and defe…

### `src/server/index.ts`
- [ ] `src/server/index.ts:40-42` — [bug · high] The read/check/delete sequence is not atomic. A producer can observe the flag as present and skip its KV write while this handler is between `hasPendingMaintenanceWork()` and `clearSystemActive()`; t…
- [ ] `src/server/index.ts:99-106` — [bug · high] The duplicate check relies on substring matching an exception message. An unrelated failure whose message happens to contain `instance.already_exists` will be acknowledged and permanently dropped, wh…

### `src/server/middleware/auth.ts`
- [ ] `src/server/middleware/auth.ts:6-17` — [security · medium] No CSRF protection on session-authenticated requests. The session cookie is `SameSite=Lax`, which blocks cross-site POSTs from form submissions, but it does NOT protect against same-site CSRF or agai…

### `src/server/middleware/observability.ts`
- [ ] `src/server/middleware/observability.ts:6-6, 9-24` — [bug · high] If `next()` throws, the async function rejects, `runWithContext` propagates the rejection, and the `Request completed` log line (including status and durationMs) is never emitted. Errors also aren't…  _(+1 related in cluster: security·medium, bug·high)_

### `src/server/models/anthropic.ts`
- [ ] `src/server/models/anthropic.ts:50-53, 60-65` — [bug · high] Empty-response guard is unreachable and ineffective. When `data.content` is absent/empty, `rawText` is already `''`, so the guard falls through and the code then prepends `'{'`, yielding the string `…  _(+1 related in cluster: security·medium, bug·high)_

### `src/server/models/catalog.ts`
- [ ] `src/server/models/catalog.ts:196-198` — [security · medium] Cloudflare 401/403 silently falls back to a hardcoded model list, which masks credential misconfiguration from the user. If a user provides an invalid API token, they will see the curated default lis…

### `src/server/models/cloudflare.ts`
- [ ] `src/server/models/cloudflare.ts:15-15` — [bug · high] Retry loop never actually retries. CLOUDFLARE_MAX_RETRIES is 0, so `attempt <= maxRetries` only allows attempt 0. The `attempt < maxRetries` guards inside the loop body are tautologically false, maki…
- [ ] `src/server/models/cloudflare.ts:332-336` — [bug · high] Race between env.AI.run and the timeout: runPromise.catch(() => {}) is attached *after* Promise.race is set up. If the underlying run rejects synchronously (or its returned promise rejects) before th…

### `src/server/models/google.ts`
- [ ] `src/server/models/google.ts:149-150` — [security · medium] `providerErrorMessage(errorText)` may include raw API key fragments or PII: The error body from a non-ok response is logged via `logger.warn`/`logger.error` with the full `message`. If the API echoed…

### `src/server/models/limits.ts`
- [ ] `src/server/models/limits.ts:81-87` — [bug · high] Leaked waiter slot on abandoned acquire(). The promise stored in `waiters` only resolves via `release()` handing it the slot. If a caller awaits `acquire()` inside a timeout/AbortController and aband…

### `src/server/models/openai.ts`
- [ ] `src/server/models/openai.ts:80-84, 87-87` — [bug · high] Quota accounting is incorrect on failure. `tracker.incrementSubrequests(1)` is called before the network attempt, but if `fetch` throws (network error) or `withTimeout` aborts, or `response.ok` is fa…  _(+1 related in cluster: bug·high, security·medium)_
- [ ] `src/server/models/openai.ts:110-111` — [bug · high] Error path from `response.json()` is not wrapped. If the upstream returns a non-JSON body or malformed JSON, `response.json()` rejects with a `SyntaxError`/`TypeError`, which propagates as an uncaugh…

### `src/server/prompts/languages.ts`
- [ ] `src/server/prompts/languages.ts:102-104` — [bug · high] Path parsing is fragile and misidentifies extensions. `path.split('.').pop()` returns the last dotted segment, so: - `'.env'` -> 'env' (wrong, this is a dotfile, not an env file) - `'.eslintrc.json'`…

### `src/server/prompts/summary.ts`
- [ ] `src/server/prompts/summary.ts:34-34` — [security · medium] `f.path` and `f.summary` are interpolated directly into a prompt string. If these originate from PR data (filenames, diff text), control characters, prompt-injection payloads (e.g. `"ignore previous…
- [ ] `src/server/prompts/summary.ts:54-54` — [bug · high] Output format contract mismatch risk: the SYSTEM prompt explicitly forbids markdown, code fences, intro text, and requires a JSON object with a single `"summary"` key, yet `buildSummaryPrompt` return…

### `src/server/routes/api/jobs.ts`
- [ ] `src/server/routes/api/jobs.ts:18-28` — [bug · high] `terminateJobWorkflow` swallows every error (including potentially transient CF API failures, network blips, auth errors, etc.), logging only at `info`. After it returns, `stop` immediately calls `ca…
- [ ] `src/server/routes/api/jobs.ts:49-51` — [bug · high] Schema validation is performed but then immediately discarded via `as any`, which silently disables the entire purpose of `jobsQuerySchema.parse`. Any extra/unknown or malformed query parameters will…
- [ ] `src/server/routes/api/jobs.ts:126-143` — [bug · high] `insertJob` and `REVIEW_QUEUE.send` are not wrapped in a try/catch. If `supersedeOlderJobs` or `REVIEW_QUEUE.send` throws after `insertJob` succeeded, the caller receives a 500 but a job row already…

### `src/server/routes/api/models.ts`
- [ ] `src/server/routes/api/models.ts:75-81` — [bug · high] API key precedence ambiguity in `encryptedApiKeyFromBody`: if `clearApiKey=true` is supplied together with a non-empty `apiKey`, the function returns `null` (clearing) and silently drops the newly pr…
- [ ] `src/server/routes/api/models.ts:260-274` — [bug · high] In the PATCH handler, when `input.clearApiKey` is `true` and `input.apiKey` is also supplied, `encryptedApiKeyFromBody` returns `null` (clearing) due to the precedence bug above, overriding what is o…

### `src/server/routes/api/repos.ts`
- [ ] `src/server/routes/api/repos.ts:99-99` — [bug · high] Do not use only successfully synced repositories as the stale-deletion source. A transient `syncRepoConfig` failure returns `null`, so an existing repository is omitted from `installationSynced` and…
- [ ] `src/server/routes/api/repos.ts:199-204` — [bug · high] The transaction callback ignores the transaction client. Both helpers receive `c.env` and resolve the ambient/default DB client themselves, so atomicity depends on undocumented context behavior and c…
- [ ] `src/server/routes/api/repos.ts:222-222, 225-228` — [security · medium] The response returns the complete credential record immediately after persisting encrypted secrets. Depending on `upsertVcsCredential`'s return shape, this can expose encrypted token/webhook-secret m…  _(+1 related in cluster: security·medium)_

### `src/server/routes/api/settings.ts`
- [ ] `src/server/routes/api/settings.ts:34-36, 34-37` — [bug · high] PATCH performs a read-modify-write on global_settings with no locking, no version check, and no transaction. Two concurrent PATCH requests can both read the same current settings, each merges only it…  _(+1 related in cluster: bug·high)_

### `src/server/routes/api/stats.ts`
- [ ] `src/server/routes/api/stats.ts:9-11` — [bug · high] `parseInt(daysParam, 10)` silently accepts non-numeric input, returns `NaN` (e.g. for `?days=abc`), and that `NaN` is then forwarded to `getStats`. `Number.isNaN(days)` must be checked and the reques…

### `src/server/routes/api/vcs-credentials.ts`
- [ ] `src/server/routes/api/vcs-credentials.ts:33-35` — [security · medium] isEncryptionConfigError only matches when `error instanceof Error`. If encryptSecret throws a raw string, an object with no `.message`, or a non-Error value, the check returns false and the route re-…

### `src/server/routes/auth-bitbucket.ts`
- [ ] `src/server/routes/auth-bitbucket.ts:46-47` — [security · medium] Missing null/undefined guard on `profile.account_id` before the allow-list `Set.has(...)` lookup (Pre-scan #5). If `fetchBitbucketOAuthProfile` ever returns a profile with an empty/missing `account_i…
- [ ] `src/server/routes/auth-bitbucket.ts:56-58, 59-65` — [security · medium] Session-rotation ordering is correct (destroy before create), but the redirect response is issued immediately after `createSession` returns without awaiting cookie propagation on the response object.…  _(+1 related in cluster: security·medium)_

### `src/server/routes/auth.ts`
- [ ] `src/server/routes/auth.ts:7-10` — [security · medium] Open-redirect risk via `redirectToLogin`: the `reason` parameter is taken straight from `c.req.query('error')` (set by GitHub) and other server-controlled strings, then placed into `URLSearchParams`.…

### `src/server/routes/webhook-bitbucket.ts`
- [ ] `src/server/routes/webhook-bitbucket.ts:142-146` — [bug · high] The discriminated-union dispatch is fragile. The handler builds `envelope = { eventName: xEventKey, ...parsedBody }` — but if `parsedBody` is itself an object and contains its own `eventName` key, th…

### `src/server/routes/webhook.ts`
- [ ] `src/server/routes/webhook.ts:15-16` — [security · medium] `c.req.text()` buffers the complete unauthenticated request body before signature verification, with no route-level size limit. An attacker can repeatedly send very large bodies (even with no signatu…
- [ ] `src/server/routes/webhook.ts:42-46` — [bug · high] The delivery is persisted before config loading/extraction/ingestion completes. If any later DB/config/queue operation throws, GitHub receives a 500 and retries the same delivery ID, but the retry im…

### `src/server/services/formatter.ts`
- [ ] `src/server/services/formatter.ts:28-30` — [security · medium] `baseUrl` is interpolated into HTML attributes (`<img src="${iconBase}/...">`) and Markdown link targets without any validation or escaping. If `baseUrl` ever contains `"`, `>`, whitespace, or a `jav…
- [ ] `src/server/services/formatter.ts:48-49` — [bug · high] `stripLeadingTags` is over-eager: `[:\-\s\uFE0F]|[^\w\s]` in the character class matches any non-word/non-space character at the start, so legitimate opening punctuation (quotes, parentheses, em-dash…
- [ ] `src/server/services/formatter.ts:60-65` — [bug · high] Title-dedup heuristic is too loose: `startsWith` on lowercased strings will strip the first line whenever the title is a *prefix of a word* on that line (e.g. title `'fix'` matches a body line `'fixi…

### `src/server/services/model.ts`
- [ ] `src/server/services/model.ts:0-0` — [bug · high] isTransientModelFailure uses overly broad substring matches that will misclassify permanent errors as transient: `lower.includes('network')` matches messages like "no network access configured" or "n…
- [ ] `src/server/services/model.ts:495-499` — [bug · high] lastError is declared `let lastError: unknown;` (undefined) and only assigned inside the loop body. If every model is skipped via `continue` (e.g. all `resolveModel` calls fail, or every provider is…
- [ ] `src/server/services/model.ts:611-614` — [bug · high] Same `throw lastError;` issue exists in `generateSummary` at the bottom of the fallback loop. If every summary model is skipped via `continue` (all `resolveModel` failures or all providers marked una…

### `src/server/services/vcs.ts`
- [ ] `src/server/services/vcs.ts:0-0` — [bug · high] Default-provider fall-through masks unknown values: any `repositoryVcsProvider` that is not the literal string 'bitbucket' (including `null`, `undefined`, typos like 'bitbucet', 'gitlab', etc.) silen…  _(+1 related in cluster: bug·high, security·medium)_
- [ ] `src/server/services/vcs.ts:68-73, 75-75` — [bug · high] Empty-commit risk is not guarded at the seam: `job.headSha ?? job.commitSha ?? null` resolves to `null` when both are absent, and the spread `{ ...job, headSha }` then sets `headSha: null` on the Bit…  _(+1 related in cluster: bug·high)_

### `src/server/vcs/bitbucket.ts`
- [ ] `src/server/vcs/bitbucket.ts:174-174` — [bug · high] `headSha ?? ''` silently produces an empty commit path component, so `upsertCodeInsightsReport` and `postCommitBuildStatus` issue requests against `<workspace>/<repo>/commit//reports/codra-review` an…
- [ ] `src/server/vcs/bitbucket.ts:203-203` — [bug · high] Same `headSha ?? ''` silent-degradation pattern in the build-status POST. Pair the guard with the report PUT fix so both calls share a single validated `headSha` local; otherwise the second request s…
- [ ] `src/server/vcs/bitbucket.ts:352-364` — [bug · high] dedup index misclassifies 'context' line_type as 'added' — `from !== undefined ? 'removed' : 'added'` drops the context case entirely, so an existing PR comment posted on a context line is indexed as…

### `src/server/vcs/github.ts`
- [ ] `src/server/vcs/github.ts:70-72` — [bug · high] Focus area #2 (partial): The `typeof id !== 'number'` guard correctly rejects null/undefined/string/boolean but does NOT reject `NaN` — `typeof NaN === 'number'` is `true`. If GitHub's API ever retur…
- [ ] `src/server/vcs/github.ts:83-89` — [bug · high] Focus area #2 (update-side): `updateStatusCheck` performs the symmetric `ref -> id` conversion with `Number(ref)` but does NOT mirror the create-side validation. A corrupt ref (e.g. round-trip of a f…
- [ ] `src/server/vcs/github.ts:105-111` — [bug · high] `submitReview` does NOT mirror the createStatusCheck id-validation guard. `GitHubService.createReview` ultimately returns `(await response.json()) as { id: number }` (see `core/github.ts:566`), so a…
- [ ] `src/server/vcs/github.ts:120-121` — [bug · high] Focus area #4: `findExistingReviewForCommit` injects `this.env.BOT_USERNAME` directly into `findBotReviewForCommit` without validating it is non-empty. In `core/github.ts:591-595`, `findBotReviewForC…

### `src/server/workflows/review.ts`
- [ ] `src/server/workflows/review.ts:25-33` — [bug · high] `bind-workflow-id` step is not idempotent against workflow replay / newer instanceIds. On replay the step body re-executes and overwrites whatever `workflowInstanceId` is currently stored. If the wor…
- [ ] `src/server/workflows/review.ts:43-48` — [bug · high] The `while (phase)` loop has no upper bound on `attempt` or on consecutive `retry` results. `step.do` step names like `run-${phase}-${attempt}` are unique per attempt and persisted in the workflow lo…
- [ ] `src/server/workflows/review.ts:72-86` — [bug · high] Fresh-instance handoff is silently skipped when no jobId is resolvable. If both `result.jobId` and the initial `jobId` are undefined, the `if (nextJobId)` check fails and execution falls through to t…  _(+1 related in cluster: bug·high)_

### `src/shared/api.ts`
- [ ] `src/shared/api.ts:50-52` — [security · medium] Most response shapes are type-only and have no Zod runtime parser. `AuthSessionResponse` in particular wraps `user: AuthSessionUser`, which IS backed by a runtime schema, but the wrapper itself isn't…

### `src/shared/bitbucket.ts`
- [ ] `src/shared/bitbucket.ts:132-132` — [security · medium] The OAuth response's `avatar` field is accepted as any string, unlike `links.avatar.href`, which is URL-validated. If consumers use this provider field when constructing an image or link, an attacker…

### `src/shared/schema.ts`
- [ ] `src/shared/schema.ts:61-64` — [bug · high] `summaryModelOutputSchema` is declared as `z.union([z.array(...), z.object(...)])`. Zod's default for a union is to try the FIRST member; if a provider returns the object shape, parsing fails because…
- [ ] `src/shared/schema.ts:481-482, 483-484` — [bug · high] `vcsCredentialStoreSchema` normalizes `workspace`/`repoSlug` to lowercase via `.trim().toLowerCase()`. However `repoConfigRecordSchema` does NOT lowercase `owner`/`repo` when storing `parsedJson` — s…  _(+1 related in cluster: bug·high, security·medium)_

### `test/github-fetch-mock.ts`
- [ ] `test/github-fetch-mock.ts:53-60` — [bug · high] Body parsing only handles string bodies. If GitHubClient ever posts a Blob, FormData, ArrayBuffer, ReadableStream, URLSearchParams, or a plain object, init.body won't be a string and body silently be…
- [ ] `test/github-fetch-mock.ts:126-134` — [bug · high] fetch is stubbed globally via vi.stubGlobal, but if a test forgets to call restore() (or throws before doing so), the stub leaks into subsequent tests and the original fetch is never restored. There…

### `test/helpers.ts`
- [ ] `test/helpers.ts:125-125` — [security · medium] LLM_CONFIG_ENCRYPTION_KEY is set to the literal string `'test-llm-config-encryption-key'`. While the comment says this is fine for testing, if `encryptLlmApiKey` derives a key by simply hashing/using…

### `test/setup.ts`
- [ ] `test/setup.ts:18-23` — [security · medium] vi.stubGlobal('QUEUE', ...) mocks the Cloudflare Queue binding for every test, but the mock is `any`-typed and unconditionally shadows any real QUEUE binding. Any test that legitimately exercises que…

### `tsconfig.json`
- [ ] `tsconfig.json:3-4` — [bug · high] Lib target inconsistency: `target` is `"ES2024"` and `lib` includes `"ES2024"`, but `"ES2024"` is not a valid TypeScript lib name — TypeScript currently ships `"ES2023"` and the closest valid lib is…

### `vite.config.ts`
- [ ] `vite.config.ts:26-51` — [bug · high] manualChunks uses fragile substring matching on full module paths. The keyword 'react' will match any package whose path contains that substring (e.g., 'react-native', 'react-query', 'react-hook-form…

## P2 — bug·medium / security·low

### `.gitignore`
- [ ] `.gitignore:142-143` — [security · low] The `.dev.vars` entry is Cloudflare-specific and correct, but `.wrangler` is a directory (Cloudflare's local state). Without a trailing slash it matches both files and dirs at any depth, which is fin…

### `db/migrations/001_initial.sql`
- [ ] `db/migrations/001_initial.sql:252-262` — [bug · medium] The repository backfill silently coerces non-numeric `installation_id` values to `0`, then updates `repositories.installation_id` on conflict. This destroys information for any row where the legacy v…
- [ ] `db/migrations/001_initial.sql:282-282` — [bug · medium] The fresh-schema `CREATE TABLE IF NOT EXISTS jobs` declares `repository_id INTEGER NOT NULL REFERENCES repositories(id)`, but immediately afterward the migration also has `ALTER TABLE jobs ADD COLUMN…
- [ ] `db/migrations/001_initial.sql:528-529` — [bug · medium] `file_reviews_correctness_idx` and `review_comments_file_idx` are created without `IF NOT EXISTS` syntax leak — actually they do have IF NOT EXISTS in the file (lines 102, 117). However the fresh CRE…

### `db/migrations/004_vcs_credentials.sql`
- [ ] `db/migrations/004_vcs_credentials.sql:31-33` — [bug · medium] **Focus area #2: no auto-update trigger on `updated_at`.** Both `created_at` and `updated_at` are `DEFAULT now()`, but there is no trigger or application-layer invariant documented to bump `updated_a…

### `db/migrations/005_bitbucket_repo_identity.sql`
- [ ] `db/migrations/005_bitbucket_repo_identity.sql:36-36, 43-51` — [bug · medium] Concurrent migration runner hazard: `ADD COLUMN IF NOT EXISTS` is safe under a single serialized migration runner, but two concurrent migration processes racing on a fresh database can both pass the…  _(+1 related in cluster: bug·medium)_

### `db/migrations/006_review_comment_confidence.sql`
- [ ] `db/migrations/006_review_comment_confidence.sql:12-14` — [bug · medium] `ADD COLUMN IF NOT EXISTS` will silently no-op when the column already exists with a divergent type or default. If a previous hotfix, manual ops change, or a partially-applied earlier version of this…

### `index.html`
- [ ] `index.html:9-10` — [security · low] Note that `crossorigin` is set on the `fonts.gstatic.com` preconnect but not on `fonts.googleapis.com`. The `crossorigin` attribute on `rel="preconnect"` controls whether credentials are sent on the…

### `scripts/apply_migrations.js`
- [ ] `scripts/apply_migrations.js:5-6` — [bug · medium] `pool.connect()` is outside the `try`, so connection failures bypass this function's error handling and reach the trailing promise as an unhandled rejection. Also, `process.exit()` can terminate befo…
- [ ] `scripts/apply_migrations.js:30-33` — [bug · medium] If `ROLLBACK` itself fails (for example, because the connection was lost), that rejection escapes the catch, the intended migration error may be obscured, and `process.exit(1)` is never reached. Pres…

### `scripts/fix-max-files.mjs`
- [ ] `scripts/fix-max-files.mjs:36-36` — [bug · medium] The predicate selects rows based on the textual JSON representation, while the replacement is numeric JSON (`100`), so this intentionally converts only values represented as `15` but does not establi…

### `scripts/migrate.mjs`
- [ ] `scripts/migrate.mjs:228-230` — [bug · medium] `ensureModelCatalog()` only checks whether `model_configs` exists, but then assumes `llm_providers` can be created and that `model_configs` has the legacy `provider` and `model_id` columns. On databa…
- [ ] `scripts/migrate.mjs:459-463` — [bug · medium] The script acquires the advisory lock on the session, then starts a transaction and runs all migrations through the same single pooled connection. If the process is terminated or the connection is lo…

### `scripts/render-design.mjs`
- [ ] `scripts/render-design.mjs:150-151` — [bug · medium] stdout/stderr from the Vite child are piped but never consumed. On some platforms (notably Windows) this can cause the child to block once the pipe buffer fills, and any startup errors from Vite are…
- [ ] `scripts/render-design.mjs:168-173` — [bug · medium] `addInitScript` reassigns `Date.now` and `Math.random` on the page's `window`. Because the script runs in every new context (per-iteration), but is also registered for any subsequent pages on the sam…
- [ ] `scripts/render-design.mjs:199-200` — [bug · medium] Route handlers are attached after `browser.newContext()` and after `addInitScript`, but there is no guarantee about when navigation-triggered XHRs fire relative to route registration when scripts are…

### `scripts/setup-cloudflare.js`
- [ ] `scripts/setup-cloudflare.js:74-77, 80-80` — [bug · medium] `wrangler kv namespace list` output is parsed by slicing between the first `[` and the last `]`. If wrangler prints any non-JSON line after the array (e.g., a warning or hint), the substring is inval…  _(+2 related in cluster: bug·medium)_
- [ ] `scripts/setup-cloudflare.js:135-135` — [bug · medium] Hyperdrive recovery substring match is fragile: `'code: 2017'` appears in many unrelated messages (e.g., `error code: 20170` for validation errors), and Cloudflare wording changes will silently break…
- [ ] `scripts/setup-cloudflare.js:269-275, 282-292` — [bug · medium] `.dev.vars` parsing splits each line on the first `=`. This breaks values that legitimately contain `=` (e.g., connection strings, base64 keys). Use `indexOf('=')` and split at most once.  _(+1 related in cluster: bug·medium)_
- [ ] `scripts/setup-cloudflare.js:342-345` — [security · low] Sensitive data exposure in logs: the queues step prints `e.stderr` (which can contain the wrangler command line and any echoed env values) and the secret-failure step prints `e.message`. While `e.std…
- [ ] `scripts/setup-cloudflare.js:394-394` — [bug · medium] `workersDev` prompt default `initial: 'https://codra.<username>.workers.dev'` is a literal placeholder string. The script then writes this placeholder into wrangler.jsonc and constructs callback URLs…
- [ ] `scripts/setup-cloudflare.js:448-448, 453-460` — [bug · medium] Config-replacement regexes are silently no-ops when the keys are absent. The script will report "Updated wrangler.jsonc with new resource IDs" even though none of the targeted keys existed, leaving t…  _(+1 related in cluster: security·low, bug·medium)_
- [ ] `scripts/setup-cloudflare.js:476-479` — [bug · medium] Regex-based wrangler.jsonc mutation assumes the KV and Hyperdrive bindings exist exactly once with that binding name. If the binding is absent the regex silently fails and `configSpinner.succeed` sti…

### `scripts/test.mjs`
- [ ] `scripts/test.mjs:40-46` — [bug · medium] Empty keys bypass the `process.env[key] === undefined` guard incorrectly, and the override semantics are surprising. A line like `=value` in an env file creates `process.env[''] = 'value'`, which is…
- [ ] `scripts/test.mjs:56-69` — [bug · medium] Migration runs unconditionally before Vitest, and a Vitest failure leaves the DB in a migrated state with no transactional rollback or cleanup hook. If the disposable DB is truly disposable this is a…

### `src/client/app.css`
- [ ] `src/client/app.css:487-496` — [bug · medium] `prefers-reduced-motion` conflict: the global safety net forces ~0ms transitions and `animation-iteration-count: 1` on every element, but `reveal-on-scroll` and `page-enter` re-enable animations insi…

### `src/client/components/features/job-detail/comment-card.tsx`
- [ ] `src/client/components/features/job-detail/comment-card.tsx:54-54` — [bug · medium] Splitting on the literal ` ```suggestion ` is not fence-aware. When the body contains a suggestion fence, this leaves the preceding markdown with an unmatched code fence (the closing fence is discard…
- [ ] `src/client/components/features/job-detail/comment-card.tsx:67-67` — [bug · medium] This regex removes any ` ```suggestion `/closing fence tokens independently and then wraps the result in a generic fence. A language suffix such as ` ```suggestion ts ` is lost, and triple backticks…

### `src/client/components/features/job-detail/file-finding.tsx`
- [ ] `src/client/components/features/job-detail/file-finding.tsx:63-65` — [bug · medium] Inline comments are keyed by `${file.id}-${index}`. `ParsedReviewComment` (see `src/shared/schema.ts`) has no stable identifier — only `path/line/position/title/body`. When comments are re-ordered, i…

### `src/client/components/features/job-detail/job-header.tsx`
- [ ] `src/client/components/features/job-detail/job-header.tsx:82-90` — [security · low] External PR link is safe today: `pullRequestUrl` encodes owner/repo, `job.prNumber` is a non-optional integer in the schema, and `rel="noopener noreferrer"` plus `target="_blank"` are present. Howeve…

### `src/client/components/features/job-detail/job-meta-cards.tsx`
- [ ] `src/client/components/features/job-detail/job-meta-cards.tsx:99-99` — [bug · medium] Fragile classification coupling: `isPartialReview` is derived from `job.errorMessage.startsWith('Partial review:')`, tying UI presentation to a server-controlled string prefix. Any change in wording,…
- [ ] `src/client/components/features/job-detail/job-meta-cards.tsx:185-185` — [bug · medium] Date parsing safety: `new Date(job.createdAt)` is rendered directly without validating that it's a valid date. If `job.createdAt` is malformed/missing in an unexpected shape, the UI will render "Inva…

### `src/client/components/features/job-detail/job-progress.tsx`
- [ ] `src/client/components/features/job-detail/job-progress.tsx:13-13, 16-17` — [bug · medium] `pct` is not constrained to the progress bar's declared 0–100 range. Because completed files are counted from `job.files` while the denominator comes from the independently supplied `job.fileCount`,…  _(+1 related in cluster: bug·medium)_

### `src/client/components/features/job-detail/job-review-overview.tsx`
- [ ] `src/client/components/features/job-detail/job-review-overview.tsx:69-69` — [bug · medium] `Number(job.overallConfidenceScore) * 100` will produce `NaN%` if the field is a non-numeric string or `Infinity` if it exceeds the expected 0..1 range. The header guard checks for `undefined`/`null`…

### `src/client/components/features/models/model-chain.tsx`
- [ ] `src/client/components/features/models/model-chain.tsx:117-124` — [bug · medium] Empty model Select option submits empty string, breaking value typing. `ModelSelector`'s model `Select` is initialized with `value ?? ''`, but its `options` come from `filteredModels`. If `value` is…
- [ ] `src/client/components/features/models/model-chain.tsx:146-149` — [bug · medium] `addFallback` may silently insert a duplicate model. `addFallback` uses `models[0]?.value` as the new fallback without checking if it's already in `primary` or existing `fallbacks`. If callers expect…

### `src/client/components/features/stats/overview-stats.tsx`
- [ ] `src/client/components/features/stats/overview-stats.tsx:33-38` — [bug · medium] **Loading state indistinguishable from a legitimate zero reading** — `value: null` is used both for the not-yet-loaded skeleton and (after a successful fetch) for any totals that resolve to `0` (sinc…

### `src/client/components/features/stats/stats-grid.tsx`
- [ ] `src/client/components/features/stats/stats-grid.tsx:54-58` — [bug · medium] The null check only handles `value !== null`. A legitimate `undefined` (e.g., from a partial API response) or empty string would render as a `Skeleton`, hiding real data. Decide which states mean "lo…

### `src/client/components/features/stats/time-range-select.tsx`
- [ ] `src/client/components/features/stats/time-range-select.tsx:21-21, 25-26` — [bug · medium] The silent fallback to `timeRanges[2]` (30 days) when `value` does not match any preset hides bugs from callers. If a parent passes `0`, `undefined` (in non-strict mode), or a new range value, the UI…  _(+1 related in cluster: bug·medium)_

### `src/client/components/layout/app-shell.tsx`
- [ ] `src/client/components/layout/app-shell.tsx:552-555` — [bug · medium] This logout handler has no rejection handling. If `api.logout()` fails (for example, due to a transient network error), the async event handler rejects, `location.href` is never reached, and the user…

### `src/client/components/motion/shared-layout-bg.tsx`
- [ ] `src/client/components/motion/shared-layout-bg.tsx:8-17` — [security · low] XSS / injection risk is not introduced here — no `innerHTML`, `eval`, or `Function()` use, and props are spread from React-owned sources only. No action required; recorded for completeness.
- [ ] `src/client/components/motion/shared-layout-bg.tsx:59-63` — [bug · medium] Mouse-leave on the wrapper does not clear activeId when pointer leaves via keyboard focus, and `onMouseEnter` on each row only fires for pointer — touch devices will never trigger the hover state, so…

### `src/client/components/motion/smooth-scroll.tsx`
- [ ] `src/client/components/motion/smooth-scroll.tsx:131-143` — [bug · medium] useNativeScrollSync: the effect re-binds a scroll listener whenever any of scrollY/progress/velocity change identity. In the Lenis path the listener is correctly disabled (enabled=false), but in the…
- [ ] `src/client/components/motion/smooth-scroll.tsx:182-185` — [bug · medium] scrollTo (provider): when `reduce` is true and the caller passes `target: HTMLElement` referencing a node outside the container's scroll context, `resolveTop` falls into the HTMLElement branch and re…
- [ ] `src/client/components/motion/smooth-scroll.tsx:199-207, 211-221` — [bug · medium] Reduced-motion branch: when `reduce` is true the provider renders a plain `<div>` instead of <ReactLenis>. Any descendant component calling `useLenis()` (e.g. a third-party scroll-driven component) w…  _(+1 related in cluster: bug·medium)_

### `src/client/components/motion/stepped-slider.tsx`
- [ ] `src/client/components/motion/stepped-slider.tsx:65-65` — [bug · medium] `clamp(NaN, lo, hi)` returns `NaN` because `Math.max/min` propagate NaN. Combined with the divisions above, NaN can flow into `percent`, the motion values, and `onValueChange`. Add a `Number.isFinite…
- [ ] `src/client/components/motion/stepped-slider.tsx:86-86` — [bug · medium] Pre-scan focus #5: Uncontrolled value drift. `useState(defaultValue)` only seeds on mount; subsequent changes to `defaultValue` are silently ignored, and toggling between controlled/uncontrolled by p…
- [ ] `src/client/components/motion/stepped-slider.tsx:119-122` — [bug · medium] `snapValue` uses `Math.round` against `step`, but doesn't validate `step > 0`. If a caller passes `step <= 0` (or NaN), the math produces `Infinity`/`NaN` and `clamp` lets `NaN` through (`Math.max/mi…
- [ ] `src/client/components/motion/stepped-slider.tsx:278-278, 280-281` — [bug · medium] `aria-valuenow={Math.round(current)}` reports the raw rounded value, but the *accessible* step semantics usually require the value to lie on a step boundary. With arbitrary `step` (e.g., 2.5) and non…  _(+1 related in cluster: bug·medium)_
- [ ] `src/client/components/motion/stepped-slider.tsx:302-313` — [security · low] XSS surface (low): `tick.label` is rendered as raw JSX text content (safe — React escapes it), but if a future change moves it into `style` or `dangerouslySetInnerHTML`, it would be injectable. Worth…

### `src/client/components/motion/tabs.tsx`
- [ ] `src/client/components/motion/tabs.tsx:48-48, 51-56, 58-58, 64-64` — [bug · medium] Uncontrolled mode initializes state to empty string when `defaultValue` is omitted: on first render `current === ''` matches no trigger, so the active indicator is missing and no `TabsContent` panel…  _(+3 related in cluster: bug·medium)_
- [ ] `src/client/components/motion/tabs.tsx:179-185` — [bug · medium] Inactive `TabsContent` panels are kept mounted but rendered with the HTML `hidden` attribute, and active panels use `motion.div`. Mixing the two means server-rendered HTML for the inactive panel is a…

### `src/client/components/shared/empty-state.tsx`
- [ ] `src/client/components/shared/empty-state.tsx:47-52, 58-68` — [bug · medium] Using array index as the React key for `hints` can cause rendering/state bugs if the list is ever reordered, filtered, or prepended. Prefer a stable id (e.g., `hint-${hint.id ?? hint}`) or require ca…  _(+1 related in cluster: bug·medium)_

### `src/client/components/shared/jobs-table.tsx`
- [ ] `src/client/components/shared/jobs-table.tsx:86-107` — [bug · medium] formatRelativeDate has incorrect sign/divisor logic for future dates. `diffSeconds = (date - Date.now()) / 1000` is negative for past dates, but the code divides the (negative) diffSeconds by the uni…
- [ ] `src/client/components/shared/jobs-table.tsx:135-136` — [bug · medium] Mobile "files" cell uses `job.fileCount.toLocaleString()` which assumes `fileCount` is always a number. If the schema allows `fileCount` to be nullable or undefined, this will throw. Add a guard or f…
- [ ] `src/client/components/shared/jobs-table.tsx:263-264` — [bug · medium] Same null-safety issue on the desktop row: `job.fileCount.toLocaleString()`, `job.totalInputTokens + job.totalOutputTokens`, and `tokenTotal.toLocaleString()` in the title attribute all assume the fi…
- [ ] `src/client/components/shared/jobs-table.tsx:412-416` — [security · low] `job.owner`, `job.repo`, `job.prTitle`, `job.prAuthor` and `job.prNumber` are rendered as React text children, so React escapes them automatically — no XSS risk here. However, the action cell's `aria…

### `src/client/components/shared/opencodra-logo.tsx`
- [ ] `src/client/components/shared/opencodra-logo.tsx:31-31` — [bug · medium] The SVG stroke uses `var(--primary)` with no fallback. If the `--primary` CSS custom property is unset (SSR before theme injection, or a theme variant that doesn't define it), the ring will be invisi…

### `src/client/components/shared/skeleton.tsx`
- [ ] `src/client/components/shared/skeleton.tsx:37-39` — [bug · medium] `SkeletonCard` formula `70 - i * 10` produces negative widths when `lines >= 8` (e.g., `lines=8` yields `width="-10%"`). Add input validation or clamp the value so callers can't accidentally get nons…

### `src/client/components/shared/sparkline.tsx`
- [ ] `src/client/components/shared/sparkline.tsx:3-4` — [bug · medium] Missing guard for null/undefined `data`: The `data.map(...)` call on line 4 will throw `Cannot read properties of undefined (reading 'map')` if a consumer ever passes `undefined`. While the current c…

### `src/client/components/ui/badge.tsx`
- [ ] `src/client/components/ui/badge.tsx:36-55, 62-68` — [bug · medium] The pre-scan description says 'running' returns 'info' (correct), but it also claims 'comment' falls through to 'neutral' in some paths — it actually returns 'warning'. However, a real coverage issue…  _(+1 related in cluster: bug·medium)_

### `src/client/components/ui/button.tsx`
- [ ] `src/client/components/ui/button.tsx:24-24` — [security · low] The 'link' variant relies entirely on `:hover` for an underline and is otherwise visually indistinguishable from surrounding text. When combined with `asChild` and an `<a>` that lacks `href`, or with…

### `src/client/components/ui/confirm-dialog.tsx`
- [ ] `src/client/components/ui/confirm-dialog.tsx:43-56` — [bug · medium] Inconsistent close handling between Cancel and Confirm: the Cancel button uses `Dialog.Close asChild` (which gives Radix full control over the close lifecycle, focus restoration, and the modal's `onO…

### `src/client/components/ui/dropdown-menu.tsx`
- [ ] `src/client/components/ui/dropdown-menu.tsx:0-0` — [security · low] No XSS risks identified — content is rendered via JSX (auto-escaped), no use of `dangerouslySetInnerHTML`, `eval`, `Function`, `innerHTML`, or `document.write`. Native prototypes are not modified. Li…
- [ ] `src/client/components/ui/dropdown-menu.tsx:77-79` — [bug · medium] `DropdownMenuTrigger`'s `setRef` is a plain function declared on each render, which is fine for a callback ref, but it does not compose with any forwarded ref. Consumers using `asChild` with their ow…
- [ ] `src/client/components/ui/dropdown-menu.tsx:118-118` — [bug · medium] Initial positioning uses `visibility: 'hidden'` but the effect that flips it to `visible` only runs when `ctx.open` becomes true. On the *first* render where `ctx.open` is already true (e.g. controll…

### `src/client/components/ui/select.tsx`
- [ ] `src/client/components/ui/select.tsx:85-91` — [bug · medium] Highlight restoration can select an unintended value when the currently selected option disappears. When `value` no longer matches any option, the code falls back to `highlightedIndex = 0`, so pressi…
- [ ] `src/client/components/ui/select.tsx:139-142` — [bug · medium] Placement flip logic only compares `below < h + 16 && above > below`. When the trigger is in the middle of the viewport and `above == below`, the panel goes downward, which is fine, but it doesn't co…
- [ ] `src/client/components/ui/select.tsx:158-160` — [bug · medium] The `moveHighlight` helper does not clamp to a non-empty `options` array. Calling it with `options.length === 0` will store `highlightedIndex = 0` (since `Math.max(next, 0)` dominates) and later caus…
- [ ] `src/client/components/ui/select.tsx:243-248` — [bug · medium] Trigger's `motion.button` receives an `animate` object whose `borderTopLeftRadius` value is `kf` (an array `[0, 0, 12]` or `[12, 0, 12]`) — Motion's `animate` API for arrays is valid for tweening thr…

### `src/client/components/ui/switch.tsx`
- [ ] `src/client/components/ui/switch.tsx:4-7` — [bug · medium] The component is uncontrolled: it accepts `defaultChecked` (inherited) but not a controlled `checked` prop. Because of the `sr-only` checkbox, keyboard activation (Space/Enter) and the visual state r…
- [ ] `src/client/components/ui/switch.tsx:17-20` — [bug · medium] Call ordering inside the change handler invokes `onChange` before `onCheckedChange`. If a consumer uses `onChange` to call `event.preventDefault()` or `stopPropagation()`, the `onCheckedChange` callb…

### `src/client/hooks/use-is-dark-mode.ts`
- [ ] `src/client/hooks/use-is-dark-mode.ts:12-15` — [bug · medium] There is a race between the render-time class read and observer registration: if the class changes before this effect runs, the observer misses that mutation and state remains stale until another cla…

### `src/client/hooks/use-job-detail.ts`
- [ ] `src/client/hooks/use-job-detail.ts:32-34` — [bug · medium] `fetchJob` is referenced from the second/third `useEffect` and inside `setTimeout`, but it is recreated every render. The `setTimeout` callback closes over the render-time `fetchJob`. If a stale clos…
- [ ] `src/client/hooks/use-job-detail.ts:59-59` — [security · low] The `pollTimeout.current = window.setTimeout(...)` is acceptable here because the argument is a function reference (`fetchJob`), not a string — no code-injection risk. However, the timer captures `fe…
- [ ] `src/client/hooks/use-job-detail.ts:82-93` — [bug · medium] `handleRetry` lacks any toast/loading UX that the sibling handlers (`handleRerun`, `handleStop`, `handleDelete`) provide. On failure the error is only stored in component state — the user gets no not…
- [ ] `src/client/hooks/use-job-detail.ts:116-120, 122-122` — [bug · medium] `handleStop` calls `fetchJob()` after a successful stop, which resets `error` to `null` via the success path. If the user had a prior unrelated error displayed, calling stop will silently clear it wi…  _(+1 related in cluster: bug·medium)_

### `src/client/lib/api.ts`
- [ ] `src/client/lib/api.ts:47-49` — [bug · medium] The wrapper forces `application/json` even when there is no body and, more importantly, when a future caller supplies `FormData`, `Blob`, or another non-JSON body. For `FormData`, overriding this hea…
- [ ] `src/client/lib/api.ts:122-122` — [bug · medium] `requestWithMeta` handles 204 differently from `request`: after a successful 204 it still calls `response.json()`, which rejects with an unexpected end-of-input error. Either support 204 explicitly o…
- [ ] `src/client/lib/api.ts:173-173, 176-178` — [bug · medium] Unlike the other job helpers, this URL interpolates `id` without `pathSegment()`. IDs containing `/`, `?`, `#`, or `%` can target a different route or produce a malformed request. Encode the identifi…  _(+1 related in cluster: bug·medium)_

### `src/client/lib/theme.tsx`
- [ ] `src/client/lib/theme.tsx:18-25` — [security · low] Silent swallowed errors in storage helpers: `getStoredTheme` and `applyTheme` both `catch` and ignore `localStorage` errors. While this is intentional (Safari private mode, etc.), it makes storage fa…
- [ ] `src/client/lib/theme.tsx:69-75` — [bug · medium] Possible React 18+ concurrent-mode desync: combining DOM mutation inside `setThemeState`'s updater function with a paused-transition timer means in strict mode or concurrent rendering the updater can…

### `src/client/lib/utils.ts`
- [ ] `src/client/lib/utils.ts:8-12, 10-10, 14-16` — [bug · medium] Negative numbers are not handled: e.g. `fmtNumber(-1500)` returns `-1.5k` and `formatDuration(-500)` returns `-0.5s`. Add an input guard or document the precondition that callers pass non-negative fi…  _(+3 related in cluster: bug·medium)_

### `src/client/lib/vcs.ts`
- [ ] `src/client/lib/vcs.ts:18-20` — [bug · medium] Bitbucket URLs use a workspace as the first path segment, but falling back to a generic GitHub-style `owner` can silently generate a non-existent link when the two identifiers differ. Do not substitu…

### `src/client/main.tsx`
- [ ] `src/client/main.tsx:66-74` — [security · low] ErrorBoundary renders `this.state.error.toString()` directly inside a `<pre>`. If an error message contains user-controlled data (e.g., from API responses or URL params that propagate into thrown err…
- [ ] `src/client/main.tsx:116-125` — [bug · medium] Non-null assertion `document.getElementById('root')!` silently hides a real failure mode: if the root element is missing (malformed HTML, script loaded before DOM, wrong bundler output), `createRoot(…

### `src/client/pages/job-detail.tsx`
- [ ] `src/client/pages/job-detail.tsx:30-38` — [bug · medium] Concurrent action flags (`isRerunning`, `isStopping`, `isDeleting`) are forwarded independently but `useJobDetail`'s handlers don't guard against each other. Pressing "Stop" while "Rerun" is still in…

### `src/client/pages/job-logs.tsx`
- [ ] `src/client/pages/job-logs.tsx:90-92, 95-100` — [bug · medium] Mobile meta strip (lines 88-92) hides the row entirely unless `inTok` is truthy, even when only `outTok` is present. The desktop row (lines 53-59) correctly renders when either token is set via `(inT…  _(+1 related in cluster: bug·medium, security·low)_
- [ ] `src/client/pages/job-logs.tsx:138-138` — [bug · medium] Race condition / duplicate error UI risk (lines 142, 173): when `useJobDetail` first returns `{ job: undefined, error: 'Not found' }` and later returns `{ job: <data>, error: null }` after a successf…

### `src/client/pages/jobs.tsx`
- [ ] `src/client/pages/jobs.tsx:51-51` — [bug · medium] Pagination can land on a non-existent page: when `total` shrinks (e.g., after applying a filter or when a job is deleted between polls) `totalPages` can become 0 while `filters.page` remains > 0, lea…

### `src/client/pages/landing.tsx`
- [ ] `src/client/pages/landing.tsx:97-104` — [security · low] The `/auth/github` anchor opens in the same tab (no `target="_blank"`) so the missing `rel` attribute is fine, but the `Get started with GitHub` CTA is a primary marketing link and clicking it will n…

### `src/client/pages/login.tsx`
- [ ] `src/client/pages/login.tsx:10-12` — [security · low] Error message source not strictly constrained. `getErrorMessage` is safe today because it switches on a fixed allow-list, but `error` is rendered as raw text inside JSX (which React auto-escapes — so…

### `src/client/pages/not-found.tsx`
- [ ] `src/client/pages/not-found.tsx:33-33` — [bug · medium] `history.back()` is not a reliable in-app fallback: when a user opens an invalid URL directly (or has no same-app history entry), it may do nothing or navigate out of the application to an unrelated…

### `src/client/pages/repos.tsx`
- [ ] `src/client/pages/repos.tsx:246-249` — [bug · medium] `globalRouteKey` is `JSON.stringify(getGlobalRoute(globalConfig))` which runs every render. It is wrapped in `useMemo` keyed only on `globalConfig` so it is stable for equal references, but `globalCo…
- [ ] `src/client/pages/repos.tsx:459-465` — [bug · medium] `onModelApplied` / `onModelReset` mutate the parent `repos` state, which causes `RepoModelModal`'s `repo` prop reference to change, and `editingRepo` recomputes. This can interact poorly with the mod…

### `src/client/pages/repos/add-bitbucket.tsx`
- [ ] `src/client/pages/repos/add-bitbucket.tsx:0-0` — [security · low] The webhook URL is shown verbatim in instructions. Ensure this matches what the server actually exposes; if the server uses a different mount path or a configurable base URL, hardcoding `/webhook/bit…
- [ ] `src/client/pages/repos/add-bitbucket.tsx:25-25` — [bug · medium] SSR / window access safety: `window.location.origin` is accessed during component render. If this component is ever rendered server-side (e.g., during SSR, testing with jsdom limitations, or a future…
- [ ] `src/client/pages/repos/add-bitbucket.tsx:41-41` — [bug · medium] tokenExpiresAt is passed through unchanged as a string from a date input with no format validation. An empty string is mapped to `null` but any other value (including invalid dates like '2026-13-40'…
- [ ] `src/client/pages/repos/add-bitbucket.tsx:111-113` — [security · low] Password fields use `type="password"` with `autoComplete="off"`, but there is no masked preview or confirmation step before submitting the access token. If a user pastes the wrong token (e.g., an exp…

### `src/client/pages/settings.tsx`
- [ ] `src/client/pages/settings.tsx:402-403` — [bug · medium] On a save failure, `savedReviewSettings` may still be `null` (for example, if a slider interaction occurs before the initial settings request finishes or after a failed/partial load). This sets `revi…

### `src/client/pages/stats.tsx`
- [ ] `src/client/pages/stats.tsx:39-43` — [bug · medium] `formatDay` silently prepends `T00:00:00` only because the caller passes a date-only string. If `value` is ever passed with a time component, no suffix is appended and parsing is implementation-defin…
- [ ] `src/client/pages/stats.tsx:342-354` — [bug · medium] On the very first failed load, `stats` stays `null` while `error` is set, so the user sees the loading skeleton AND the destructive alert simultaneously. Prefer to treat `loading && !stats` as the sk…
- [ ] `src/client/pages/stats.tsx:364-371` — [bug · medium] Manual refresh has no stale-while-revalidate guarantee and races the auto-poll: while a manual `load(true)` is in flight, `usePolling` can fire another `load()` against the same `days`, and a slow ma…

### `src/client/pages/vcs-credentials.tsx`
- [ ] `src/client/pages/vcs-credentials.tsx:92-94` — [security · low] Access token / webhook secret values are kept in React state in plain text: this is unavoidable for an editable form, but note that any future browser extension, devtools session, or replay-log infra…
- [ ] `src/client/pages/vcs-credentials.tsx:115-118` — [bug · medium] Form's onSubmit silently no-ops if canSubmit is false: pressing Enter in the form when the button is disabled (e.g. while submitting, or when required fields are empty after editing) gives no user fe…
- [ ] `src/client/pages/vcs-credentials.tsx:254-261, 260-261` — [bug · medium] handleSubmit mutation assumes optional fields: the code does `input.accessToken = ...`, `input.webhookSecret = ...`, `input.tokenExpiresAt = ...` after constructing the input. If `VcsCredentialStoreI…  _(+1 related in cluster: bug·medium)_
- [ ] `src/client/pages/vcs-credentials.tsx:270-272, 275-279` — [bug · medium] Date-only expiry sent as YYYY-MM-DD is timezone-dependent: on add, `currentExpiry` is the raw `<input type="date">` value (e.g. '2026-12-31'). If the server interprets this as UTC midnight, users in…  _(+1 related in cluster: bug·medium)_
- [ ] `src/client/pages/vcs-credentials.tsx:312-319` — [bug · medium] Delete error path: on failure the toast.error uses `tid` (the same loading-toast id), which is correct for replacement. However, `setDeleteTarget(null)` runs in the `finally` block, which closes the…

### `src/server/core/bitbucket-oauth.ts`
- [ ] `src/server/core/bitbucket-oauth.ts:18-23` — [security · low] Authorization header may leak via fetch exception: while the thrown errors here only include status codes (good), if `fetch` itself rejects (network failure) the underlying error object can include t…
- [ ] `src/server/core/bitbucket-oauth.ts:35-43` — [bug · medium] Untyped JSON parsing on token exchange: `response.json() as { access_token?: string; ... }` is an unchecked structural cast. While downstream code only reads known fields, no runtime validation occur…
- [ ] `src/server/core/bitbucket-oauth.ts:65-76` — [bug · medium] Missing identity-field validation before mapping to session user: `toDashboardSessionUser` assumes `account_id`, `uuid`, and `username` are present, but `bitbucketOAuthProfileSchema` is not checked h…

### `src/server/core/bitbucket.ts`
- [ ] `src/server/core/bitbucket.ts:61-70` — [bug · medium] Off-by-one in retry budget: `attempt` is incremented before the budget check, so the loop performs up to `maxRetries + 1` total attempts (initial + N retries) rather than `maxRetries`. With the defau…
- [ ] `src/server/core/bitbucket.ts:212-216` — [bug · medium] `postPullRequestComment` uses `as` to assert the response is `{ id: number }` without validating it. Bitbucket responses can include extra fields, but a malformed/empty payload (e.g., transient proxy…

### `src/server/core/config.ts`
- [ ] `src/server/core/config.ts:60-62` — [bug · medium] Revision key uses wall-clock and stale per-repo entries are never pruned: `invalidateAllRepoConfigCache` stores `Date.now()` as the revision but never deletes the previous per-repo cache entries, so…
- [ ] `src/server/core/config.ts:78-79` — [bug · medium] Inconsistent enabled/default semantics between DB and cache: `loadRepoConfig` defaults `enabled` to `true` when there is no DB record, while `hasRepoModelOverride` treats a missing `existing` the sam…

### `src/server/core/crypto.ts`
- [ ] `src/server/core/crypto.ts:56-56` — [security · low] Confirmed: error messages do not leak plaintext or key material — the literal `'LLM_CONFIG_ENCRYPTION_KEY must be at least 16 characters long.'` and `'Unsupported encrypted secret format.'` contain o…

### `src/server/core/diff.ts`
- [ ] `src/server/core/diff.ts:111-118` — [bug · medium] For renames, `isIgnored` is recomputed first on 'rename to' (using the new path) and then again on the subsequent '+++ ' line. Since git always emits '+++ ' after 'rename to', the second evaluation o…

### `src/server/core/github-oauth.ts`
- [ ] `src/server/core/github-oauth.ts:23-23` — [bug · medium] Network/abort failures from `fetch` are not distinguished from HTTP errors. If the network is down, DNS fails, or the request is aborted, `fetch` rejects with a `TypeError`, which will propagate as a…
- [ ] `src/server/core/github-oauth.ts:41-45` — [bug · medium] Untyped JSON parsing in `exchangeGitHubOAuthCode`: `response.json()` is cast via `as` to a partial shape without runtime validation. If GitHub returns an unexpected payload (e.g., missing fields, wro…
- [ ] `src/server/core/github-oauth.ts:55-55` — [bug · medium] Same network/abort handling issue applies in `fetchGitHubOAuthProfile`. Same fix recommended.

### `src/server/core/http.ts`
- [ ] `src/server/core/http.ts:1-3, 5-8` — [bug · medium] Naive Accept-header sniffing: `includes('text/html')` matches any substring, so clients that explicitly exclude HTML (e.g. `Accept: application/json, text/html;q=0`) or prefer JSON via q-values will…  _(+1 related in cluster: security·low, bug·medium)_

### `src/server/core/logger.ts`
- [ ] `src/server/core/logger.ts:33-34` — [bug · medium] `redact` recursively traverses arbitrary objects without cycle detection. Passing a cyclic object causes infinite recursion/stack overflow before logging, and even non-cyclic unsupported values can l…
- [ ] `src/server/core/logger.ts:60-60` — [bug · medium] The truthiness check omits valid data values such as `0`, `false`, and `''`, so callers lose those payloads from the log. Check for `undefined` (or use an explicit optional-data policy) instead of tr…

### `src/server/core/model-output.ts`
- [ ] `src/server/core/model-output.ts:137-143` — [bug · medium] Truncated-JSON recovery uses stale `escape` and `inString` state. If the loop exits because stack never returns to 0 (truncation) while escape is true, the subsequent appends `'}'` without first clos…
- [ ] `src/server/core/model-output.ts:227-237, 243-253` — [bug · medium] preprocessJson handles \n and \r but ignores other control characters (\t, \b, \f, lone \v, and 0x00-0x1F) inside strings. These characters are not valid inside JSON strings and will cause JSON.parse…  _(+1 related in cluster: bug·medium)_
- [ ] `src/server/core/model-output.ts:271-275` — [security · low] Logger prefixes `rawPrefix: raw.slice(0, 500)` may leak parts of the LLM prompt or sensitive content into logs. While the prefix is bounded, ensure that downstream log sinks are access-controlled and…
- [ ] `src/server/core/model-output.ts:395-406` — [bug · medium] cleanText's regex `(?:QUALITY|SECURITY|BUG|PERFORMANCE|CORRECTNESS|P[0-3]|NIT)\b` strips category prefixes from titles/snippets. The loop iterates until the text stops changing, which is correct, but…
- [ ] `src/server/core/model-output.ts:433-442` — [bug · medium] `verdict` is overridden whenever any comment survived, even if the model said 'Patch is correct'. This means an `approve` verdict can never be returned if there's at least one comment — which may be…
- [ ] `src/server/core/model-output.ts:460-468` — [bug · medium] `parseSummaryResponse` silently swallows jsonrepair and parse errors. If the model output is structured JSON that fails schema validation, the function returns the raw text — meaning an LLM that emit…

### `src/server/core/oauth.ts`
- [ ] `src/server/core/oauth.ts:15-15` — [bug · medium] Allowlist match logic is missing/unspecified: this file parses the allowlist but does not export any isUserAllowed(provider, id) helper. Downstream code is therefore likely to re-implement provider-s…
- [ ] `src/server/core/oauth.ts:37-43, 51-55` — [bug · medium] Untyped/unsanitized provider keys in the JSON branch: record.github / record.bitbucket are read without validating that they are arrays of primitives; an object-valued entry slips through Array.isArr…  _(+1 related in cluster: bug·medium)_

### `src/server/core/review.ts`
- [ ] `src/server/core/review.ts:0-0` — [bug · medium] `formattersummarizeVerdict` is called with `finalComments` (post-cap) and `hasFailures`, but the verdict bucket (`labelMap[verdictSummary.verdict]`) only knows about `'comment'` and `'approve'`. If `…
- [ ] `src/server/core/review.ts:281-289` — [security · low] `extractReviewRequest` casts `input.payload` to `IssueCommentWebhookPayload` / `PullRequestWebhookPayload` without runtime validation. Untrusted webhook payloads flow directly into the returned `Revi…
- [ ] `src/server/core/review.ts:793-796` — [bug · medium] `reviewChunkFileLimit <= 0` guard throws a NEW plain `Error` whose message starts with `'Subrequest budget...'`. This generic Error does NOT match `isSubrequestBudgetError` (which only matches `messa…
- [ ] `src/server/core/review.ts:818-843` — [bug · medium] `bulkInheritFileReviews` returns `inheritedPaths` -- the loop on lines 833-842 uses that to set `currentReviews` to the PARENT review objects. However, `currentReviews.set(path, parent)` puts a `pare…
- [ ] `src/server/core/review.ts:1290-1301` — [bug · medium] `runFinalizePhase` calls `bulkMarkFilesFailed(... , { modelUsed: config.model?.main ?? 'unconfigured', errorMessage: 'This file was not reviewed before the review run completed.' })` -- but `bulkMark…
- [ ] `src/server/core/review.ts:1347-1350` — [bug · medium] `omittedCount` is computed BEFORE the slice but uses `reviewedComments.length` (the post-filter, post-confidence-floor pool) as the base. When `finalComments` was filtered down by severity/confidence…

### `src/server/core/sessions.ts`
- [ ] `src/server/core/sessions.ts:46-46` — [bug · medium] The cookie is always marked `Secure`, so browsers will not send it over plain HTTP. This makes local or other non-TLS deployments create a KV session that can never be resumed. Derive this flag from…

### `src/server/core/timeout.ts`
- [ ] `src/server/core/timeout.ts:0-0` — [bug · medium] If `fn` throws synchronously (does not return a promise), the thrown value is a plain Error/exception rather than a rejection. The catch block then runs against a synchronous throw, but the check `er…
- [ ] `src/server/core/timeout.ts:15-17` — [bug · medium] No validation of `ms` — passing 0 or a negative value causes `controller.abort()` to be scheduled on the next macrotask, so any non-trivial `fn` will reject/throw as if it timed out. Negative values…

### `src/server/core/token-tracker.ts`
- [ ] `src/server/core/token-tracker.ts:53-61` — [bug · medium] record() lacks input validation and accepts non-numeric or NaN values silently. Passing NaN, undefined, a string, or negative numbers from upstream will silently corrupt token totals and produce `NaN…

### `src/server/core/verify.ts`
- [ ] `src/server/core/verify.ts:29-39` — [security · low] Timing analysis: failure paths return at different points (missing prefix, bad hex, crypto error). While `crypto.subtle.verify` itself is constant-time, the differing early-return paths based on `inp…

### `src/server/db/app-settings.ts`
- [ ] `src/server/db/app-settings.ts:0-0` — [bug · medium] omittedCount miscalculates the number of comments dropped by the max_comments cap: it uses `reviewedComments.length` (the count BEFORE severity/confidence filtering) instead of the count of comments…  _(+1 related in cluster: bug·medium)_

### `src/server/db/client.ts`
- [ ] `src/server/db/client.ts:73-73` — [bug · medium] Each cached entry owns a postgres.js pool, but the map has neither eviction nor a shutdown path. Tests that create dynamic connection strings—and any accidental unscoped production usage during crede…
- [ ] `src/server/db/client.ts:105-105` — [bug · medium] `JSON.parse` throws on malformed stored data, despite this API accepting a `fallback`. A corrupt/legacy value can therefore fail the whole request instead of returning the supplied fallback. Catch `S…

### `src/server/db/file-reviews.ts`
- [ ] `src/server/db/file-reviews.ts:328-330` — [bug · medium] The comment-copy join assumes exactly one parent review for each path. If legacy data, schema drift, or duplicate parent rows violate that assumption, every matching parent's comments are copied into…
- [ ] `src/server/db/file-reviews.ts:381-382` — [bug · medium] `SUM(... )::int` can overflow PostgreSQL's 32-bit integer once cumulative token usage exceeds 2,147,483,647, causing this stats query to fail. Keep the aggregate as `bigint`/`numeric` and model the r…

### `src/server/db/jobs.ts`
- [ ] `src/server/db/jobs.ts:78-84` — [bug · medium] `hexToBytes` silently corrupts input on odd-length hex strings: `parseInt("0", 16)` for the trailing half returns `NaN`, and the resulting `Uint8Array` is one element too small. Either throw on odd l…
- [ ] `src/server/db/jobs.ts:169-179` — [bug · medium] `setJobWorkflowInstance` casts `$2` directly to `uuid` — if `workflowInstanceId` is empty/undefined (e.g. the workflow binding isn't ready yet), Postgres raises `invalid input syntax for type uuid` a…
- [ ] `src/server/db/jobs.ts:374-377` — [bug · medium] The `listJobs` search clause matches `pr_number` with `CAST(j.pr_number AS TEXT) LIKE %input%`. A search like `"123"` will match PR #1230 too — and worse, the cast + LIKE makes the result depend on a…
- [ ] `src/server/db/jobs.ts:729-733` — [bug · medium] `completeJob` doesn't filter `WHERE id = $1 AND status IN ('queued', 'running', ...)` — it unconditionally UPDATEs, including already-terminal jobs. If `completeJob` is called twice (replay, double-f…

### `src/server/db/model-configs.ts`
- [ ] `src/server/db/model-configs.ts:262-281` — [bug · medium] `updateModelConfig` silently returns `null` when the supplied `providerId` does not exist in `llm_providers` (the `INSERT ... SELECT ... FROM llm_providers WHERE p.id = $2` yields zero rows, so `upse…
- [ ] `src/server/db/model-configs.ts:324-328, 331-332` — [bug · medium] `upsertDiscoveredModelConfigs` builds the in-memory `usedModelIds` from `LIKE '${providerSlug}:%'` — this matches any provider's row whose id happens to start with the slug and a colon. If two provid…  _(+1 related in cluster: bug·medium)_

### `src/server/db/stats.ts`
- [ ] `src/server/db/stats.ts:57-67` — [bug · medium] `topRepos` query forces a hash aggregate on `r.vcs_provider, r.owner, r.repo` for the whole jobs table. Without a date filter or `LIMIT` being applied *before* the aggregate, the planner has to scan+…

### `src/server/db/vcs-credentials.ts`
- [ ] `src/server/db/vcs-credentials.ts:0-0` — [bug · medium] isRetryableFileReviewErrorMessage explicitly fails fast for timeouts (returns false on isTimeoutMessage), but a previously-failed row that was a timeout will then be excluded by countsAsHandledFileRe…  _(+3 related in cluster: bug·medium, security·low)_

### `src/server/db/webhook-deliveries.ts`
- [ ] `src/server/db/webhook-deliveries.ts:41-41, 44-44` — [bug · medium] `JSON.stringify(input.payload)` will throw a `TypeError: Converting circular structure to JSON` (or similar) for non-serializable values, and that throw happens between the SELECT and the INSERT — so…  _(+1 related in cluster: bug·medium)_

### `src/server/env.ts`
- [ ] `src/server/env.ts:33-38, 41-41` — [bug · medium] `DASHBOARD_ALLOWED_USERS` is typed as a single `string` despite the plural name implying a list. If it is parsed (CSV / JSON / newline-separated) at use sites, parsing failures or empty values may si…  _(+1 related in cluster: security·low, bug·medium)_

### `src/server/index.ts`
- [ ] `src/server/index.ts:68-71` — [bug · medium] This validation is weaker than the schema's `z.uuid()` check: it accepts arbitrary 36-character combinations of hex digits and hyphens (for example, a string of 36 `a` characters). Because malformed…

### `src/server/middleware/auth.ts`
- [ ] `src/server/middleware/auth.ts:0-0` — [security · low] Session cookie missing the `secure` flag in some environments: although `secure: true` is set in `createSession`, the `deleteCookie` call in `destroySession` only passes `{ path: '/' }` and omits `se…  _(+1 related in cluster: security·low)_

### `src/server/models/anthropic.ts`
- [ ] `src/server/models/anthropic.ts:27-28` — [bug · medium] Subrequest counter is never rolled back on failure. `tracker.incrementSubrequests(1)` runs before `withTimeout`, so any subsequent failure — timeout, network error, non-2xx HTTP, JSON parse error, em…
- [ ] `src/server/models/anthropic.ts:55-58, 55-62` — [bug · medium] Unchecked type assertion on provider response. `data as AnthropicResponse` performs no runtime validation, and content parts are accessed via optional chaining without discriminating block type. A ma…  _(+1 related in cluster: bug·medium)_

### `src/server/models/catalog.ts`
- [ ] `src/server/models/catalog.ts:114-116` — [bug · medium] The Gemini `supportedGenerationMethods` filter has inverted null-safety. When the field is missing/non-array, the condition `Array.isArray(...) ? includes(...) : true` treats absence as "supports gen…
- [ ] `src/server/models/catalog.ts:141-142` — [bug · medium] `response.json()` will throw a `SyntaxError` on malformed/non-JSON upstream payloads, and that raw error propagates to the caller as a non-user-friendly message. Wrap parsing in a try/catch and conve…

### `src/server/models/cloudflare.ts`
- [ ] `src/server/models/cloudflare.ts:101-111` — [bug · medium] `extractMessageContent` silently coerces unknown array parts to `''`. If a model emits a multi-part response containing one text part plus tool-use/non-text parts, the resulting string may contain pa…
- [ ] `src/server/models/cloudflare.ts:135-135` — [bug · medium] extractCloudflareText is typed as `string` but it can terminate via `failUnparseable` which returns `never` and throws. Callers cannot tell "empty response" from "reasoning-only/non-JSON finish_reaso…
- [ ] `src/server/models/cloudflare.ts:220-229` — [bug · medium] extractBatchInnerResult trusts the shape of `container.responses`/`container.results` elements. The `isRecord(first) ? first.result ?? first : first` branch will return `undefined` if `responses[0]`…
- [ ] `src/server/models/cloudflare.ts:244-258` — [bug · medium] submitCloudflareBatch increment-then-fail ordering: `tracker.incrementSubrequests(1)` is called *before* validating the result contains a `request_id`. If `env.AI.run` succeeds but returns no `reques…
- [ ] `src/server/models/cloudflare.ts:362-365` — [bug · medium] Dead/incorrect quota branch: this check happens inside the catch block, but `errorMsg` from an AbortError-triggered TimeoutError won't contain '4006' or 'daily free allocation', and Cloudflare's actu…

### `src/server/models/google.ts`
- [ ] `src/server/models/google.ts:179-185, 188-188, 195-199` — [bug · medium] Untyped cast of response body: `(await response.json()) as {...}` trusts the upstream payload completely. If Gemini ever returns a non-object (e.g., a string error envelope on certain failures), `dat…  _(+2 related in cluster: bug·medium)_

### `src/server/models/openai.ts`
- [ ] `src/server/models/openai.ts:110-114` — [bug · medium] `OpenAIResponse` is declared but not exported as a type-only contract, and the cast `as OpenAIResponse` performs no validation. A provider returning a structurally different payload (e.g., missing `c…

### `src/server/models/types.ts`
- [ ] `src/server/models/types.ts:41-49` — [bug · medium] [object Object] coercion when error body lacks a message string. When `obj.error` is an object without a `.message` property, `?? obj.error` assigns the object itself to `message`. The outer `typeof…

### `src/server/prompts/file-review.ts`
- [ ] `src/server/prompts/file-review.ts:107-109` — [bug · medium] Misleading line numbers when `oldLineNumber`/`newLineNumber` are missing. `String(left).padStart(4, ' ')` on an empty string produces four spaces, which the LLM can misread as 'line 0' or as a valid…

### `src/server/prompts/languages.ts`
- [ ] `src/server/prompts/languages.ts:102-103` — [bug · medium] No null/undefined guard on the `path` parameter. If a caller passes `undefined` (e.g. an unindexed file), `path.split` throws a TypeError. Add an early return.
- [ ] `src/server/prompts/languages.ts:113-113` — [bug · medium] The compound persona is grammatically broken. Joining personas like "an expert TypeScript developer who prioritizes type safety..." with "and a senior React developer who focuses on..." produces a ma…

### `src/server/prompts/summary.ts`
- [ ] `src/server/prompts/summary.ts:45-45` — [bug · medium] `String.prototype.replace` with a string pattern only replaces the first occurrence. If the upstream producer ever embeds the substring `Review failed: ` inside the reason (e.g. nested message), the…

### `src/server/routes/api/auth.ts`
- [ ] `src/server/routes/api/auth.ts:9-13` — [bug · medium] Inconsistent response construction: The 401 path uses `Response.json` (global), while the success path uses `c.json` (Hono context helper). This mixes two response APIs which can lead to subtle diffe…

### `src/server/routes/api/jobs.ts`
- [ ] `src/server/routes/api/jobs.ts:63-64, 65-73` — [bug · medium] `job.updatedAt` is used to build the ETag and Last-Modified, but the function trusts it is a parseable timestamp. If `updatedAt` is ever `undefined`/null/malformed, `new Date(undefined).getTime()` yi…  _(+1 related in cluster: bug·medium)_
- [ ] `src/server/routes/api/jobs.ts:189-201` — [bug · medium] `/delete` does not check the job status before deleting, so a `queued`/`running` job whose workflow termination fails (see the `terminateJobWorkflow` swallow) is deleted out from under the worker, wh…

### `src/server/routes/api/models.ts`
- [ ] `src/server/routes/api/models.ts:35-41` — [security · low] API key in `providerCreateSchema`/`providerUpdateSchema` is `z.string().optional()` without `min(1)` — combined with `encryptedApiKeyFromBody` accepting `undefined` to mean "keep existing", an explic…
- [ ] `src/server/routes/api/models.ts:52-65` — [security · low] Global model config size_override model lookups: `globalModelConfigSchema` allows arbitrary `modelIdSchema` strings in `main`/`fallbacks`/`size_overrides[].model`. There is no server-side validation…
- [ ] `src/server/routes/api/models.ts:173-180` — [bug · medium] Sync semantics: `syncProviderModelCatalog` performs reads (upserts) but has no atomic guarantee with the subsequent `listLlmProviders`/`listModelConfigs` reads in the POST handler. If a DB read fails…
- [ ] `src/server/routes/api/models.ts:272-287` — [bug · medium] In PATCH handler, when changing `apiFormat` from a keyless format (e.g. cloudflare-workers-ai) to a key-required one without supplying `apiKey` or `clearApiKey`, `encryptedApiKey` remains `existing.e…
- [ ] `src/server/routes/api/models.ts:305-310, 312-317` — [bug · medium] DELETE endpoint returns a generic `result.reason` with HTTP 409 when the delete was blocked (e.g., provider still in use), but the message originates from the DB layer and is forwarded verbatim. This…  _(+1 related in cluster: bug·medium)_
- [ ] `src/server/routes/api/models.ts:337-358` — [bug · medium] `response` is declared with `let` but never initialized when a non-cloudflare, non-gemini/openai/anthropic format reaches the default branch — `return jsonError(...)` exits the switch but if the sche…

### `src/server/routes/api/stats.ts`
- [ ] `src/server/routes/api/stats.ts:9-10, 11-12` — [bug · medium] `parseInt(daysParam, 10)` will also accept mixed strings like `"30abc"` and silently parse them to `30`. Use a stricter check (e.g. `Number.isInteger` plus a regex/digits-only check, or `Number(daysP…  _(+1 related in cluster: bug·medium, security·low)_

### `src/server/routes/auth.ts`
- [ ] `src/server/routes/auth.ts:46-50` — [bug · medium] `profile.login.toLowerCase()` will throw a TypeError if GitHub ever returns a profile without a `login` field (e.g., suspended accounts, bot accounts returning only `id`). Because the surrounding `tr…

### `src/server/routes/webhook-bitbucket.ts`
- [ ] `src/server/routes/webhook-bitbucket.ts:85-94` — [bug · medium] `safeJsonParse` returns `undefined` when the body is not valid JSON. `bitbucketIdentityProjectionSchema.safeParse(undefined)` will succeed only by accident (Zod 3 returns a failure for non-object inp…
- [ ] `src/server/routes/webhook-bitbucket.ts:161-186` — [bug · medium] D-04 dedup footgun: `bytesToHex('')` on an empty hex string or zero-length bytea returns `''`, and the comparison `recentCommitSha === incomingCommitSha` treats empty strings as equal. If `recent.com…
- [ ] `src/server/routes/webhook-bitbucket.ts:221-221` — [bug · medium] `prAuthor` is computed via a brittle chain of `typeof`/`'in'` casts on `parsedBody` rather than reading from the validated `parsed.data`. The `pullRequestWebhookPayloadSchema` uses `.passthrough()` o…

### `src/server/routes/webhook.ts`
- [ ] `src/server/routes/webhook.ts:34-40` — [bug · medium] The parsed JSON is asserted to be `GitHubWebhookPayload`, but runtime structure is not validated before dereferencing `payload.repository.owner.login`. A validly signed malformed payload such as `{"r…

### `src/server/services/model.ts`
- [ ] `src/server/services/model.ts:166-184` — [bug · medium] markProviderUnavailable eagerly writes `Promise.resolve(true)` to the in-memory cache *before* the `APP_KV.put` call resolves. If the put fails (the catch block only logs a warning), the in-memory ca…
- [ ] `src/server/services/model.ts:417-427` — [bug · medium] submitReviewBatch permanently adds `resolved.modelName` to `asyncUnsupportedModels` on *any* submit failure, including transient 5xx / network blips / rate limits. Once added, every later file in the…

### `src/server/vcs/bitbucket.ts`
- [ ] `src/server/vcs/bitbucket.ts:229-250` — [bug · medium] `submitReview` aborts halfway through the inline-comment loop on the first thrown error from `postPullRequestComment`, leaving the PR with N partially-posted inline comments and no summary/footer. On…
- [ ] `src/server/vcs/bitbucket.ts:328-338` — [bug · medium] `anchorForComment` casts `line.newLineNumber` and `line.oldLineNumber` without verifying they are positive integers. The downstream POST body uses `line` as a positive integer, and `prCommentSchema`…

### `src/server/vcs/types.ts`
- [ ] `src/server/vcs/types.ts:18-19` — [bug · medium] `VcsPullRequest` makes `title`, `body`, `headRef`, `baseRef`, and `authorLogin` nullable but does not document whether `baseSha`/`baseRef` may be null when branches are deleted. Downstream rendering…

### `src/server/workflows/review.ts`
- [ ] `src/server/workflows/review.ts:63-63` — [bug · medium] `result.action === 'next_phase'` does an early `break` only inside the `if (result.freshInstance && nextJobId)` block. All other branches fall through to the bottom of the `if/else if/else` chain and…
- [ ] `src/server/workflows/review.ts:76-83` — [security · low] `deliveryId: crypto.randomUUID()` is good, but ensure `ReviewJobMessage`'s schema treats `deliveryId` and `jobId` as opaque, server-validated identifiers before they end up in D1 queries. The `if (jo…

### `src/shared/bitbucket.ts`
- [ ] `src/shared/bitbucket.ts:94-98` — [bug · medium] `reportDataSchema` validates `type` and `value` independently, so payloads such as `{ type: 'BOOLEAN', value: 'not-a-boolean' }` or `{ type: 'TEXT', value: true }` pass despite violating the Code Ins…

### `src/shared/github.ts`
- [ ] `src/shared/github.ts:34-39` — [bug · medium] `IssueCommentWebhookPayload` does not narrow `issue.pull_request` presence. Any downstream code that assumes a comment is attached to a PR (e.g. reading `issue.pull_request.url`) must explicitly chec…

### `src/shared/schema.ts`
- [ ] `src/shared/schema.ts:181-191` — [bug · medium] `reviewJobMessageSchema.superRefine` only enforces "jobId OR eventName". A message with neither `installationId`, `owner`, `repo`, nor `prNumber` will still pass validation but cannot be resolved to…
- [ ] `src/shared/schema.ts:284-284` — [bug · medium] `repoConfigRecordSchema.installed`/shape loss: there is no field for the legacy `installationId` being a per-provider (GitHub=number, Bitbucket=workspace) composite. `installationId` is typed as `z.s…
- [ ] `src/shared/schema.ts:456-456` — [bug · medium] The loose `dateStringSchema` (string|Date transform) is correctly used for DB-write paths that already store TIMESTAMPTZ, but the OUTPUT-side `vcsCredentialStatusSchema.tokenExpiresAt` reuses it — me…

### `test/bitbucket-fetch-mock.ts`
- [ ] `test/bitbucket-fetch-mock.ts:71-71, 79-81` — [bug · medium] The `originalFetch` snapshot is not sufficient for nested or overlapping installations. If an outer mock is active when this helper is installed, restoring the inner helper restores the outer handler…  _(+1 related in cluster: bug·medium)_

### `test/github-fetch-mock.ts`
- [ ] `test/github-fetch-mock.ts:43-44` — [bug · medium] existingLabels (line 43) and issueLabels (line 44) are declared as locals of installGitHubFetchMock, but they live as long as the fetch stub does. If a test calls installGitHubFetchMock twice (e.g. n…
- [ ] `test/github-fetch-mock.ts:88-95, 97-101` — [bug · medium] Math.min(reviewCallIndex, reviewResponses.length - 1) clamps the script index, so once the script is exhausted every subsequent POST replays the LAST scripted response. This silently hides ordering b…  _(+1 related in cluster: bug·medium, security·low)_
- [ ] `test/github-fetch-mock.ts:112-115, 117-121` — [bug · medium] POST .../issues/.../labels iterates body?.labels ?? [], which assumes labels is an array. If the client sends an unexpected shape (e.g. a single string, an object, or undefined), the code silently it…  _(+1 related in cluster: bug·medium, security·low)_

### `test/helpers.ts`
- [ ] `test/helpers.ts:156-174` — [bug · medium] saveTestProviderApiKey's INSERT...ON CONFLICT (model_id) leaves rows behind after each test run (no cleanup). On a shared CI database this can cause order-dependent flakes: a prior test inserting `'g…
- [ ] `test/helpers.ts:188-188` — [bug · medium] generateMockDiff's hunk header uses `lines.length` for both the old and new count, but the old side should be `0` because all lines are insertions (`+`) with no removals (`-`) or context (` `). A cor…
- [ ] `test/helpers.ts:236-249` — [security · low] signWebhookPayload accepts a raw `secret` and `payload` and only signs them. Consider also adding a helper or doc note about verifying that tests do not log the resulting signature alongside the payl…

### `test/setup.ts`
- [ ] `test/setup.ts:31-41` — [bug · medium] parseEnvValue is a minimal .env parser that silently mis-handles common cases. It does not strip a leading `export ` keyword, does not treat `#` inside a value as a comment (only at line start), and…
- [ ] `test/setup.ts:84-85` — [bug · medium] loadTestEnvFromFiles/assertRequiredTestEnv throw at module-import time, which crashes the Vitest worker before any test file is collected and produces a stack trace that points at this setup file rat…
- [ ] `test/setup.ts:122-125` — [bug · medium] isJsonLog uses substring matching on `args[0]` which is fragile. Any user-facing message that happens to contain both `"timestamp"` and `"level"` (e.g., documentation text, error messages printed ver…

### `vitest.config.ts`
- [ ] `vitest.config.ts:14-14` — [security · low] Mock file path is not validated: The alias 'cloudflare:workers' points to a local mock. If this file is missing or fails to load, alias resolution will fail at test time with a confusing error. Consi…
- [ ] `vitest.config.ts:26-26` — [bug · medium] Browser project spec inclusion/exclusion mismatch: The node project excludes 'test/browser/**' but the browser project only includes 'test/browser/**/*.spec.tsx'. Any *.spec.ts files under test/brows…
- [ ] `vitest.config.ts:36-36` — [bug · medium] Browser project omits 'environment' (defaults to 'browser') and does not set 'fileParallelism'. Combined with 'instances: [{ browser: 'chromium' }]' and no explicit 'rootDir', test isolation and para…

### `worker-configuration.d.ts`
- [ ] `worker-configuration.d.ts:1-3` — [bug · medium] `Ai.run()` returns `Promise<any>`, which defeats TypeScript's type safety. Callers cannot rely on the shape of the response and may introduce runtime errors. Define a generic or concrete return type…

## P3 — maintainability · style · docs · perf · test

### `.github/ISSUE_TEMPLATE/bug_report.yml`
- [ ] `.github/ISSUE_TEMPLATE/bug_report.yml:1-2, 1-3` — [documentation · low] The `release.yml` file referenced in the checklist is not part of this template file. If release configuration is expected in the same `.github` directory, it is missing entirely from the repository…  _(+1 related in cluster: documentation·low)_

### `.github/ISSUE_TEMPLATE/feature_request.yml`
- [ ] `.github/ISSUE_TEMPLATE/feature_request.yml:6-7, 11-13` — [documentation · low] The markdown intro says "make Codra better!" but the GitHub repo URL referenced in the project name is "Codra" — this is the only textual reference to the project name in the template. Worth confirmi…  _(+1 related in cluster: documentation·low, maintainability·low)_

### `.github/dependabot.yml`
- [ ] `.github/dependabot.yml:3-6, 3-11` — [maintainability · medium] Missing `ignore` policy: There is no `ignore` block to suppress disruptive updates (e.g., major version bumps, specific pinned dependencies, or dependencies known to break with upgrades). Without thi…  _(+2 related in cluster: maintainability·medium, maintainability·low)_

### `.github/workflows/ci.yml`
- [ ] `.github/workflows/ci.yml:3-6` — [performance · medium] `push:` trigger has no `branches:` or `paths:` filter, so every push to any branch (including feature branches, docs-only changes, or `*.md` edits) runs the full typecheck + test + Playwright browser…

### `.gitignore`
- [ ] `.gitignore:1-7` — [maintainability · low] `logs` and `pids` are listed as bare names without a leading `/`, so they match at any depth in the tree. If the project has subdirectories like `docs/logs/` or `scripts/pids/`, those will be silentl…
- [ ] `.gitignore:40-42` — [maintainability · low] Directory patterns are inconsistent: some use trailing slashes (`node_modules/`, `jspm_packages/`, `lib-cov` omitted, `bower_components` no slash, `.nuxt` no slash, `dist` no slash, `.vitepress/dist`…
- [ ] `.gitignore:82-84, 86-88, 88-88` — [maintainability · low] The `dist` entry is listed under the Nuxt.js section but is generic — it is also produced by Vite, Rollup, esbuild, Parcel, and many other bundlers. A bare `dist` entry will match at any depth, poten…  _(+2 related in cluster: maintainability·low, documentation·low)_
- [ ] `.gitignore:108-112` — [maintainability · low] The Docusaurus, SvelteKit, VitePress, and other tool-specific blocks all assume a single ecosystem. If the project uses multiple frameworks (e.g., SvelteKit + VitePress for docs), the ignore list is…
- [ ] `.gitignore:129-136, 138-140, 145-145, 151-162` — [maintainability · medium] The `.yarn` block is specific to Yarn 3.x Berry. The selective re-includes (`!.yarn/patches`, `!.yarn/plugins`, `!.yarn/releases`, `!.yarn/sdks`, `!.yarn/versions`) are correct for Yarn 3, but if thi…  _(+3 related in cluster: maintainability·medium, maintainability·low)_

### `LICENSE`
- [ ] `LICENSE:0-0` — [style · low] The FSF official AGPL-3 text (https://www.gnu.org/licenses/agpl-3.0.txt) uses the literal ASCII form 'http://fsf.org/' and '&lt;http://fsf.org/&gt;' / '&lt;http://www.gnu.org/licenses/&gt;' rather th…
- [ ] `LICENSE:497-501` — [documentation · high] Section numbering jumps from Section 10 directly to Section 12, omitting the Section 11 heading ('### 11. Automatic Licensing of Downstream Recipients' is missing) — but more importantly, no Section…
- [ ] `LICENSE:596-597` — [maintainability · medium] Pre-scan focus area #3: Verify the copyright year '2026' and author 'Devarshi Shimpi' are correct and consistent with repository metadata (e.g. package.json 'author', git initial-commit author/date,…  _(+1 related in cluster: maintainability·medium)_

### `db/migrations/001_initial.sql`
- [ ] `db/migrations/001_initial.sql:1-7` — [maintainability · medium] The migration mixes fresh CREATE TABLE statements with multi-version backfill logic in a single file. A migration titled `001_initial.sql` should establish the canonical schema only; legacy-shape det…
- [ ] `db/migrations/001_initial.sql:285-294` — [performance · medium] The `tmp_jobs_owner_repo_idx` is created with `CREATE INDEX` (not CONCURRENTLY) inside the migration, then dropped moments later. On a non-empty jobs table this serializes writes for the duration of…
- [ ] `db/migrations/001_initial.sql:304-316` — [performance · low] commit_sha/base_sha BYTEA conversion and type-mismatch guards: (1) the hex-decoding UPDATE runs row-by-row and can block concurrent inserts on a large jobs table — batch via cursor/loop with LIMIT or…
- [ ] `db/migrations/001_initial.sql:352-360` — [performance · low] Recreating `jobs_repo_idx`, `jobs_active_idx`, `jobs_created_idx`, `jobs_head_sha_idx` via DROP + CREATE issues invalidation overhead. CREATE INDEX IF NOT EXISTS is idempotent and avoids the rebuild…
- [ ] `db/migrations/001_initial.sql:536-536` — [maintainability · low] Storing `encrypted_api_key` as a single TEXT column lacks documentation on what encryption is applied (column-level pgcrypto? KMS?). Without knowing the threat model, downstream code may inadvertentl…
- [ ] `db/migrations/001_initial.sql:639-639` — [test · medium] No `_test/` directory or migration test fixtures are referenced here; the pre-scan focus areas 1-5 all describe scenarios where the migration will silently corrupt data. A migration of this complexit…

### `db/migrations/002_jobs_async_review.sql`
- [ ] `db/migrations/002_jobs_async_review.sql:2-2, 9-12, 20-21` — [maintainability · medium] `ON CONFLICT DO NOTHING` makes these values write-once: changing the migration defaults later will not update installations that already contain the keys, and existing values are not validated agains…  _(+2 related in cluster: performance·low, maintainability·medium)_

### `db/migrations/003_vcs_provider_foundation.sql`
- [ ] `db/migrations/003_vcs_provider_foundation.sql:0-0` — [test · medium] No test coverage for re-run idempotency or partial-failure paths. There is no assertion that running this migration twice against the same database is a no-op, nor that the DROP + ADD ordering handle…
- [ ] `db/migrations/003_vcs_provider_foundation.sql:25-32, 34-40, 39-40` — [maintainability · medium] Guarded DO block is fragile: the guard reads `conname = 'repositories_vcs_provider_owner_repo_key'` and the ADD CONSTRAINT uses the same literal. Any future rename must touch both sites, and silent d…  _(+2 related in cluster: maintainability·medium)_

### `db/migrations/004_vcs_credentials.sql`
- [ ] `db/migrations/004_vcs_credentials.sql:20-20, 27-29` — [maintainability · medium] **Focus area #2: `token_expires_at` semantics are documented only in comments; no schema-level guard against impossible values.** The column is `TIMESTAMPTZ NULL` with the comment "NULL means no expi…  _(+1 related in cluster: maintainability·low, maintainability·medium)_
- [ ] `db/migrations/004_vcs_credentials.sql:41-48, 55-56` — [performance · medium] **Focus area #4: provider-filtered lookups on `(workspace, repo_slug)` will NOT use the new index.** The index `idx_vcs_credentials_workspace_slug` is on `(workspace, repo_slug)` only. Any query that…  _(+2 related in cluster: maintainability·low, performance·medium, test·medium)_

### `db/migrations/005_bitbucket_repo_identity.sql`
- [ ] `db/migrations/005_bitbucket_repo_identity.sql:7-7` — [test · medium] No schema-test coverage referenced for the dual-write invariant or the Bitbucket insert path. The migration lacks any inline test or accompanying test file that exercises: (1) inserting a Bitbucket r…
- [ ] `db/migrations/005_bitbucket_repo_identity.sql:24-29, 36-36` — [maintainability · high] No schema-enforced dual-write invariant between `owner` and `workspace` for Bitbucket rows. The header comment (REV-R-C) mandates that Bitbucket rows populate both columns with the workspace slug, bu…  _(+1 related in cluster: maintainability·high, performance·low)_

### `db/migrations/006_review_comment_confidence.sql`
- [ ] `db/migrations/006_review_comment_confidence.sql:7-10, 12-14, 14-14` — [maintainability · medium] FAIL-OPEN semantics are asserted in the header comment but not enforced by the migration. The header says "NULL = treated FAIL-OPEN (always kept) by the finalize gate", but nothing here verifies that…  _(+2 related in cluster: maintainability·medium, maintainability·low)_

### `index.html`
- [ ] `index.html:6-6, 7-8, 9-10, 12-15` — [maintainability · medium] Missing a `<base href="/">` (or equivalent router base). If the app is ever served from a sub-path (e.g., `https://host/opencodra/`), all absolute paths like `/icons/opencodra-icon-dark.svg` and `/sr…  _(+3 related in cluster: maintainability·medium, maintainability·low, performance·low)_

### `scripts/apply_migrations.js`
- [ ] `scripts/apply_migrations.js:29-29` — [maintainability · medium] The migration count is not the total number of migrations: adding a count to the highest version is wrong for non-contiguous versions (e.g. max 10 plus one migration logs 11 even if only three rows e…

### `scripts/fix-max-files.mjs`
- [ ] `scripts/fix-max-files.mjs:23-23` — [maintainability · medium] The empty catch suppresses every read error, including permission failures and malformed/unavailable files. This makes a fallback failure indistinguishable from a missing file and can lead to connect…

### `scripts/render-design.mjs`
- [ ] `scripts/render-design.mjs:11-11, 14-14` — [maintainability · low] Hard-coded `'2026-07-18T10:00:00.000Z'` appears in three places (`now`, `addInitScript`, and as the implicit anchor for `ago(...)`). If the fixture date ever changes it must be updated everywhere; ex…  _(+1 related in cluster: maintainability·low)_
- [ ] `scripts/render-design.mjs:132-144, 143-143, 146-146` — [performance · low] `waitForServer` polls every 150 ms without abort signal. On success it returns immediately, but on the success path the Vite server may still be serving the very first request when `page.goto` is cal…  _(+2 related in cluster: performance·low, maintainability·low, test·low)_
- [ ] `scripts/render-design.mjs:214-215` — [maintainability · low] Final console.log lines hard-code the rendered filenames; if a theme name or output filename ever changes they will silently drift out of sync. Derive the log messages from the loop (`for (const them…

### `scripts/setup-cloudflare.js`
- [ ] `scripts/setup-cloudflare.js:64-64, 66-67` — [maintainability · medium] Large blocks of duplicated logic between `handleKVNamespace` and `handleHyperdrive`: the prompt menu, manual-entry fallback, and 'fetch existing' branches are nearly identical. Extract a shared `sele…  _(+1 related in cluster: maintainability·low, maintainability·medium)_
- [ ] `scripts/setup-cloudflare.js:303-316` — [documentation · medium] The comment `Wrangler sometimes exits with 0 even when not logged in` is helpful; preserve it. But the surrounding try/catch lumps network errors and auth errors together, so a transient DNS failure…  _(+1 related in cluster: maintainability·low, documentation·medium)_
- [ ] `scripts/setup-cloudflare.js:388-389` — [maintainability · high] `routesConfigStr` builds the entire routes object via string interpolation with the user-supplied `customDomain`. If `customDomain` contains a newline, quote, or backslash, the resulting JSONC is mal…
- [ ] `scripts/setup-cloudflare.js:405-411` — [maintainability · high] `customDomain` is interpolated raw into a regex pattern and into the JSONC routes array without escaping. A custom domain containing `"` or `\` will produce invalid JSONC. Use the same `escapeJson` h…
- [ ] `scripts/setup-cloudflare.js:495-495` — [maintainability · low] Spelling/grammar in log message: "Wrangler jsonc was not modified." — should read "wrangler.jsonc" to match the filename referenced elsewhere.
- [ ] `scripts/setup-cloudflare.js:536-536` — [maintainability · medium] `secretValue` (which may be a private key or API token) is held in JS memory for the lifetime of the script and printed to the terminal by `prompts`. There is no zeroization. Consider warning the use…

### `scripts/test.mjs`
- [ ] `scripts/test.mjs:1-2` — [performance · low] `readFileSync` and `spawnSync` block the event loop, which is fine for a short-lived bootstrap script — flagging this only to note that if this script is ever reused for a long-running watch mode, th…
- [ ] `scripts/test.mjs:14-14, 16-30` — [test · medium] This bootstrap script has no test coverage. `parseEnvValue`, `usableEnvValue`, and the precedence rules of `loadEnvFiles` are pure functions with non-trivial edge cases (quoted strings, inline commen…  _(+1 related in cluster: maintainability·low, test·medium)_
- [ ] `scripts/test.mjs:66-79` — [maintainability · low] `process.exit(...)` is called directly on the failure paths, bypassing any process-level error handler the rest of the project may install (e.g. an `uncaughtException` handler, a Sentry/Datadog init,…

### `src/client/app.css`
- [ ] `src/client/app.css:12-15, 21-22` — [maintainability · medium] Duplicate `:root` block: lines 12-15 (easing tokens) and lines 20-81 (light-mode tokens) split the same logical theme into two `:root` declarations. Consolidate them into a single `:root` to keep tok…  _(+1 related in cluster: maintainability·medium, style·low)_
- [ ] `src/client/app.css:236-245` — [performance · low] `body::before` is created in both light and dark modes but in light mode is given `background: none` with `transition: opacity 0.4s`. The opacity never changes, so the transition (and the fixed posit…
- [ ] `src/client/app.css:322-326` — [maintainability · low] `@utility animate-in` / `@utility animate-out` set `animation-duration` / `timing-function` / `fill-mode` but never `animation-name`, so by themselves they produce no visible animation. They only wor…
- [ ] `src/client/app.css:506-518` — [maintainability · low] `@utility surface-static` and `@utility surface-static-shadow` are nearly identical except for initial shadow vs hover-shadow semantics. Two near-identical utilities are easy to confuse; collapse the…
- [ ] `src/client/app.css:572-573` — [maintainability · medium] Several places inline raw OKLCH literals instead of the existing semantic tokens: `.P2` / `.quality` tag utilities use `oklch(95% 0.06 65)` / `oklch(50% 0.14 65)` / `oklch(83% 0.09 65)` (should be `-…
- [ ] `src/client/app.css:649-651` — [maintainability · high] `.dashboard-sidebar` is hard-coded to `#000000` in both light and dark modes with `!important`, completely bypassing the design-token system. This makes future theme changes painful and can break con…
- [ ] `src/client/app.css:662-670` — [maintainability · high] Two `.app-shell-content` blocks in the same file redeclare the same surface tokens with slightly different OKLCH values (one uses 96.3% / 90.9% / 88.5% on hue 286.3, the other 97.2% / 94.5% / 88.5% o…
- [ ] `src/client/app.css:790-792` — [maintainability · low] `.dashboard-sidebar-tooltip:not(.dashboard-sidebar-action-label) { display: none !important; }` matches the negation of `.dashboard-sidebar-action-label`, which is intended to hide the tooltip in exp…
- [ ] `src/client/app.css:892-894` — [maintainability · medium] The Sonner toast styling (`.codra-toast*`) overrides nearly every property with `!important` and duplicates every variant for `.dark`. High `!important` density makes future Tailwind v4 upgrades (uti…

### `src/client/components/features/job-detail/comment-card.tsx`
- [ ] `src/client/components/features/job-detail/comment-card.tsx:0-0` — [style · low] The code block colors are static CSS-variable references but are supplied through an inline style. This conflicts with the component's styling approach and makes them harder to override via classes/t…  _(+1 related in cluster: style·low, maintainability·low)_

### `src/client/components/features/job-detail/constants.ts`
- [ ] `src/client/components/features/job-detail/constants.ts:4-10, 5-9` — [maintainability · medium] Loose key typing defeats the type system: `Record<string, ...>` allows any string key, so `severityConfig[someUntypedVar]` compiles silently and returns `undefined` (call sites then rely on `?? sever…  _(+1 related in cluster: maintainability·medium)_

### `src/client/components/features/job-detail/file-finding.tsx`
- [ ] `src/client/components/features/job-detail/file-finding.tsx:10-10` — [maintainability · low] `safeRehypePlugins` is declared as a module-level constant containing `[rehypeRaw, rehypeSanitize]`, but the rest of this file (and `comment-card.tsx`) repeats the same one-liner. Given the XSS-sensi…
- [ ] `src/client/components/features/job-detail/file-finding.tsx:39-43` — [maintainability · low] The error panel uses raw inline `style` attributes referencing `var(--danger-bg)`, `var(--danger-border)` and `var(--danger)` while the rest of the component (and `comment-card.tsx`) consistently use…
- [ ] `src/client/components/features/job-detail/file-finding.tsx:63-63` — [maintainability · low] The inline `: any` style typing on the map callback is unnecessary and inconsistent with the rest of the codebase (`comment: ParsedReviewComment` is already inferred from `file.parsedComments`). The…

### `src/client/components/features/job-detail/job-findings-list.tsx`
- [ ] `src/client/components/features/job-detail/job-findings-list.tsx:27-27` — [maintainability · low] The tab callback accepts an arbitrary string and asserts it is one of the two valid values without checking it. If the Tabs implementation emits an unexpected value, `viewBy` can enter an unsupported…
- [ ] `src/client/components/features/job-detail/job-findings-list.tsx:56-57` — [performance · low] The failed-file predicate is evaluated three times during the same render (the `some` check, count, and list). For large jobs this adds avoidable traversal and can let the displayed count/list diverg…
- [ ] `src/client/components/features/job-detail/job-findings-list.tsx:80-85` — [performance · medium] Comments are flattened and filtered from every file once for each severity during render. This repeats the full files/comments traversal for every group and on unrelated re-renders; derive the groupe…
- [ ] `src/client/components/features/job-detail/job-findings-list.tsx:112-114` — [maintainability · medium] Using the array index as the key means inserting, removing, or reordering findings causes existing `CommentCard` instances to be reused for different comments, which can produce stale internal state…

### `src/client/components/features/job-detail/job-header.tsx`
- [ ] `src/client/components/features/job-detail/job-header.tsx:105-138` — [maintainability · low] Three icon-only buttons repeat the exact same pattern (`variant="outline"|"destructive-outline"`, `size="icon"`, a loading-state ternary using `Loader2`, and identical `disabled`/`title`/`aria-label`…

### `src/client/components/features/job-detail/job-meta-cards.tsx`
- [ ] `src/client/components/features/job-detail/job-meta-cards.tsx:34-41, 43-52` — [style · low] Nested ternaries: the `accentColor` expression chains four mutually-exclusive status branches in nested `?:` form, which the project rules disallow. Extract a small lookup (e.g. `const accentByStatus…  _(+1 related in cluster: style·low)_
- [ ] `src/client/components/features/job-detail/job-meta-cards.tsx:125-132` — [maintainability · low] Duplicated metadata-row markup: the `<dt>…</dt><dd>…</dd>` pair (including the identical uppercase/tracking className for the label) is repeated six times across the grid. Extract a small `MetaRow({…
- [ ] `src/client/components/features/job-detail/job-meta-cards.tsx:191-197, 202-202` — [style · low] Dynamic inline style: the error/partial banner reads theme colors from CSS custom properties (`var(--warning-bg)`, etc.) at runtime. These can't be statically verified, and if a theme doesn't define…  _(+1 related in cluster: style·low, maintainability·low)_
- [ ] `src/client/components/features/job-detail/job-meta-cards.tsx:220-222` — [maintainability · medium] Key stability: `steps.map((step, idx) => <StepRow key={step.name || idx} ... />` falls back to the array index when `step.name` is missing. If the server ever reorders steps mid-stream (which a live…

### `src/client/components/features/job-detail/job-review-overview.tsx`
- [ ] `src/client/components/features/job-detail/job-review-overview.tsx:0-0` — [maintainability · low] `renderSummary` mixes data preprocessing (strip header) with template-string UI copy. The hardcoded OpenCodra marketing block (intro paragraph, about details, separator) is duplicated business logic…
- [ ] `src/client/components/features/job-detail/job-review-overview.tsx:9-9` — [maintainability · medium] `safeRehypePlugins = [rehypeRaw, rehypeSanitize]` is declared at module scope but the name implies a guarantee that isn't enforced — `rehypeRaw` first passes HTML through and `rehypeSanitize` follows…
- [ ] `src/client/components/features/job-detail/job-review-overview.tsx:19-19` — [performance · low] `allComments` is built via `job.files.flatMap` on every render. For large jobs with many files/comments this re-traverses the tree each render. Memoize with `useMemo` keyed on `job` (or `job.files`)…
- [ ] `src/client/components/features/job-detail/job-review-overview.tsx:35-37` — [maintainability · medium] The strip-header branching is fragile and partly redundant: the explicit `startsWith('### 💡 OpenCodra Review') || startsWith('### 💡 Codra Review')` checks are subsumed by the `includes('OpenCodra R…

### `src/client/components/features/job-detail/job-skeleton.tsx`
- [ ] `src/client/components/features/job-detail/job-skeleton.tsx:14-14` — [style · low] The error banner uses an inline style object even though these values are static CSS token references. This conflicts with the stated project rule to avoid inline styles and makes the presentation ha…

### `src/client/components/features/models/model-chain.tsx`
- [ ] `src/client/components/features/models/model-chain.tsx:40-46` — [maintainability · low] Optional chaining on `config.fallbacks` is redundant given the declared type. `ModelRouteConfig.fallbacks` is typed as `string[]` (required), so `config.fallbacks?.length ?? 0` should be `config.fall…
- [ ] `src/client/components/features/models/model-chain.tsx:83-86` — [performance · low] `filteredModels` recomputes on every `models` reference change, including unrelated updates. `useMemo` here is fine, but `ModelSelector` is rendered inside `fallbacks.map` and inside each tier's `Mod…
- [ ] `src/client/components/features/models/model-chain.tsx:168-175` — [maintainability · low] `hideLabels` prop on `ModelSelector` is only partially honored. The labels for both Provider and Model Selects are gated on `hideLabels`, but `hideLabels` is used inconsistently by callers (`ModelCha…
- [ ] `src/client/components/features/models/model-chain.tsx:295-297` — [maintainability · low] `largestTier` description label is misleading. The text reads `files over {largestTier} lines`, but the tier's `max_lines` is a *threshold* for that tier (e.g., "files up to 300 lines"), not "files o…
- [ ] `src/client/components/features/models/model-chain.tsx:317-319` — [maintainability · low] Nested ternary for label rendering. The expression `tiers.length > 1 ? index + 1 : ''` is a simple ternary, which is fine; however, the surrounding className uses nested ternaries via `cn(...)` exten…

### `src/client/components/features/reviews/live-review-stepper.tsx`
- [ ] `src/client/components/features/reviews/live-review-stepper.tsx:8-9, 13-17, 18-25, 18-30, 32-33` — [maintainability · medium] Long `if`/`else if` chain for status-to-label mapping: the entire `activeLabel` derivation is a flat ladder of `else if` branches with no shared structure. Replace with a single `Record<JobSummary['s…  _(+4 related in cluster: style·low, maintainability·medium)_
- [ ] `src/client/components/features/reviews/live-review-stepper.tsx:43-51` — [maintainability · medium] Style map omits entries for `cancelled` and `stopped` (and any future status). `cls = styles[status] ?? styles.queued` then renders those states in the muted `queued` chip colour, which is misleading…

### `src/client/components/features/stats/overview-stats.tsx`
- [ ] `src/client/components/features/stats/overview-stats.tsx:0-0` — [maintainability · low] **Duplicate icon/label/value/trend shape across four items** — The four KPI entries repeat the same shape four times. Extract a small helper or a data-driven config to remove duplication and make add…
- [ ] `src/client/components/features/stats/overview-stats.tsx:25-30` — [maintainability · medium] **Magic-number baselines for mock trends** — `40`, `100`, `80`, `20` are unnamed magnitudes that drive the visual scale of the fabricated sparklines and have no relation to the actual `stats.totals`…

### `src/client/components/features/stats/stats-grid.tsx`
- [ ] `src/client/components/features/stats/stats-grid.tsx:14-17, 19-23, 26-33` — [maintainability · medium] The `type` of the `StatsGridProps.columns` prop is `number`, but the lookup uses `columns as 2 | 4` to bypass the type system. Tightening the prop type to `2 | 4` would remove the unsafe cast entirel…  _(+3 related in cluster: maintainability·medium, maintainability·low)_
- [ ] `src/client/components/features/stats/stats-grid.tsx:48-50` — [maintainability · low] The `0{i + 1}` index label uses the same array index that was (correctly) flagged as an unstable key candidate. Since this label is purely cosmetic and could differ from the chosen stable key, that's…

### `src/client/components/features/stats/time-range-select.tsx`
- [ ] `src/client/components/features/stats/time-range-select.tsx:10-13, 13-18` — [maintainability · low] Accepting `triggerStyle` and forwarding it as an inline style directly violates the "avoid inline styles" guideline. If the style is truly static per call site, it should be passed via `className`/Ta…  _(+1 related in cluster: maintainability·low)_

### `src/client/components/layout/page-header.tsx`
- [ ] `src/client/components/layout/page-header.tsx:21-26, 34-37` — [maintainability · low] Redundant Fragment wrapping a single <header>. The fragment adds noise without benefit and can mask structural changes during review. Return the <header> directly.  _(+1 related in cluster: maintainability·low, style·low)_
- [ ] `src/client/components/layout/page-header.tsx:51-55` — [maintainability · low] actions container has no semantic role or aria-label. When actions are icon-only buttons, screen readers may announce them without context. Consider accepting an `actionsLabel` prop or wrapping in a…

### `src/client/components/motion/shared-layout-bg.tsx`
- [ ] `src/client/components/motion/shared-layout-bg.tsx:35-46` — [maintainability · low] Duplicated variant definitions for reduced motion. `variants` and `reducedVariants` differ only in whether `filter: blur(...)` is present, and both `exit` callbacks do the same `!isActive ? {...} : {…
- [ ] `src/client/components/motion/shared-layout-bg.tsx:64-84` — [maintainability · medium] Inner component declared inside the render. The mapping block contains an inline anonymous function that returns JSX with nested motion components — this is fine for inline JSX, but combined with `cl…

### `src/client/components/motion/smooth-scroll.tsx`
- [ ] `src/client/components/motion/smooth-scroll.tsx:35-36` — [documentation · low] JSDoc on `velocity` claims units are "px/frame", but the implementation is `(px/ms) * 16`, which is px/frame only at exactly 60 Hz. At 120 Hz the returned value is 2x too large; at 30 Hz it's 0.5x. E…
- [ ] `src/client/components/motion/smooth-scroll.tsx:87-90` — [maintainability · medium] resolveTop: when the source is a non-Window HTMLElement, the function uses `el.offsetTop`, which is the element's offset relative to its `offsetParent` — not relative to `source`. If the target is ne…
- [ ] `src/client/components/motion/smooth-scroll.tsx:103-103` — [maintainability · low] The `lenis` field in `SmoothScrollApi` is typed as `Lenis | null`, but `Lenis` is imported only as a type. The exported `useSmoothScroll().lenis` value will be `null` in every non-provider case, and…

### `src/client/components/motion/stepped-slider.tsx`
- [ ] `src/client/components/motion/stepped-slider.tsx:33-33` — [maintainability · low] `seedFromId('id')` for very short ids can collide (e.g., `'a'` and `'b'` both produce small hashes that modulo 997 may cluster). Not a correctness issue for typical usage, but worth noting that the f…
- [ ] `src/client/components/motion/stepped-slider.tsx:61-62` — [maintainability · low] `'aria-label'` and `'aria-labelledby'` props are typed but the validation rule "use one or the other, not both" is not enforced. Recommend either documenting the intent in JSDoc or normalizing to a s…
- [ ] `src/client/components/motion/stepped-slider.tsx:102-108, 110-113` — [maintainability · low] `hue`, `glowColor`, `fillGradient`, `dotDriftDuration`, `glowDuration` are recomputed every render even though they only depend on `isDark`, `isMaxed`, and `seed`. Promote to `useMemo` to avoid re-st…  _(+1 related in cluster: maintainability·low, performance·low)_
- [ ] `src/client/components/motion/stepped-slider.tsx:153-159` — [maintainability · low] `onPointerMove` is bound to the entire track div, so the browser will fire `pointermove` at the native rate (often 120Hz+) for the full window even when the pointer is far from the track, and React w…
- [ ] `src/client/components/motion/stepped-slider.tsx:241-253, 259-267` — [maintainability · low] Duplicate "tick render" logic: the unlabeled tick dots inside the track (lines around `<div className="pointer-events-none absolute inset-x-2 inset-y-0">`) and the labeled tick row below the track (l…  _(+1 related in cluster: maintainability·low, performance·low)_

### `src/client/components/motion/tabs.tsx`
- [ ] `src/client/components/motion/tabs.tsx:0-0` — [maintainability · low] `capitalize` baked into the pill/segment button classes silently transforms `children` text — if any consumer passes `children` that is not a plain string (icons, mixed-case acronyms, JSX with `<span…
- [ ] `src/client/components/motion/tabs.tsx:48-56` — [performance · low] Missing `useCallback` (or a `useEffect` to mirror external changes) for `setValue` and the provider `value`. Every render of `<Tabs>` creates a new `setValue` reference and a new context value, forci…
- [ ] `src/client/components/motion/tabs.tsx:101-107` — [maintainability · low] Duplicated JSX in the two `TabsTrigger` branches: the `button` element (type, role, aria-selected, onClick, base classes, active/inactive text colors) is repeated verbatim in the underline branch and…
- [ ] `src/client/components/motion/tabs.tsx:134-134` — [style · low] `borderRadius` set via inline `style` for the indicator is a dynamic style and is fine, but the value `9999` for the pill is magic. Add a brief comment or extract to a `RADIUS` constant alongside `EA…
- [ ] `src/client/components/motion/tabs.tsx:166-167` — [performance · low] `useReducedMotion()` is called twice in the same tree (once in `Tabs`, once in `TabsContent`). Both should return the same value, but this is redundant work and couples the two components. Either lif…

### `src/client/components/shared/bitbucket-mark.tsx`
- [ ] `src/client/components/shared/bitbucket-mark.tsx:2-2` — [maintainability · low] Props accept any numeric `size`, including 0, negative numbers, or `NaN`, which would produce invalid SVG dimensions (or a hidden icon with size=0). The sibling `GithubMark` has the same shape, so th…
- [ ] `src/client/components/shared/bitbucket-mark.tsx:12-12` — [maintainability · low] No `displayName` is set on the component. Combined with the very generic JSDoc reference to "D-23" and the lack of any internal source comment for the SVG `viewBox` / path coordinates, future maintai…

### `src/client/components/shared/empty-state.tsx`
- [ ] `src/client/components/shared/empty-state.tsx:11-21` — [maintainability · medium] `linkAction` and `action` are conceptually mutually exclusive (two call-to-action buttons stacked under an empty state looks like a UX bug), but the props allow both and the component renders both. E…

### `src/client/components/shared/jobs-table.tsx`
- [ ] `src/client/components/shared/jobs-table.tsx:77-84` — [maintainability · low] `formatDate` uses `toLocaleString` with no explicit locale, so it will use the user's browser locale. Combined with the relative time formatter using a different `Intl.RelativeTimeFormat` instance (a…
- [ ] `src/client/components/shared/jobs-table.tsx:109-132` — [maintainability · low] NumericMetric's `strong` prop is declared with a default value but is never passed by any caller. This is dead code per the checklist; either remove the prop or actually use it where appropriate (e.g…
- [ ] `src/client/components/shared/jobs-table.tsx:170-178` — [maintainability · medium] Duplicate rendering logic between JobMobileCard and the desktop `<tr>` block leads to divergent UX. For example, the mobile "verdict" empty state renders "No verdict" while the desktop empty state re…
- [ ] `src/client/components/shared/jobs-table.tsx:193-195, 202-204` — [maintainability · low] Mobile card's `created` cell renders only `formatRelativeDate(job.createdAt)` (e.g., "2 hours ago"), while desktop's `created` cell renders BOTH `formatDate(job.createdAt)` and `formatRelativeDate(jo…  _(+1 related in cluster: maintainability·low)_
- [ ] `src/client/components/shared/jobs-table.tsx:263-268, 271-277, 279-279` — [performance · low] Per the React checklist, defining new components inside another component's render path is discouraged. While `JobMobileCard` is properly hoisted to module scope, the JSX for each cell (repo, pr, sta…  _(+2 related in cluster: performance·low, maintainability·low)_
- [ ] `src/client/components/shared/jobs-table.tsx:342-355` — [maintainability · low] Dead/inconsistent fallback in the desktop "verdict" cell. Mobile uses "No verdict" while desktop uses "-" for the same null verdict state. Pick one and share it (also: a hyphen is a less informative…

### `src/client/components/shared/opencodra-logo.tsx`
- [ ] `src/client/components/shared/opencodra-logo.tsx:15-16` — [maintainability · medium] `className` merge order is misleading: the hardcoded default `text-lg` is listed before the consumer-provided `className`, so a caller passing `text-sm` (or `text-xl`, etc.) will silently fail to ove…
- [ ] `src/client/components/shared/opencodra-logo.tsx:26-36` — [maintainability · low] Magic numbers in the SVG (`cx`/`cy` 50, `r` 27, `strokeWidth` 12, `strokeDasharray` "132 38", `transform` rotate -16) make the glyph's visual tuning opaque to future maintainers and tightly coupled t…

### `src/client/components/shared/page-header-actions.tsx`
- [ ] `src/client/components/shared/page-header-actions.tsx:19-20, 24-36, 29-39, 37-37` — [style · medium] Hardcoded theme color bypassing design tokens: `btnBg = isDark ? undefined : '#ffffff'` encodes a palette value directly in the component instead of using a design-token-driven class. This duplicates…  _(+3 related in cluster: style·medium, maintainability·low, style·low)_

### `src/client/components/shared/skeleton.tsx`
- [ ] `src/client/components/shared/skeleton.tsx:12-30, 38-38` — [style · low] Mix of default-value styles: `className = ''` uses legacy parameter default while `width`, `height`, `borderRadius` use `??` inside the style object. This is mostly fine because `??` is required for…  _(+1 related in cluster: style·low, maintainability·low)_

### `src/client/components/shared/sparkline.tsx`
- [ ] `src/client/components/shared/sparkline.tsx:3-4, 4-4, 7-8` — [performance · low] `chartData` is recomputed on every render: `data.map(...)` runs on every parent re-render, creating a new array reference each time. Combined with Recharts' internal diffing, this can trigger unneces…  _(+2 related in cluster: performance·low, maintainability·low)_
- [ ] `src/client/components/shared/sparkline.tsx:22-24` — [performance · medium] Animation on a decorative, always-visible overlay: `isAnimationActive={true}` causes the sparkline to re-animate on every prop change (and on every responsive resize, which Recharts triggers frequent…

### `src/client/components/ui/alert.tsx`
- [ ] `src/client/components/ui/alert.tsx:0-0` — [maintainability · medium] className ordering: `className` is passed to `cn(...)` before `{...props}` is spread on the element, so any `className` provided via `...props` would be overwritten by the explicit `className` attrib…
- [ ] `src/client/components/ui/alert.tsx:50-54` — [maintainability · medium] Inline styles for theming: the component injects `backgroundColor`, `borderColor`, and `color` via a `style` object on every render. This allocates a new object per render (preventing memoization dow…

### `src/client/components/ui/badge.tsx`
- [ ] `src/client/components/ui/badge.tsx:4-5, 13-14, 17-17` — [maintainability · high] The `badge-warning` class used in the `warning` variant is not defined in this file and is not one of the Tailwind utility classes used elsewhere in the variants (which use `bg-*-bg text-* border-*-b…  _(+2 related in cluster: maintainability·medium, style·low, maintainability·high)_

### `src/client/components/ui/button.tsx`
- [ ] `src/client/components/ui/button.tsx:15-18` — [maintainability · high] Custom variant tokens 'danger-border', 'danger-bg', 'warning-border', 'warning-bg' (and corresponding foreground tokens 'danger', 'warning') are not part of the standard shadcn theme. If these tokens…
- [ ] `src/client/components/ui/button.tsx:48-59, 52-56` — [maintainability · medium] When `asChild` is true the rendered element is the consumer's child (commonly an `<a>`). `React.forwardRef<HTMLButtonElement, ButtonProps>` ties the ref type to HTMLButtonElement, which is too narrow…  _(+1 related in cluster: maintainability·medium, maintainability·low)_

### `src/client/components/ui/card.tsx`
- [ ] `src/client/components/ui/card.tsx:4-12, 4-20, 22-27` — [maintainability · medium] CardTitle is hardcoded as an <h3>, which is a significant accessibility concern. Card components are often reused in varying document contexts (some inside an <h2> section, others at the top level wh…  _(+2 related in cluster: performance·low, maintainability·low, maintainability·medium)_

### `src/client/components/ui/confirm-dialog.tsx`
- [ ] `src/client/components/ui/confirm-dialog.tsx:36-39` — [maintainability · low] No `Dialog.Title`/Description fallback for `sr-only`: If a future consumer wants to hide the title/description visually (e.g. icon-only) Radix still requires the elements for screen readers. Currentl…

### `src/client/components/ui/dropdown-menu.tsx`
- [ ] `src/client/components/ui/dropdown-menu.tsx:85-87` — [maintainability · low] The `DropdownMenu` component always renders children (including the trigger) regardless of `open`, but the portal-mounted content uses `AnimatePresence` correctly. However, `aria-expanded` and `aria-…
- [ ] `src/client/components/ui/dropdown-menu.tsx:189-195` — [performance · low] The positioning effect attaches `scroll` and `resize` listeners on `window` for the lifetime of the open state; if many dropdowns open in sequence this is fine (each cleans up), but `getBoundingClien…
- [ ] `src/client/components/ui/dropdown-menu.tsx:222-223` — [maintainability · medium] Project rules explicitly prohibit nested ternaries. The `originX`/`originY` derivations each use 3+ levels of nesting and should be refactored into lookup tables or small helpers for readability and…
- [ ] `src/client/components/ui/dropdown-menu.tsx:252-252` — [performance · low] Every render of `DropdownMenuContent` re-creates `transition`, `originX`, `originY`, and the `style` object passed into `motion.div`. `style` is recreated by `setStyle` and `setStyle` itself is fine,…

### `src/client/components/ui/input.tsx`
- [ ] `src/client/components/ui/input.tsx:4-4, 11-12` — [maintainability · medium] `className` override relies on `cn()` performing a Tailwind-aware merge (e.g., via `tailwind-merge`) for conflicts like `border-zinc-200` vs. a consumer-supplied `border-red-500`. If `cn()` only conc…  _(+1 related in cluster: maintainability·low, maintainability·medium)_

### `src/client/components/ui/select.tsx`
- [ ] `src/client/components/ui/select.tsx:204-212` — [maintainability · low] The motion configuration block (kf, kfT, flatT, nearGap, nearRadius, gapT, radiusT, instant) contains magic numbers (0.6, 0.42, 0.3, 0.16, 8, 12, etc.) with no central constants. Several are duplicat…
- [ ] `src/client/components/ui/select.tsx:350-354` — [maintainability · low] Option buttons inside the listbox use inline ref callbacks that mutate an array via closure. These run on every render and on unmount (with `null`). On hot re-renders (e.g., during the stagger animat…

### `src/client/components/ui/switch.tsx`
- [ ] `src/client/components/ui/switch.tsx:4-7, 12-23` — [maintainability · low] The `onChange` prop type is kept on the public Props interface but its declaration `Omit<...,'onChange'>` strips it from the inherited signature, then it's added back here as an optional re-declarati…  _(+1 related in cluster: maintainability·low)_

### `src/client/hooks/use-job-detail.ts`
- [ ] `src/client/hooks/use-job-detail.ts:19-19, 27-27` — [maintainability · medium] `terminalStatuses` and the route literals (`/jobs/${id}`, `/jobs`) are hardcoded business strings/URL paths. Per the review rules, these should be centralized in a constants or router module to avoid…  _(+2 related in cluster: maintainability·medium, maintainability·low, performance·medium)_
- [ ] `src/client/hooks/use-job-detail.ts:86-87` — [maintainability · low] `handleRetry`/`handleRerun` both navigate to `/jobs/${response.job.id}`. The id is duplicated as both a route param string and a business identifier — centralizing the route helper (e.g., `routes.job…
- [ ] `src/client/hooks/use-job-detail.ts:137-142` — [maintainability · low] All four action handlers share the same try/catch/finally and `e instanceof Error ? e.message : '...'` pattern. Extract a small helper like `formatError(e, fallback)` to remove duplication and ensure…

### `src/client/hooks/use-polling.ts`
- [ ] `src/client/hooks/use-polling.ts:3-3, 4-8` — [maintainability · medium] Avoid `any` for deps: `deps: any[]` defeats dependency tracking and violates the project's no-`any` rule. Spreading unconstrained deps into a useEffect dependency array is also a footgun. Prefer a ge…  _(+2 related in cluster: maintainability·medium, maintainability·low)_
- [ ] `src/client/hooks/use-polling.ts:21-21` — [performance · medium] Risky interaction between `delay` and spread `...deps`: spreading `...deps` together with `delay` into the useEffect dependency array means any identity-unstable dependency (object/array literal, inl…

### `src/client/lib/api.ts`
- [ ] `src/client/lib/api.ts:80-103` — [maintainability · low] This header/401/error-handling block duplicates `request()` almost verbatim. The copies can drift (for example, one may later gain CSRF or parsing behavior that the other lacks). Extract the shared f…

### `src/client/lib/ease.ts`
- [ ] `src/client/lib/ease.ts:1-1` — [maintainability · low] The constant lacks a JSDoc or inline comment explaining what easing curve this represents (e.g., "Expo out" / cubic-bezier(0.16, 1, 0.3, 1)). Since easing tuples are magic numbers to most readers, a…  _(+1 related in cluster: maintainability·low)_

### `src/client/lib/theme.tsx`
- [ ] `src/client/lib/theme.tsx:18-25, 27-42, 38-41` — [maintainability · medium] Shared mutable timer across instances: `themeTransitionPauseTimer` is module-scoped, so multiple `ThemeProvider` mounts (SSR + client hydration, tests using `render` repeatedly, micro-frontends, Reac…  _(+2 related in cluster: style·low, maintainability·medium, maintainability·low)_
- [ ] `src/client/lib/theme.tsx:57-59, 58-59` — [maintainability · high] Module-level side effect on import: `applyTheme(initial)` touches `localStorage` and `document.documentElement` at module evaluation time. This breaks SSR (no `document`/`window`), prevents testabili…  _(+1 related in cluster: maintainability·high, maintainability·low)_
- [ ] `src/client/lib/theme.tsx:69-75` — [performance · medium] Redundant theme application on toggle: `toggleTheme` already schedules the transition pause via `pauseTransitions`, then `setThemeState` runs, which causes React to re-render — but the DOM class was…

### `src/client/lib/utils.ts`
- [ ] `src/client/lib/utils.ts:8-12, 15-15` — [maintainability · low] Magic thresholds (`60_000`, `60`, `1_000_000`, `1_000`, `10_000`) are repeated across both formatters. Extracting named constants (e.g. `MS_PER_SECOND`, `MS_PER_MINUTE`, `THOUSAND`, `MILLION`) would…  _(+1 related in cluster: maintainability·low, style·low)_

### `src/client/lib/vcs.ts`
- [ ] `src/client/lib/vcs.ts:49-50` — [maintainability · low] `reviewUrl` duplicates provider resolution separately from the shared repository URL logic. This can drift from `repositoryParts` as provider-specific URL support evolves, and the `!reviewId` check a…

### `src/client/main.tsx`
- [ ] `src/client/main.tsx:7-18, 26-30, 26-54` — [maintainability · low] Lazy imports repeat the `.then(m => ({ default: m.X }))` boilerplate 12 times. Extract a small helper `lazyNamed = (loader, name) => React.lazy(() => loader().then(m => ({ default: m[name] })))` to r…  _(+2 related in cluster: maintainability·low, performance·low)_
- [ ] `src/client/main.tsx:87-114` — [maintainability · medium] Route path strings are hardcoded here and likely duplicated in navigation consumers (NavLinks, programmatic navigate calls). Extract a single source-of-truth `routes.ts` module exporting path constan…

### `src/client/pages/dashboard.tsx`
- [ ] `src/client/pages/dashboard.tsx:36-41` — [maintainability · medium] First-load error leaves UI in a confusing state: if the very first poll fails, `setError(...)` runs in catch but `setLoading(false)` runs in `finally`, so the user sees the destructive Alert on top o…
- [ ] `src/client/pages/dashboard.tsx:116-137, 127-127, 129-132` — [maintainability · medium] Redundant/overlapping conditional rendering: the three branches `loading || recentJobs.length > 0`, `!loading && recentJobs.length === 0`, and `!loading && recentJobs.length > 0` are mutually exclusi…  _(+2 related in cluster: maintainability·medium, maintainability·low)_

### `src/client/pages/job-detail.tsx`
- [ ] `src/client/pages/job-detail.tsx:12-12` — [maintainability · medium] Defaulting a missing route param to `''` hides a real routing bug (a 404 route would silently render the page with an empty id). Either treat empty id as an error/not-found state in the page, or asse…
- [ ] `src/client/pages/job-detail.tsx:30-38` — [maintainability · low] `handleRerun`/`handleStop`/`handleDelete` are bound directly from the hook without stabilization. If `JobHeader` is wrapped in `React.memo` (or should be for perf) it will re-render every render beca…

### `src/client/pages/job-logs.tsx`
- [ ] `src/client/pages/job-logs.tsx:34-36` — [maintainability · low] `STATUS_META` (lines 21-28) uses string keys without typing the record's keys explicitly, then falls back via `STATUS_META[file.fileStatus] ?? STATUS_META.pending`. With the schema confirmed to be `[…
- [ ] `src/client/pages/job-logs.tsx:140-145` — [performance · low] Four sequential `.filter()` passes over `job.files` (lines 148-151) walk the array four times just to compute counts. For typical PRs this is small, but it's also easy to fix and makes it impossible…
- [ ] `src/client/pages/job-logs.tsx:175-180` — [maintainability · medium] The counts summary intentionally omits a "pending" bucket, but `STATUS_META` (lines 21-28) does include `pending`. If `fileStatus === 'pending'` ever appears in `job.files` (e.g., for jobs that haven…

### `src/client/pages/jobs.tsx`
- [ ] `src/client/pages/jobs.tsx:10-10` — [maintainability · low] Unused imports: `RotateCcw`, `Trash2`, `AlertTriangle`, `Info`, and `Search` are imported from `lucide-react` but never referenced in the JSX. They add bundle weight and signal dead code; remove them…
- [ ] `src/client/pages/jobs.tsx:49-49` — [performance · medium] `usePolling(load, 15_000, [filters, itemsPerPage])` recreates `load` on every filter/pagination change and immediately re-subscribes the polling timer. Combined with the un-debounced search input, th…
- [ ] `src/client/pages/jobs.tsx:82-89, 94-109` — [performance · high] Search input is not debounced: every keystroke updates `filters.search`, which changes `load`'s identity and re-triggers the 15s polling timer plus an immediate fetch. This causes excessive requests,…  _(+1 related in cluster: performance·high, maintainability·low)_
- [ ] `src/client/pages/jobs.tsx:147-150` — [maintainability · medium] Hardcoded external URL violates the "no business hardcoding" rule: `https://github.com/michnicki/opencodra#readme` is embedded directly in component source. Move it to a shared config/constants modul…

### `src/client/pages/landing.tsx`
- [ ] `src/client/pages/landing.tsx:25-42` — [maintainability · medium] Hardcoded GitHub repository URL is duplicated in three places (header GitHub icon, "Read the docs" CTA, footer link). Extract to a module-level constant (e.g. `const REPO_URL = 'https://github.com/mi…
- [ ] `src/client/pages/landing.tsx:52-65, 68-74` — [maintainability · low] Duplicated styling: the `h-8 w-8 items-center justify-center rounded-lg border border-border bg-card text-foreground shadow-sm transition-colors hover:bg-secondary dark:...` Tailwind class string is…  _(+1 related in cluster: maintainability·low)_

### `src/client/pages/login.tsx`
- [ ] `src/client/pages/login.tsx:35-35, 37-42` — [maintainability · low] Redundant `useMemo` around `getErrorMessage`. `getErrorMessage` is a pure, cheap function and `searchParams` reference identity rarely changes in practice. The `useMemo` adds no measurable benefit an…  _(+1 related in cluster: maintainability·low, documentation·low)_
- [ ] `src/client/pages/login.tsx:53-53` — [performance · low] Effect dependency on `navigate`. In react-router-dom v6 the `navigate` function returned by `useNavigate` is a stable reference, so listing it in the dependency array is harmless. However, including…
- [ ] `src/client/pages/login.tsx:111-113, 111-114` — [maintainability · medium] Hardcoded OAuth entry paths. Per the project's "hardcoding" rule, business-related URL paths should not be inlined in JSX. `/auth/github` and `/auth/bitbucket` should live in a shared constants modul…  _(+1 related in cluster: maintainability·medium, maintainability·low)_

### `src/client/pages/not-found.tsx`
- [ ] `src/client/pages/not-found.tsx:37-37` — [maintainability · low] The home URL is embedded directly in the page component, so changing the application's home route requires finding and updating this literal manually and can diverge from the router configuration. Us…

### `src/client/pages/repos.tsx`
- [ ] `src/client/pages/repos.tsx:66-68` — [maintainability · low] `getGlobalRoute` is a thin wrapper around `normalizeRoute` with no added behavior — it's only used in two places (modal init/reset and the row's `getRepoRoute`). Inlining it (or removing it) would si…
- [ ] `src/client/pages/repos.tsx:385-385` — [maintainability · medium] `globalConfig` state in `ReposPage` is typed as `ModelRouteConfig` (already-normalized), but the helper functions accept `GlobalModelConfig | ModelRouteConfig | null` and re-normalize. The two layers…
- [ ] `src/client/pages/repos.tsx:538-544` — [maintainability · medium] Hardcoded business URL `/api/repos/install` is embedded directly in UI code. This couples the client to a server route path and prevents per-environment configuration. Derive from a runtime config/en…

### `src/client/pages/repos/add-bitbucket.tsx`
- [ ] `src/client/pages/repos/add-bitbucket.tsx:33-34` — [maintainability · low] Error state is never reset on successful submission or when the user starts editing. If a previous submit fails and the user fixes the input and resubmits successfully, the stale `error` state would…
- [ ] `src/client/pages/repos/add-bitbucket.tsx:80-85` — [maintainability · low] The component has no `id`/`htmlFor` attributes wiring labels to inputs, which harms accessibility (screen readers and click-to-focus on the label). Although React passes `children` to the surrounding…

### `src/client/pages/stats.tsx`
- [ ] `src/client/pages/stats.tsx:13-19` — [maintainability · low] Unused imports `TimeRangeSelect` (from `'@client/components/features/stats/time-range-select'`) and `Button` (from `'@client/components/ui/button'`) are never referenced anywhere in this file. Remove…
- [ ] `src/client/pages/stats.tsx:58-70` — [maintainability · medium] `ChartTooltip` is typed as `any` for both props (`{ active, payload, label }: any`) and per-item (`item: any`), violating the project's "avoid `any`" rule and discarding Recharts' typed `TooltipProps…
- [ ] `src/client/pages/stats.tsx:86-88` — [maintainability · low] `GraphSectionTitle` is a function component whose entire body is `<div className="pt-2" aria-hidden="true" />` — a decorative spacer with no semantic role. `MetricsGridSkeleton` recreates the same di…
- [ ] `src/client/pages/stats.tsx:110-113` — [maintainability · low] `inputColor` is an alias for `commentsColor` (they're assigned the same light/dark pair). The input-tokens bar is rendered in the same blue as the comments line, which is at best confusing visually a…
- [ ] `src/client/pages/stats.tsx:342-349` — [style · low] `Text` typo check: no obvious misspellings in identifiers or user-facing strings. Strings such as "Daily review and comment activity for the selected range." and "Failed to load stats." are well-form…

### `src/client/pages/vcs-credentials.tsx`
- [ ] `src/client/pages/vcs-credentials.tsx:254-257` — [maintainability · medium] Hardcoded 'bitbucket' provider string: the handler hardcodes `vcsProvider: 'bitbucket'` even though `VcsCredentialStatus` carries a `vcsProvider` per record and the page lists it. When a second provi…
- [ ] `src/client/pages/vcs-credentials.tsx:274-275` — [maintainability · low] Duplicate toast pattern: the load/save/delete paths each repeat the same try/catch/toast pattern with similar wording. Consider extracting a small `runWithToast` helper that wraps an async fn with a…
- [ ] `src/client/pages/vcs-credentials.tsx:323-333` — [maintainability · low] Page title is generic: `PageHeader title="Credentials"` does not mention Bitbucket, yet the empty state says "No Bitbucket credentials yet" and the add form says "Store a Bitbucket credential". Make…

### `src/server/app.ts`
- [ ] `src/server/app.ts:26-56, 48-56` — [maintainability · medium] Per project rules, business URL paths should not be hardcoded inline as string literals — they are duplicated here and across the routers, which causes drift risk on rename/refactor (a typo silently…  _(+1 related in cluster: maintainability·medium, maintainability·low)_

### `src/server/bitbucket/constants.ts`
- [ ] `src/server/bitbucket/constants.ts:5-7, 11-11, 15-15` — [maintainability · medium] REPORT_TYPE_VALUES, REPORT_RESULT, LINE_TYPES, and BUILD_STATUS_STATE are declared with `as const`, but their inferred type is a readonly tuple like `readonly ['BUG']`. Consumers iterating these arra…  _(+2 related in cluster: maintainability·medium)_

### `src/server/core/bitbucket.ts`
- [ ] `src/server/core/bitbucket.ts:72-74` — [performance · medium] No jitter on exponential backoff. `Math.pow(2, attempt) * 1000` produces deterministic delays (1s, 2s, 4s). Under a 5xx storm or a `Retry-After`-less 429 burst, many concurrent Workers will retry in…
- [ ] `src/server/core/bitbucket.ts:127-129, 133-136` — [performance · medium] `incrementSubrequests(1)` is invoked inside `withRetry`'s operation closure, so each retry attempt increments the subrequest counter again. Callers that budget subrequests (Cloudflare Workers free ti…  _(+1 related in cluster: performance·medium, maintainability·low)_
- [ ] `src/server/core/bitbucket.ts:158-240` — [maintainability · low] Several public methods (`getPullRequest`, `listPullRequestComments`, `postPullRequestComment`, `approvePullRequest`, `upsertCodeInsightsReport`, `postCommitBuildStatus`, `getPullRequestDiff`) are mis…

### `src/server/core/config.ts`
- [ ] `src/server/core/config.ts:11-12, 14-16, 23-23` — [maintainability · low] Duplicate revision-prefix construction: `REPO_CONFIG_CACHE_PREFIX` and `REPO_CONFIG_REVISION_KEY` both embed the `config:${REPO_CONFIG_CACHE_VERSION}:` template literal but are constructed independen…  _(+2 related in cluster: maintainability·low)_
- [ ] `src/server/core/config.ts:69-76` — [performance · medium] Cache-miss path performs two sequential KV reads (`getRepoConfigCacheRevision` and `getRepoConfigRecord`) plus potentially two more (`getGlobalConfig` which itself reads KV). For frequently accessed…

### `src/server/core/crypto.ts`
- [ ] `src/server/core/crypto.ts:54-57, 59-60, 59-64` — [maintainability · low] decryptSecret does not declare the return type or reject empty inputs explicitly: an empty string or whitespace-only `encrypted` will pass the split (yielding `['']`), fail the version check, and thr…  _(+2 related in cluster: maintainability·low, performance·low)_

### `src/server/core/diff.ts`
- [ ] `src/server/core/diff.ts:41-43` — [performance · medium] `customMatchers` is rebuilt by `.map(pattern => picomatch(pattern, ...))` on every invocation of `parseUnifiedDiff` and `filterReviewableFiles`. For large `skip_files` lists this is wasted work and b…
- [ ] `src/server/core/diff.ts:214-217` — [maintainability · low] Both `truncateFileDiff` and `chunkFileDiff` produce a `FileDiff` with `isTruncated: true` and `originalLineCount: file.lineCount` literally inlined. There is also significant structural overlap (`for…
- [ ] `src/server/core/diff.ts:254-255` — [performance · low] `findClosestValidLine` recomputes `getValidNewLines(file)` (which builds a fresh Set from all hunks) on every call. If this is invoked inside an inner loop (e.g. once per comment from the LLM), the s…
- [ ] `src/server/core/diff.ts:270-270, 270-273, 278-279` — [maintainability · medium] The `20` in `if (minDiff > 20) return undefined;` is a magic number buried in business logic. Extract it as a named module-level constant (e.g. `MAX_LINE_PROXIMITY_FOR_COMMENT = 20`) so it is discove…  _(+2 related in cluster: maintainability·low, maintainability·medium, performance·low)_
- [ ] `src/server/core/diff.ts:342-348` — [maintainability · low] `chunkFileDiff` references `originalLineCount` against the *original* file's `lineCount`, but every emitted chunk is marked `isTruncated: true` with the same original value. Only the *last* chunk con…
- [ ] `src/server/core/diff.ts:382-383` — [maintainability · low] The trailing blank line after the closing `}` of `chunkFileDiff` is fine, but the file ends without a final newline consistently — verify the editor's "insert final newline" setting matches project c…

### `src/server/core/github-oauth.ts`
- [ ] `src/server/core/github-oauth.ts:14-14` — [maintainability · low] `User-Agent: 'codra-app'` is hardcoded. If the product/brand is renamed or this code is reused by another service, the UA will silently drift from product naming. Consider deriving it from a constant…
- [ ] `src/server/core/github-oauth.ts:38-38` — [maintainability · low] The error messages thrown from both functions only embed the HTTP status string. Consider including the response body (or at least a sanitized error code) so operators can diagnose 4xx failures (e.g.…
- [ ] `src/server/core/github-oauth.ts:48-48` — [maintainability · low] Token-exchange error messages expose GitHub's `error_description` directly to end users. GitHub's descriptions can include technical/internal detail; consider mapping to a stable set of user-facing m…

### `src/server/core/github.ts`
- [ ] `src/server/core/github.ts:345-346` — [performance · medium] `listRepositories` can make up to 100 sequential API requests, and `withRetry` can add exponential delays for each page. This can exceed the 30-second per-call timeout expectations and Workers execut…

### `src/server/core/http.ts`
- [ ] `src/server/core/http.ts:1-3, 6-7` — [maintainability · medium] Missing content negotiation for HTML callers: `jsonError` unconditionally returns `application/json`, but `wantsHtml` exists in the same module precisely to detect browser requests that should receiv…  _(+1 related in cluster: maintainability·medium, style·low)_

### `src/server/core/job-recovery.ts`
- [ ] `src/server/core/job-recovery.ts:29-31` — [maintainability · low] Duplicated `Error -> unknown` coercion pattern. The pattern `err instanceof Error ? err : new Error(String(err))` is repeated four times (lines 30, 100, 114, and effectively all logger call sites). E…
- [ ] `src/server/core/job-recovery.ts:40-44` — [maintainability · low] The narrative `REV-M-X` comment blocks (lines 40–57, 86–90) explain historical migrations but bloat the hot path. They are useful in the PR that introduced them, but in the source tree they obscure t…
- [ ] `src/server/core/job-recovery.ts:110-126` — [performance · medium] Sequential subrequest use inside `waitUntil` has no deadline and can blow the Worker CPU-time budget. `scheduleBestEffortJobMaintenance` passes the whole maintenance task to `waitUntil` with no `Abor…

### `src/server/core/llm-crypto.ts`
- [ ] `src/server/core/llm-crypto.ts:1-2, 1-14, 4-7, 8-14` — [maintainability · high] Wrappers are missing the `async` modifier on their declaration. The underlying `encryptSecret`/`decryptSecret` are `async` and therefore always return a `Promise`. Callers in `routes/api/models.ts` c…  _(+4 related in cluster: maintainability·low, maintainability·medium, maintainability·high)_

### `src/server/core/logger.ts`
- [ ] `src/server/core/logger.ts:18-18` — [maintainability · medium] The public API and redaction implementation use `any` throughout, which removes type checking for log payloads and context and permits unsupported/cyclic values to reach serialization. Introduce an e…

### `src/server/core/model-output.ts`
- [ ] `src/server/core/model-output.ts:19-19` — [maintainability · low] `extractJson` is a 100+ line function with multiple concerns (code-block extraction, brace scanning, truncated recovery) and no unit-test-friendly seams. The 'Truncated JSON' branch and the scoring-b…
- [ ] `src/server/core/model-output.ts:55-63` — [performance · low] The brace-matching loop reuses the same string-indexed scan as preprocessJson but is O(n^2) only via raw.slice. The bigger perf issue is the 'Outer Brace' fallback that calls `raw.slice(idx, idx + 20…

### `src/server/core/oauth.ts`
- [ ] `src/server/core/oauth.ts:41-43` — [performance · low] JSON branch reads the same array twice via separate reads of record.github / record.bitbucket and re-walks the trimmed value; trivial here, but if a future per-provider validator is added this should…
- [ ] `src/server/core/oauth.ts:73-81` — [maintainability · medium] Missing error handling around async KV calls: createOAuthState and consumeOAuthState have no try/catch, so a KV outage/timeout propagates as an unhandled rejection in the OAuth callback path, produci…

### `src/server/core/review.ts`
- [ ] `src/server/core/review.ts:323-336` — [maintainability · low] `extractReviewRequest` returns `commitSha: ''` and `baseSha: ''` for `issue_comment` (mention) triggers, then `resolveQueuedJob` overwrites both by calling `commentVcs.getPullRequest(...)`. If `comme…
- [ ] `src/server/core/review.ts:421-423` — [maintainability · low] Variable shadowing with confusing name reuse: the catch-block redeclares `messageText` for an error message string, shadowing the outer `message` (the queue message) on the prior line. This makes rea…
- [ ] `src/server/core/review.ts:803-804` — [maintainability · low] `parentReviews` filter on line 804 includes only rows with `review.file_status === 'done'`, but if the parent job had files that were 'pending' (async batch in flight) at the moment the new job was c…
- [ ] `src/server/core/review.ts:947-953` — [maintainability · low] The chunk's for-loop bounds (`reviewChunkFileLimit`) and wall-clock limit (`REVIEW_CHUNK_WALL_CLOCK_MS`) are checked at the END of each iteration AFTER pushing the task to `reviewTasks`. This means a…
- [ ] `src/server/core/review.ts:1182-1191` — [maintainability · low] `Object.defineProperty(error, 'retryAfterSeconds', ...)` monkey-patches an in-flight error to carry a duration. This works in V8 but is fragile (errors can be cloned across worker boundaries, frozen,…
- [ ] `src/server/core/review.ts:1272-1283` — [performance · low] `runFinalizePhase` makes a fresh `getDiffFiles` call that hits the 6h cache (good) but ALSO calls `vcs.getPullRequest` near the top AND `vcs.updateStatusCheck` for cosmetics. Three synchronous VCS ca…
- [ ] `src/server/core/review.ts:1292-1296` — [test · low] Comment claims `bulkMarkFilesFailed` does ONE INSERT (for subrequest efficiency); verify the function and test that confirm. The behavior is critical for finalize-to-complete on a partial-PR degrade…
- [ ] `src/server/core/review.ts:1337-1338` — [maintainability · low] Hardcoded `severityRanks` and `min_severity` keys (`P0..nit`) spread across this file -- the same constants likely live in `@shared/schema` or the formatter. If a future severity (e.g., `info`, `crit…

### `src/server/core/timeout.ts`
- [ ] `src/server/core/timeout.ts:1-7` — [documentation · medium] The docstring claims the wrapper "Works with both fetch (via the signal option) and arbitrary promises", but the current implementation does NOT reliably support arbitrary promises — if `fn` ignores…
- [ ] `src/server/core/timeout.ts:21-24` — [maintainability · low] Avoid `any` for the caught error. The project rule discourages `any`; narrowing to `unknown` and using a type guard preserves type safety while still allowing `err.name` access.

### `src/server/core/token-tracker.ts`
- [ ] `src/server/core/token-tracker.ts:16-16` — [maintainability · low] MAX_SUBREQUESTS = 50 is duplicated knowledge. The comment on `remainingSafeBudget()` hard-codes "50 subrequests/invocation" and "Workers Free plan" in prose, but the constant `MAX_SUBREQUESTS = 50` a…
- [ ] `src/server/core/token-tracker.ts:30-36` — [maintainability · medium] isNearLimit() and hasRemainingSubrequests(needed) are easy to confuse. isNearLimit() returns a fixed boolean against `MAX_SUBREQUESTS - SAFE_MARGIN`, while hasRemainingSubrequests(needed) is a budget…
- [ ] `src/server/core/token-tracker.ts:63-68` — [maintainability · low] Logger reads stale `existing` totals even though they were copied into the new map entry. After `this.usage.set(model, ...)`, the new aggregate is authoritative. The log line currently uses `existing…

### `src/server/core/verify.ts`
- [ ] `src/server/core/verify.ts:7-7` — [maintainability · low] The exported union type `WebhookSignatureHeaderName` only has one member that the codebase actually invokes (`x-hub-signature-256`, via `verifyGitHubWebhookSignature`). Bitbucket is referenced in the…
- [ ] `src/server/core/verify.ts:33-36` — [maintainability · medium] `signatureHeaderName` parameter is accepted and explicitly voided (`void input.signatureHeaderName`). It has no runtime effect and the comment about "preserving call shape / preventing callers from c…

### `src/server/core/webhook-ingest.ts`
- [ ] `src/server/core/webhook-ingest.ts:6-19` — [documentation · low] The large block-comment header (lines 1-14) narrates the history of the extraction ("05-04 widening", "NREG-02 byte-identical", "Phase-3 deferred item closure"). This is commit/PR commentary, not API…
- [ ] `src/server/core/webhook-ingest.ts:41-47` — [documentation · low] The inline comment on the `effectiveProvider` line (lines 38-44) repeats the migration narrative from the file header. Same issue: it describes history rather than current behavior. A short note abou…
- [ ] `src/server/core/webhook-ingest.ts:106-116, 123-133` — [maintainability · medium] Duplicate provider-attach logic at the two `send` sites (job message and event message). The same `if (input.provider !== undefined) ... else if (effectiveProvider !== 'github') ...` ladder is repeat…  _(+1 related in cluster: maintainability·medium)_

### `src/server/db/app-settings.ts`
- [ ] `src/server/db/app-settings.ts:0-0` — [style · low] Project rules prohibit loose equality (`==`/`!=`); use strict equality (`===`/`!==`). The `== null` idiom here is intentionally matching both `null` and `undefined`, but it should be expressed as `co…  _(+1 related in cluster: style·low, maintainability·low)_

### `src/server/db/client.ts`
- [ ] `src/server/db/client.ts:23-23` — [maintainability · medium] These `any[]` casts bypass the postgres.js parameter type contract in both regular and transactional queries, so unsupported values can reach the driver without a compile-time error. Give `normalizeP…

### `src/server/db/file-reviews.ts`
- [ ] `src/server/db/file-reviews.ts:379-384` — [maintainability · medium] Grouping only by `model_used` while selecting `MIN(model_provider)` merges usage from different providers that expose the same model name and reports an arbitrary provider alongside combined calls/to…

### `src/server/db/jobs.ts`
- [ ] `src/server/db/jobs.ts:203-216` — [maintainability · medium] `markSystemActive` and `clearSystemActive` silently swallow all KV errors. There is no log, no metric — if Workers KV has an extended outage, the only signal that the system has fallen back to the 20…
- [ ] `src/server/db/jobs.ts:285-297, 299-323` — [maintainability · medium] `insertJob` `installationId ?? ''` is silent coercion to an empty string for the GitHub path. If a future caller supplies `installationId: null` *without* `repositoryId` (e.g. a refactor that forgets…  _(+1 related in cluster: maintainability·medium, maintainability·low)_
- [ ] `src/server/db/jobs.ts:448-471` — [performance · low] `getJobDetail` does a N+1-shaped aggregate via two correlated subqueries (`file_reviews` then `review_comments` per row). For a 50-file PR, that's 1 + 50 subquery aggregations in a single round-trip…
- [ ] `src/server/db/jobs.ts:1253-1253` — [style · low] `getOtherRunningJobsCount` uses an inline `import('@server/env').AppBindings` type instead of the module-level `AppBindings` already imported at the top of the file. Reuse the existing import for con…

### `src/server/db/model-configs.ts`
- [ ] `src/server/db/model-configs.ts:55-60` — [maintainability · low] `mapProviderSecret` spreads `mapProvider(row)` which already runs `llmProviderSchema.parse` and builds `hasApiKey` from `encrypted_api_key`. Spreading is fine, but it ties the secret shape to the pub…
- [ ] `src/server/db/model-configs.ts:85-93, 85-103` — [maintainability · low] The provider SELECT column list is duplicated across `listLlmProviders`, `listLlmProviderSecrets`, `getLlmProvider`, `createLlmProvider`, `findLlmProviderByName`, and `updateLlmProvider`. Extract a s…  _(+1 related in cluster: maintainability·low, performance·low)_
- [ ] `src/server/db/model-configs.ts:160-165` — [maintainability · low] `updateLlmProvider` builds SQL by string concatenation (`apiKeySql`) which is safe here because the appended fragment is a hard-coded `, encrypted_api_key = $N`, but the pattern is fragile — any futu…
- [ ] `src/server/db/model-configs.ts:297-303` — [maintainability · low] `slugify` rejects valid model names containing only special characters by falling back to the literal string `'provider'`. If two different providers both fall back to `'provider'`, they will collide…
- [ ] `src/server/db/model-configs.ts:318-329` — [performance · low] `upsertDiscoveredModelConfigs` issues two separate SELECTs (`existingForProvider`, `existingModelIds`) before the INSERT. These can be combined into a single query (e.g., one SELECT that returns both…

### `src/server/db/repo-configs.ts`
- [ ] `src/server/db/repo-configs.ts:6-19` — [maintainability · n/a] TypeScript `any` usage violates the no-any rule. `size_overrides: any | string | null` loses type safety. Replace with the proper type inferred from the shared schema (e.g., `RepoConfig['model']['siz…
- [ ] `src/server/db/repo-configs.ts:143-150, 151-163, 167-194, 199-229` — [maintainability · n/a] Error handling: all exported functions lack try/catch and rely on queryRows to throw. While the helper may centralize errors, callers in routes may not receive user-friendly messages. Consider wrappi…  _(+3 related in cluster: maintainability·n/a, performance·n/a)_

### `src/server/db/stats.ts`
- [ ] `src/server/db/stats.ts:12-14` — [documentation · low] The `parsedDays` coercion has a subtle gap. `Math.trunc` of a non-finite number returns `NaN`, which `Number.isFinite` rejects via the `safeDays` ternary, so `clampedDays` defaults to `30`. That's co…
- [ ] `src/server/db/stats.ts:48-56` — [performance · low] The `verdicts` query aggregates over the entire `jobs` table without a date filter while every other bucketed query uses the date window. For a large `jobs` table the full-count GROUP BY on `verdict`…
- [ ] `src/server/db/stats.ts:144-150, 157-160, 160-160, 160-163` — [maintainability · medium] Silent dropping of enum-typed rows masks schema-migration bugs and data corruption. The current comment acknowledges the intent ("legacy rows back-migrated from JSON"), but in a healthy schema these…  _(+3 related in cluster: performance·low, maintainability·medium, maintainability·low)_

### `src/server/db/vcs-credentials.ts`
- [ ] `src/server/db/vcs-credentials.ts:0-0` — [style · low] passesConfidenceFloor uses non-strict == (== null) for the confidence null/undefined check. Project rules require strict equality. Although == null is an idiomatic null-or-undefined check, the projec…  _(+3 related in cluster: style·low, maintainability·low, performance·low)_

### `src/server/db/webhook-deliveries.ts`
- [ ] `src/server/db/webhook-deliveries.ts:4-5, 11-16` — [maintainability · low] `Pick<AppBindings, 'HYPERDRIVE'>` is duplicated in both exported functions. `src/server/db/client.ts` already declares `type DbEnv = Pick<AppBindings, 'HYPERDRIVE'>` — import and reuse it (or alias i…  _(+1 related in cluster: maintainability·low)_

### `src/server/env.ts`
- [ ] `src/server/env.ts:23-24` — [maintainability · low] Interface name shadows the imported type: `DashboardSessionUser` is declared as `export type DashboardSessionUser = AuthSessionUser`. This is fine as a re-export alias, but every internal use site sh…
- [ ] `src/server/env.ts:34-36` — [maintainability · medium] Inconsistent optionality on GitHub App identifiers: `GITHUB_APP_SLUG` is `?` while every other GitHub binding (`GITHUB_APP_ID`, `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `GITHUB_APP_WEBHOOK_SECRET`…
- [ ] `src/server/env.ts:52-56` — [documentation · low] `sessionToken` and `sessionUser` are typed as nullable (`string | null` / `DashboardSessionUser | null`) but the middleware/route code that populates them is not visible here. Callers must defensivel…

### `src/server/middleware/auth.ts`
- [ ] `src/server/middleware/auth.ts:8-14` — [maintainability · low] Missing semicolon / inconsistent style: `return Response.json(...)` is fine but the function uses two different response shapes (a 302 redirect vs a JSON 401). For an API-only middleware, returning J…

### `src/server/middleware/observability.ts`
- [ ] `src/server/middleware/observability.ts:6-6, 9-24, 16-18` — [maintainability · low] Empty/duplicate request IDs slip through: `c.req.header('x-request-id')` returns `undefined` when missing, but it can also legitimately return an empty string (e.g. `x-request-id:`), in which case th…  _(+3 related in cluster: maintainability·low, performance·low)_

### `src/server/models/anthropic.ts`
- [ ] `src/server/models/anthropic.ts:5-7, 9-15` — [maintainability · medium] Hardcoded API version and output cap with no config knob. `anthropic-version: '2023-06-01'` and `ANTHROPIC_MAX_OUTPUT_TOKENS = 4096` are baked into source; older or newer API revisions, or models wit…  _(+1 related in cluster: maintainability·medium, maintainability·low)_

### `src/server/models/catalog.ts`
- [ ] `src/server/models/catalog.ts:99-109` — [performance · low] `extractOpenAiModels` and `extractAnthropicModels` are byte-for-byte identical; the response shapes are the same. Collapse them into a single `extractIdListModels` helper to avoid drift.
- [ ] `src/server/models/catalog.ts:130-143` — [maintainability · low] Duplicate logic across the four providers (check apiKey → fetch with auth → throw on !ok → parse JSON → extract). Extract a helper `fetchProviderList(name, baseUrl, headers, format)` that performs th…
- [ ] `src/server/models/catalog.ts:215-215` — [maintainability · medium] Use of `any` violates the project's any-avoidance rule. The `CloudflareModelItem` interface is already defined for exactly this purpose — type the map callback with it.

### `src/server/models/cloudflare.ts`
- [ ] `src/server/models/cloudflare.ts:7-12` — [style · low] Comment contains a non-ASCII em-dash (`—`) and the JSDoc-style comment refers to a feature ("headroom for retries") that doesn't exist (retries are 0). Trim the speculative language to reflect actual…
- [ ] `src/server/models/cloudflare.ts:25-30` — [maintainability · low] Schema mismatch: REVIEW_RESPONSE_SCHEMA requires every `findings[]` entry to include `code_location` (among other fields), but the per-item `properties` block omits `code_location` from the listed pr…
- [ ] `src/server/models/cloudflare.ts:246-250` — [maintainability · medium] Heavy `as any` casts strip type safety at the AI binding boundary. While the binding's surface is genuinely dynamic, document *why* each cast is necessary (e.g. `model` is a runtime-config string; pa…
- [ ] `src/server/models/cloudflare.ts:303-303` — [maintainability · medium] Stale local variable `maxRetries` adds no value when the constant is `0`. Either delete the entire retry scaffolding (`for` loop, attempt counter, delay calculation, lastError, continue branches) and…
- [ ] `src/server/models/cloudflare.ts:315-320` — [performance · medium] On timeout, `controller.abort()` cancels the subrequest, but the `runPromise.catch(() => {})` (placed after `.run`) leaves the underlying timer/connection untriaged. Once abort lands the fetch should…

### `src/server/models/google.ts`
- [ ] `src/server/models/google.ts:93-98, 99-99, 101-104` — [maintainability · medium] Missing normalization/validation when `config.baseUrl` is provided: The code validates `config.baseUrl` only when truthy, then uses `config.baseUrl || DEFAULT_GEMINI_BASE_URL`. If `config.baseUrl` is…  _(+2 related in cluster: maintainability·medium, performance·low)_
- [ ] `src/server/models/google.ts:173-173` — [maintainability · low] Provider name fallback repeated multiple times: `config.providerName ?? 'Google'` appears 3 times. Minor duplication; consider destructuring `const providerName = config.providerName ?? 'Google';` on…

### `src/server/models/limits.ts`
- [ ] `src/server/models/limits.ts:12-13` — [maintainability · low] Spelling/grammar in doc comments. "Small diffs fail over fast" in `adaptiveModelTimeoutMs`'s doc is fine, but the module-level comment block uses "queued-then-timed-out call is a wasted subrequest" —…
- [ ] `src/server/models/limits.ts:48-52, 54-60` — [documentation · medium] Doc claim `MAX_CONCURRENT_MODEL_CALLS = 3` keeps free slots for KV/Hyperdrive/GitHub is not enforced. The comment asserts "Kept below the runtime's 6-connection cap so short-lived KV/Hyperdrive/GitHu…  _(+1 related in cluster: maintainability·low, documentation·medium)_
- [ ] `src/server/models/limits.ts:89-98` — [performance · low] O(n) `Array.shift()` on every release under contention. `waiters.shift()` is FIFO-correct but O(n) and called once per completed model call. With `MAX_CONCURRENT_MODEL_CALLS = 3` and a long waiter qu…

### `src/server/models/openai.ts`
- [ ] `src/server/models/openai.ts:0-0` — [maintainability · medium] Continuation of the previous issue: the entire `fetch` + status-check + `response.json()` flow should be wrapped so that the subrequest counter is only incremented after a successful (or at least ini…
- [ ] `src/server/models/openai.ts:92-96` — [maintainability · low] System/user prompt suffix strings are duplicated logic and are not configurable. If multiple providers require different output-format instructions, embedding them here couples concerns. Extract to a…
- [ ] `src/server/models/openai.ts:105-110, 116-122` — [performance · low] `response.text()` is read for the error path, but on success `response.json()` is read. If a non-2xx response includes a large body, `response.text()` is fine, but note that `response.json()` on the…  _(+1 related in cluster: performance·low, maintainability·low)_

### `src/server/models/types.ts`
- [ ] `src/server/models/types.ts:9-18` — [maintainability · low] Setting `this.name` after `super()` is fine in TS, but the name is lost across error serialization boundaries (e.g., when the error is re-thrown through worker/serializable-clone layers, or logged vi…
- [ ] `src/server/models/types.ts:27-32, 36-53, 55-56` — [maintainability · medium] Doc claims PERMANENT failure but the class carries no discriminator. The JSDoc explicitly states this is treated as a permanent (non-transient) failure so the retry policy won't burn quota, yet `Unpa…  _(+2 related in cluster: maintainability·medium, maintainability·low, style·low)_

### `src/server/prompts/file-review.ts`
- [ ] `src/server/prompts/file-review.ts:5-5` — [maintainability · medium] Unreplaced `{{MAX_COMMENTS}}` sentinel is silently shipped. `fileReviewSystemPromptBase` is exported as a const that still contains the literal `{{MAX_COMMENTS}}` token; only `buildFileReviewSystemPr…
- [ ] `src/server/prompts/file-review.ts:23-41, 31-34, 31-39` — [maintainability · high] Missing `absolute_file_path` in system-prompt schema. The user prompt tells the model that `code_location.absolute_file_path` is required (interpolated with `${input.file.path}`), but the system prom…  _(+2 related in cluster: maintainability·medium, maintainability·high)_
- [ ] `src/server/prompts/file-review.ts:51-56` — [documentation · low] Unused parameter. `input.prDescription` is declared on the `BuildFileReviewPromptsInput` shape but never referenced in the function body, while `input.prTitle` is used. Either include the description…

### `src/server/prompts/languages.ts`
- [ ] `src/server/prompts/languages.ts:1-6, 9-12` — [maintainability · medium] Extension overlap is implicit and order-dependent. `tsx`/`jsx` belong to both the TypeScript/JavaScript bucket and the React bucket, so every `.tsx` file always takes the multi-match path. This makes…  _(+1 related in cluster: documentation·low, maintainability·medium)_
- [ ] `src/server/prompts/languages.ts:106-106, 106-108, 110-117` — [maintainability · high] Set-based deduplication of `guidelines` loses the original ordering and can silently drop semantically different entries whose strings happen to differ only by punctuation/case, or drop a more specif…  _(+2 related in cluster: performance·low, maintainability·medium, maintainability·high)_

### `src/server/prompts/summary.ts`
- [ ] `src/server/prompts/summary.ts:14-18, 19-23` — [maintainability · high] Failure classification uses an inconsistent and brittle signal: `successFindings` excludes `'approve'` verdicts AND excludes summaries starting with `'Review failed'`, while `failures` only includes…  _(+2 related in cluster: maintainability·low, maintainability·high)_
- [ ] `src/server/prompts/summary.ts:33-35, 34-34` — [performance · medium] Unbounded prompt growth: the user prompt concatenates every file's full `summary` text with no length cap, while the system prompt mandates a final response of <=200 words. With many files or long pe…  _(+1 related in cluster: performance·medium, maintainability·low)_

### `src/server/routes/api/auth.ts`
- [ ] `src/server/routes/api/auth.ts:7-14, 10-10` — [performance · low] Unnecessary async: The handler is declared `async` but contains no `await` expressions. It can be a plain synchronous handler, which avoids an extra microtask hop per request.  _(+1 related in cluster: performance·low, maintainability·low)_

### `src/server/routes/api/jobs.ts`
- [ ] `src/server/routes/api/jobs.ts:4-10` — [maintainability · low] `AppEnv` is imported from `@server/env` (line 4) and `AppBindings` is imported from the same module (line 10) — two separate type imports from one module. Combine into a single import to reduce churn…
- [ ] `src/server/routes/api/jobs.ts:34-40, 45-49` — [maintainability · low] `getExecutionContext` wraps `c.executionCtx` in a try/catch that returns `undefined` on any error. In Hono this property access does not throw under normal conditions; the try/catch hides real bugs (…  _(+1 related in cluster: maintainability·low)_
- [ ] `src/server/routes/api/jobs.ts:78-78` — [performance · low] `Cache-Control: private, no-cache` makes the ETag/304 machinery moot at any intermediate cache and forces a conditional round-trip on every client. If the intent is "always revalidate but allow 304,"…
- [ ] `src/server/routes/api/jobs.ts:94-96` — [style · low] Error path in `startReplacementJob` uses `==` (`source.installationId == null`) which is flagged by the review checklist. Use the strict form `=== null || === undefined`, or destructure and check.

### `src/server/routes/api/models.ts`
- [ ] `src/server/routes/api/models.ts:125-125` — [performance · medium] Unbounded parallelism in `syncProviderModelCatalog`: `Promise.all` over `listLlmProviderSecrets` results will issue N concurrent outbound calls + DB upserts. For large numbers of providers this can e…
- [ ] `src/server/routes/api/models.ts:245-251` — [maintainability · low] Duplicate provider-validation block: `providerIdSchema.safeParse(id)` plus 400 message is repeated for PATCH and DELETE on `/providers/:id`. Extract a shared helper similar to the modelId helper to k…
- [ ] `src/server/routes/api/models.ts:312-321` — [maintainability · low] Duplicate validation logic: the validation block for `modelIdSchema.safeParse(c.req.param('id'))` is repeated three times (test, update, delete). Extract a small helper like `requireParsedModelId(c)`…
- [ ] `src/server/routes/api/models.ts:335-337` — [style · low] Trailing whitespace on the blank line after `const apiKey = await decryptLlmApiKey(c.env, config.encryptedApiKey);` (between lines 335 and 337). Cosmetic, but visible in diffs and lints.

### `src/server/routes/api/settings.ts`
- [ ] `src/server/routes/api/settings.ts:14-17` — [maintainability · low] The 'at least one field is required' refine at the top level masks a different problem at the inner level: reviewSettingsPatchSchema is defined as `z.object({...}).strict().refine(...)`. Putting the…
- [ ] `src/server/routes/api/settings.ts:34-36, 35-35` — [performance · medium] Two sequential awaits (getReviewSettings, updateReviewSettings) on every PATCH — they are dependent (the update needs the read result before merge), so they cannot be parallelized. However, with the…  _(+1 related in cluster: performance·medium, maintainability·low)_

### `src/server/routes/api/stats.ts`
- [ ] `src/server/routes/api/stats.ts:11-12` — [maintainability · medium] The async handler awaits `getStats` with no `try/catch` and no Hono `app.onError` middleware in this router. A DB failure (network, SQL error, etc.) becomes an unhandled promise rejection that surfac…

### `src/server/routes/api/vcs-credentials.ts`
- [ ] `src/server/routes/api/vcs-credentials.ts:19-25, 33-35` — [maintainability · medium] `clear` precedence over `secret` is load-bearing but undocumented in the function header. If a frontend bug sends `{ clearToken: true, accessToken: 'new-value' }`, the clear wins and the new token is…  _(+1 related in cluster: maintainability·medium)_
- [ ] `src/server/routes/api/vcs-credentials.ts:50-51` — [maintainability · low] `c.req.json().catch(() => null)` discards the parse error context. If the body is truncated JSON or exceeds a Hono body limit, the cause is lost and the client only sees a generic 400 "Invalid creden…
- [ ] `src/server/routes/api/vcs-credentials.ts:87-88` — [performance · low] Existence check reads full ciphertext row instead of using a cheaper EXISTS query. The route only uses the result as a boolean (`!existing`), but getVcsCredentialSecrets SELECTs the entire row (inclu…

### `src/server/routes/auth-bitbucket.ts`
- [ ] `src/server/routes/auth-bitbucket.ts:17-20` — [maintainability · low] Hard-coded scope `'account'` and host `https://bitbucket.org/site/oauth2/authorize` are fine for a single-provider file, but the project rule discourages hard-coded business strings. Either lift thes…
- [ ] `src/server/routes/auth-bitbucket.ts:45-47` — [performance · low] Two unawaited operations are sequenced when they could overlap. `exchangeBitbucketOAuthCode`, `fetchBitbucketOAuthProfile`, and `parseAllowedUsersByProvider` are each independent of each other in ter…

### `src/server/routes/auth.ts`
- [ ] `src/server/routes/auth.ts:55-57` — [maintainability · high] The catch block swallows the error with no logging, making production debugging and security incident response impossible. Capture and log the underlying cause (token exchange failure vs. profile fet…

### `src/server/routes/webhook-bitbucket.ts`
- [ ] `src/server/routes/webhook-bitbucket.ts:18-20` — [documentation · low] The 16-step flow comment refers to finding IDs (`REV-M-6`, `D-19`, `REV-M-1`, `D-04`, `REV-R-D`, etc.) but the comment block at the top is missing a single sentence linking to the source document (e.…
- [ ] `src/server/routes/webhook-bitbucket.ts:76-83` — [maintainability · medium] The handler never validates `parsedBody` against a max-size budget before parsing. A Bitbucket webhook with a deeply-nested or extremely large body (Bitbucket does not enforce a hard cap on `pullrequ…
- [ ] `src/server/routes/webhook-bitbucket.ts:93-94` — [maintainability · low] The non-null assertion on `identityParse.data` is correct (it's guarded by `success` above) but TypeScript's narrowing on `safeParse({ success: false })` exposes `data: undefined`. After `if (!identi…
- [ ] `src/server/routes/webhook-bitbucket.ts:142-143` — [maintainability · low] Documentation mentions `bytesToHex` is imported twice: once from `@server/db/jobs` for `bytesToHex` (line 15) and the comment block references "REV-M-1" multiple times. The double-spread `envelope` c…
- [ ] `src/server/routes/webhook-bitbucket.ts:212-214` — [maintainability · low] Spelling error in comment block: "`X-Request-UUID` (Bitbucket includes it)" is correct, but the comment at step 13b uses "Bitbucket" repeatedly which is fine. However, "step 13" comment says "(REV-M-…
- [ ] `src/server/routes/webhook-bitbucket.ts:232-244` — [maintainability · medium] `configSnapshot` overrides only `.model` from the global config but spreads the rest of `defaultRepoConfig`. As acknowledged in the comment, this silently ignores per-repo ignore-pattern / size-polic…

### `src/server/services/formatter.ts`
- [ ] `src/server/services/formatter.ts:17-38` — [maintainability · low] Duplicate `switch (severity)` with mirrored cases: the Bitbucket and GitHub branches in `severityIcon` both enumerate the same five severity keys. If a new severity is added to `ParsedReviewComment['…
- [ ] `src/server/services/formatter.ts:71-73, 75-77` — [maintainability · medium] `summarizeVerdict` reports `verdict: 'comment'` whenever `p2 > 0`, but P2 entries are tracked as `warnings` (not `errors`). Downstream consumers (e.g. the GitHub check/status that gates merging on 'a…  _(+1 related in cluster: performance·low, maintainability·medium)_

### `src/server/services/github.ts`
- [ ] `src/server/services/github.ts:4-9, 7-9, 11-13` — [maintainability · medium] Pass-through wrapper adds no value: every method unconditionally forwards to GitHubClient with identical signatures. Either inject GitHubClient (so it can be mocked) and delete this class, or add the…  _(+2 related in cluster: maintainability·medium, performance·medium)_
- [ ] `src/server/services/github.ts:23-25` — [maintainability · medium] Type unions for check-run status and conclusion are duplicated from core/github types. Importing them from a shared types module keeps a single source of truth and prevents drift if GitHub adds new v…

### `src/server/services/model.ts`
- [ ] `src/server/services/model.ts:72-74` — [documentation · low] The trailing comment on line 73 says "No reviewable output (reasoning-only / truncated / empty) is deterministic -- never retry it." for `UnparseableModelResponseError`. But `UnparseableModelResponse…
- [ ] `src/server/services/model.ts:190-193` — [style · low] `const thresholdBase = params.totalLineCount;` is a redundant alias -- `params.totalLineCount` is used exactly once on the next non-trivial line. Use `params.totalLineCount` directly to avoid the int…
- [ ] `src/server/services/model.ts:274-300` — [maintainability · medium] The four-branch `if/else if` chain in `callResolvedModel` silently routes any unknown `apiFormat` to Anthropic via the trailing `else`. If a new provider format is added later (or a misconfigured mod…
- [ ] `src/server/services/model.ts:342-345` — [style · low] The `as any` cast on `res` is unnecessary -- `reviewFileChunk`'s return type already matches the array element type. Drop the cast so TypeScript can verify the shape and surface drift early.
- [ ] `src/server/services/model.ts:614-635` — [maintainability · low] Lots of duplicated fallback-loop logic between `reviewFileChunk` and `generateSummary` -- both do: selectModel -> for each model: resolveModel -> skip-if-unavailable -> callResolvedModel -> mark prov…

### `src/server/services/vcs.ts`
- [ ] `src/server/services/vcs.ts:0-0` — [maintainability · medium] Type-system escape hatch defeats checks at the highest-scrutiny seam: `job` is an inline structural type that only lists a subset of fields, and the Bitbucket branch casts via `as Parameters<typeof B…
- [ ] `src/server/services/vcs.ts:63-63, 69-73` — [maintainability · low] Magic-string equality on `repositoryVcsProvider` / `provider`: the literals 'github' and 'bitbucket' are duplicated as raw strings here and presumably elsewhere. Consider exporting a `VcsProviderName…  _(+1 related in cluster: maintainability·low)_
- [ ] `src/server/services/vcs.ts:98-98` — [maintainability · low] Error message lacks actionable context: `'VcsService.forProvider requires installationId for github'` does not name the missing field/value or hint at the caller. Consider a more descriptive message…

### `src/server/vcs/bitbucket.ts`
- [ ] `src/server/vcs/bitbucket.ts:155-155` — [maintainability · medium] The hard-coded literals `'codra-review'` appear in three places (`createStatusCheck` returns it, `updateStatusCheck` posts it as build-status key, and `BITBUCKET_REVIEW_MARKER` is a separate string w…
- [ ] `src/server/vcs/bitbucket.ts:209-212` — [maintainability · low] Multiple `void owner;` discards are scattered through `submitReview`, `updateStatusCheck`, and `findExistingReviewForCommit`. The adapter explicitly ignores the `owner` parameter that the `VcsProvide…
- [ ] `src/server/vcs/bitbucket.ts:257-258` — [maintainability · low] `void input.jobIdHint;` discards a documented `VcsSubmitReviewInput` field with no explanation. Either the field is part of the contract (in which case it should be threaded into something — e.g., th…
- [ ] `src/server/vcs/bitbucket.ts:297-306` — [performance · medium] `loadCachedDiffFiles` best-effort caches via `APP_KV.put` but does not pass an `expirationTtl`, so a stale diff for a job whose head advanced is reused forever (KV entries have no inherent TTL). For…

### `src/server/vcs/github.ts`
- [ ] `src/server/vcs/github.ts:26-34` — [maintainability · medium] Focus area #1: The comment block at lines 31-32 explains the silent-drop risk but the guard is only a comment — TypeScript has no runtime safety net for missing `installationId`. If `installationId`…
- [ ] `src/server/vcs/github.ts:98-103` — [style · low] Focus area #3 (minor): `void input.jobIdHint;` is a sound way to silence noUnusedParameters while satisfying the comment's stated rationale, but TypeScript itself permits `noUnusedParameters: false`…

### `src/server/vcs/types.ts`
- [ ] `src/server/vcs/types.ts:33-36, 44-49` — [documentation · low] The contract says `createStatusCheck` always hardcodes `in_progress` (Bitbucket equivalent), but does not document whether adapters may reject or silently ignore a `status`/`conclusion` if callers tr…  _(+1 related in cluster: documentation·low, maintainability·low)_
- [ ] `src/server/vcs/types.ts:101-101, 106-110` — [maintainability · medium] `updateStatusCheck`'s `ref` parameter is typed as `string` but the contract relies on providers interpreting it differently (GitHub: numeric check_run_id string, Bitbucket: report_id). The type syste…  _(+1 related in cluster: maintainability·medium)_

### `src/server/workflows/review.ts`
- [ ] `src/server/workflows/review.ts:16-16, 19-19` — [test · medium] No tests for: (1) the `freshInstance && !nextJobId` fall-through path, (2) `retry` action causing `attempt` to grow without bound, (3) `bind-workflow-id` idempotency on replay, (4) post-maintenance s…  _(+2 related in cluster: maintainability·low, test·medium)_
- [ ] `src/server/workflows/review.ts:35-41` — [maintainability · medium] Empty `catch (e) {}` blocks silently swallow maintenance errors. Even if these are best-effort, the project's error-handling standards require user-friendly logging of failures. At minimum, log the e…
- [ ] `src/server/workflows/review.ts:63-65` — [documentation · low] Spelling: "subrequest" appears correctly throughout, but the comment "Hand the next phase to a BRAND-NEW workflow instance" is unusually shouty and the parenthetical is ~8 lines of dense prose. Consi…
- [ ] `src/server/workflows/review.ts:87-90, 90-90` — [performance · high] When the loop encounters `result.freshInstance` but `nextJobId` is falsy, the code falls through and sets `phase = result.phase` for an additional iteration on the same (budget-exhausted) instance. C…  _(+1 related in cluster: performance·high, maintainability·low)_
- [ ] `src/server/workflows/review.ts:105-111` — [maintainability · medium] Same swallowing issue in the post-maintenance block — the comment hints this is intentional "best-effort" behavior, but discarding the error without any log line violates the project's "Async functio…

### `src/shared/api.ts`
- [ ] `src/shared/api.ts:35-35` — [maintainability · medium] The `login` field on the Bitbucket variant is an intentional deviation documented as "runtime validation is unaffected … unused by any Bitbucket code path." The comment is correct that Zod won't reje…
- [ ] `src/shared/api.ts:74-77, 74-80, 80-84` — [maintainability · medium] `SyncReposResponse` has no failure/partial-failure channel: it only carries `ok: boolean` and `synced: string[]`. Callers cannot distinguish a full success from a partial failure (e.g., one provider…  _(+2 related in cluster: maintainability·medium, style·low, maintainability·low)_

### `src/shared/config.ts`
- [ ] `src/shared/config.ts:1-1` — [maintainability · medium] Untyped string literal for cache versioning: A bare string constant used as a cache version key provides no compile-time safeguard. An accidental rename, typo, or refactor of the string (e.g., from '…  _(+1 related in cluster: maintainability·medium, documentation·medium)_

### `src/shared/github.ts`
- [ ] `src/shared/github.ts:9-16, 10-10` — [maintainability · medium] The `installation` and `repository` fields are duplicated verbatim across `PullRequestWebhookPayload` and `IssueCommentWebhookPayload`. This duplication invites drift if one is updated and the other…  _(+1 related in cluster: maintainability·medium)_

### `src/shared/schema.ts`
- [ ] `src/shared/schema.ts:26-27` — [maintainability · low] Dead-by-default: `parsedReviewCommentSchema.line` is optional with no `path` semantics connecting it to `position` or `code_location.line_range`. Downstream is likely picking ONE of the three (`line`…
- [ ] `src/shared/schema.ts:111-136` — [maintainability · high] `repoConfigSchema.review.default({...})` and `repoConfigSchema.default({ review: {...} })` literally duplicate every default value. This is a real drift risk — one of them will get updated and the ot…
- [ ] `src/shared/schema.ts:198-198` — [documentation · low] Comment block at line ~120 (`REV-C-3`) and line ~145 (`R-01`) reference migration numbers / plan numbers / review findings by code. These will rot as the project evolves. Consider moving them to a `d…
- [ ] `src/shared/schema.ts:294-294` — [maintainability · medium] `sizeOverrides: z.any().nullable()` violates the project's "no any" rule. Reuse the model tier schema already declared inside `repoConfigSchema.model.size_overrides` (the `{ max_lines, model, fallbac…
- [ ] `src/shared/schema.ts:361-365` — [maintainability · low] `statsSchema` has no input bounds. A misbehaving seed or test fixture could push `avgDurationMs`/`p95DurationMs`/`avgConfidence` out of plausible range. Add `.min(0)` to durations and `.min(0).max(1)…
- [ ] `src/shared/schema.ts:400-405` — [performance · low] `normalizeRepoConfig` mutates no input but spreads + rebuilds `size_overrides` on every call. If this is invoked on the hot path (parse -> normalize -> persist), consider memoizing by `JSON.stringify…

### `test/bitbucket-fetch-mock.ts`
- [ ] `test/bitbucket-fetch-mock.ts:98-98, 103-107, 110-110` — [test · medium] The canned success responses make unconfigured routes silently appear successful, so a test can pass despite an unexpected endpoint call or an incorrect request sequence. In particular, the default p…  _(+2 related in cluster: test·low, test·medium)_

### `test/browser/render.tsx`
- [ ] `test/browser/render.tsx:6-13, 10-10` — [maintainability · medium] The `options` parameter uses an inline object type with no exported interface. Downstream callers get no IDE autocomplete, typo'd keys (e.g. `{ path: '/' }`) would be silently ignored, and there's no…  _(+1 related in cluster: maintainability·medium, maintainability·low)_

### `test/browser/setup.ts`
- [ ] `test/browser/setup.ts:1-7` — [maintainability · low] The setup only registers an `afterEach(cleanup)` hook but does not explicitly load `@testing-library/react`'s auto-cleanup. While `@testing-library/react`'s `cleanup` is exported and imported here, t…

### `test/github-fetch-mock.ts`
- [ ] `test/github-fetch-mock.ts:3-8` — [maintainability · low] RecordedGitHubCall.body is typed as any. If callers (tests) want to assert on the body they have to cast it back themselves. Tighten the type to `unknown` so it's clear that consumers must narrow bef…
- [ ] `test/github-fetch-mock.ts:38-39, 47-48` — [maintainability · low] The repoPrefix, reviewsListPath and several other strings are rebuilt inline on every fetch call (e.g. `${repoPrefix}/pulls/${fixtures.prNumber}`, `${repoPrefix}/issues/${fixtures.prNumber}/labels`).…  _(+1 related in cluster: maintainability·low, test·low)_
- [ ] `test/github-fetch-mock.ts:91-93` — [maintainability · low] The scripted 4xx branch always returns { message: 'Unprocessable Entity' }, regardless of the actual status (401, 403, 409, 500, ...). GitHubClient may inspect message/status text in its error handli…
- [ ] `test/github-fetch-mock.ts:123-124` — [test · medium] The catch-all returns a 404 with message 'Unhandled mock GitHub route: ...' and silently swallows the call. If a test accidentally exercises a new endpoint, GitHubClient treats it as a genuine 404 (w…

### `test/helpers.ts`
- [ ] `test/helpers.ts:14-21, 23-29, 31-37` — [maintainability · medium] MemoryKV.get has incorrect semantics for KVNamespace: when `type === 'text'` (or omitted/undefined), the method returns the raw string, but the signature accepts `Partial<KVNamespaceGetOptions<undefi…  _(+2 related in cluster: maintainability·medium, maintainability·low)_
- [ ] `test/helpers.ts:56-58` — [maintainability · low] MockQueue.send spreads `{ ...message, options }` which mutates the caller's object if it contains an `options` key, or silently overrides caller-provided `options` fields. Since the mock stores both…
- [ ] `test/helpers.ts:78-80` — [maintainability · low] `requiredEnv` and `usableEnvValue` are exported-implicit helpers that tolerate `'undefined'` and `'null'` strings. This pattern suggests env files set those literal strings as a "missing" marker. Doc…
- [ ] `test/helpers.ts:104-108, 110-111, 116-117` — [performance · high] The hardcoded AI.run mock always returns `{ file_verdict: 'approve', findings: [] }`. Tests that exercise the review pipeline but don't override AI (or rely on this default env) will silently pass be…  _(+2 related in cluster: performance·high, maintainability·low)_
- [ ] `test/helpers.ts:137-141` — [documentation · low] The block comment above `GOOGLE_TEST_MODEL_IDS` is helpful but lives only here — if another test file needs the same fixtures (e.g., for a non-helper test), they'll duplicate the IDs. Consider export…
- [ ] `test/helpers.ts:156-174, 180-192` — [maintainability · medium] saveTestProviderApiKey hardcodes the Google provider/model names ('gemini', 'Google', `gemma-4-31b-it`, `gemma-4-26b-a4b-it`) directly in the function. If the real provider/model names change in prod…  _(+2 related in cluster: maintainability·medium, performance·low)_
- [ ] `test/helpers.ts:254-273` — [maintainability · low] createMockPRWebhook uses `overrides: any = {}`. Replace with a typed shape (e.g. `Partial<WebhookPRPayload>`) so any field rename in the production webhook type is caught at compile time rather than…

### `test/mocks/cloudflare-workers.ts`
- [ ] `test/mocks/cloudflare-workers.ts:1-3, 4-4, 5-5` — [maintainability · medium] The `WorkflowEvent<T>` type is declared as `any`, which violates the project's 'avoid any' rule. Since this is a generic type intended to describe the shape of an event payload, it should either be `…  _(+2 related in cluster: maintainability·low, maintainability·medium)_

### `test/setup.ts`
- [ ] `test/setup.ts:5-5` — [maintainability · low] Env loading happens at module top level without deduplication or a clear failure boundary. Each TEST_ENV_FILE is read independently and the first file that defines a key wins because `process.env[key…
- [ ] `test/setup.ts:25-29, 31-41` — [maintainability · low] vi.mock('cloudflare:workers', ...) is hoisted and runs for every test file, but WorkflowEntrypoint is defined as an empty class with no return type. Any code that relies on the real signature (e.g.,…  _(+1 related in cluster: maintainability·low, documentation·low)_
- [ ] `test/setup.ts:87-88, 90-112, 114-143, 144-160` — [maintainability · high] Console overrides are never restored and silently swallow JSON-shaped logs. Replacing console.error/info/log globally means real test failures (e.g., unhandled promise rejections logged via console.e…  _(+3 related in cluster: performance·low, maintainability·low, maintainability·high, maintainability·medium)_

### `tsconfig.json`
- [ ] `tsconfig.json:7-12, 14-15, 22-22` — [maintainability · high] Path alias shadowing: The catch-all `"@/*": ["./src/client/*"]` alias shadows the more specific `"@client/*"`, `"@server/*"`, and `"@shared/*"` aliases. Since `"@/*"` is a prefix of all of them, most…  _(+3 related in cluster: maintainability·high, documentation·low, maintainability·medium, maintainability·low)_

### `vite.config.ts`
- [ ] `vite.config.ts:15-18, 22-23` — [maintainability · medium] emptyOutDir is set to mode !== 'development', which means the dist/client folder will be emptied during any non-dev mode (including 'test'/'staging' previews). If a server build also writes into dist…  _(+1 related in cluster: maintainability·low, maintainability·medium)_
- [ ] `vite.config.ts:40-42` — [performance · low] The 'remark'/'rehype'/'micromark' bucket is enormous: micromark alone pulls in a large dependency tree, and combining it with remark/rehype can produce a single huge chunk that defeats code-splitting…

### `vitest.config.ts`
- [ ] `vitest.config.ts:0-0, 7-7, 14-14, 18-18` — [maintainability · medium] Stale review note: The pre-scan focus area mentions the alias being on 'line 11-11' with a trailing comma oddity, and references a 'cloudflare:workers' replacement. This is a configuration file, not…  _(+3 related in cluster: maintainability·low, performance·low, maintainability·medium)_

### `worker-configuration.d.ts`
- [ ] `worker-configuration.d.ts:1-5, 8-8, 9-9` — [maintainability · medium] `Queue<any>` for REVIEW_QUEUE loses all payload type information. Consumers should be able to rely on a typed message shape. Define a message interface (e.g., `ReviewQueueMessage`) and use `Queue<Rev…  _(+2 related in cluster: documentation·low, maintainability·medium)_

### Other (uncategorized)

_bug·low and other/reliability/accessibility/correctness/concurrency findings — real but off the P0–P2 severity ladder._

#### `.github/ISSUE_TEMPLATE/bug_report.yml`
- [ ] `.github/ISSUE_TEMPLATE/bug_report.yml:58-65` — [bug · low] The `version` field uses `type: input` (single-line text) which is required, but does not enforce any format. Users can submit arbitrary text instead of a semver-like version. Consider adding a `rege…

#### `.github/dependabot.yml`
- [ ] `.github/dependabot.yml:3-6` — [other · low] Missing `labels` configuration: No `labels` array is set, so Dependabot PRs will not be automatically tagged. Adding labels (e.g., `dependencies`, `npm`, `github-actions`) enables filtering, auto-ass…

#### `.github/workflows/ci.yml`
- [ ] `.github/workflows/ci.yml:21-23, 25-26` — [reliability · medium] Missing job-level `timeout-minutes` on the `verify` job. A hung Playwright run, stalled PostgreSQL health check, or hung `npx playwright install --with-deps` step can run indefinitely and consume run…  _(+1 related in cluster: reliability·medium)_
- [ ] `.github/workflows/ci.yml:47-49, 52-54, 53-54` — [best_practices · low] Stale comment risk: the comment claims "Must match the mock GitHub login ('devarshishimpi') in test/api.spec.ts". When that file is refactored or moved, the workflow can silently break. Consider addi…  _(+2 related in cluster: best_practices·low, reliability·low, correctness·low)_

#### `.github/workflows/codeql.yml`
- [ ] `.github/workflows/codeql.yml:11-14, 13-17, 19-21, 24-25` — [reliability · medium] No `concurrency` group is defined. Push, pull_request, and the weekly schedule can all trigger overlapping runs against `main`, causing redundant CodeQL analyses and wasted runner minutes. Add a conc…  _(+3 related in cluster: reliability·medium, correctness·medium, correctness·low)_

#### `index.html`
- [ ] `index.html:12-15` — [accessibility · low] `lang="en"` is set but there is no `<html dir>` hint and no skip-link/landmark inside `<body>`. For an SPA the React app will render landmarks, but until it hydrates the document has no semantic stru…

#### `scripts/render-design.mjs`
- [ ] `scripts/render-design.mjs:169-173` — [bug · low] `Math.random` is reassigned inside `addInitScript`, which runs at every page navigation in the same context (currently only one navigation per context, but fragile). The PRNG state is shared via clos…

#### `scripts/setup-cloudflare.js`
- [ ] `scripts/setup-cloudflare.js:14-14` — [bug · low] `spawnAsync` appends `${command}.cmd` only on win32. If `npx` resolves to a non-`.cmd` shim (e.g., pnpm-shimmed environments on Linux, or future npm versions), this throws ENOENT without a hint. Cons…
- [ ] `scripts/setup-cloudflare.js:34-34` — [bug · low] Both `'already exists'` and `'code: 2017'` substrings are matched; `'already exists'` is also a substring of `code: 2017`-bearing messages, making the second check redundant. More importantly, the re…
- [ ] `scripts/setup-cloudflare.js:58-63, 64-64` — [bug · low] Spelling/grammar: "How would you like to handle this existing namespace?" and "How would you like to handle this existing Hyperdrive?" — fine. But elsewhere: "(Free & Easy)" mixes sentence case incon…  _(+1 related in cluster: bug·low)_
- [ ] `scripts/setup-cloudflare.js:274-274` — [bug · low] The `replace(/\\n/g, '\n')` blindly converts every `\n` literal in `.dev.vars` to a real newline. For secrets that are not multiline (URLs, base64 keys) this can corrupt the value if the user escaped…
- [ ] `scripts/setup-cloudflare.js:326-329` — [bug · low] Two sequential `handleKVNamespace('codra-review', ...)` calls happen in series even though they are independent. They could run in parallel via `Promise.all` to halve user-perceived latency. (Note: t…
- [ ] `scripts/setup-cloudflare.js:396-396` — [bug · low] `appUrl` from the workers.dev prompt is interpolated via `escapeJson` (good), but the user is told the placeholder is `https://codra.<username>.workers.dev`. If they keep the literal `<username>` pla…
- [ ] `scripts/setup-cloudflare.js:470-471` — [bug · low] `DASHBOARD_ALLOWED_USERS` is written as a string containing JSON (`{"github":["a","b"]}`). wrangler expects either a real JSON array or a plain string depending on `compatibility_flags`/vars handling…

#### `scripts/test.mjs`
- [ ] `scripts/test.mjs:66-68` — [bug · low] `process.exit(result.status ?? 1)` will exit `0` if `result.status === 0` (because of `?? 1` only applying on `null`/`undefined`, but combined with the early `if (result.status !== 0)` guard it's act…

#### `src/client/app.css`
- [ ] `src/client/app.css:267-270` — [bug · low] `@keyframes pulse-ring` and the `@utility pulsing-dot` / `@utility step-dot .running` definitions are disconnected: `pulse-ring` is declared but never applied, and `step-dot.running` only sets a stat…
- [ ] `src/client/app.css:398-402` — [bug · low] `@utility page-enter` applies `animation: fade-up …` to the element AND to its direct children, so each child re-animates on top of the parent. Because both have the same keyframes with `to` state =…
- [ ] `src/client/app.css:752-756` — [bug · low] `dashboard-sidebar-shine` initial state uses `transform: skew(-13deg) translateX(-110%); transition: transform 0ms linear;` — `transition-duration: 0ms` means the entrance to the off-screen state isn…

#### `src/client/components/features/job-detail/job-meta-cards.tsx`
- [ ] `src/client/components/features/job-detail/job-meta-cards.tsx:13-23` — [bug · low] Negative/zero elapsed duration: `elapsedSec` returns `formatDuration(end - start)` without rejecting `end < start` (clock skew) or `end === start` (zero). With the current `formatDuration`, a zero or…

#### `src/client/components/features/job-detail/job-progress.tsx`
- [ ] `src/client/components/features/job-detail/job-progress.tsx:20-25` — [bug · low] Splitting the raw path without normalizing empty segments mishandles trailing separators and repeated `/` characters. For example, `src/foo.ts/` produces `foo.ts/` as the display path and `src/` as t…

#### `src/client/components/features/job-detail/job-review-overview.tsx`
- [ ] `src/client/components/features/job-detail/job-review-overview.tsx:63-63` — [bug · low] `job.overallCorrectness` is rendered inside `{job.overallCorrectness}` inside the `<span>`. The surrounding string is interpolated as a plain text node (safe for XSS), but display casing of the verdi…
- [ ] `src/client/components/features/job-detail/job-review-overview.tsx:92-92` — [bug · low] Severity triage row collapses to a single 'nit' pill when all other severities are zero. Because the early `return null` inside the map returns `null` for each missing severity, the surrounding `<div…

#### `src/client/components/features/models/model-chain.tsx`
- [ ] `src/client/components/features/models/model-chain.tsx:250-260` — [bug · low] Duplicate placeholder logic across `addFallback` and `addTier`. Both use `models[0]?.value` without validation and could insert duplicate entries. Extract a small helper like `pickAvailableModel(exis…

#### `src/client/components/layout/page-header.tsx`
- [ ] `src/client/components/layout/page-header.tsx:34-44, 40-42, 45-49` — [bug · low] Heading semantics: the <h1> contains both the title text and a <span> version badge via flex layout. Screen readers may concatenate them oddly, and the badge's `v` prefix is concatenated directly to…  _(+2 related in cluster: bug·low)_

#### `src/client/components/motion/shared-layout-bg.tsx`
- [ ] `src/client/components/motion/shared-layout-bg.tsx:31-32` — [bug · low] Default `inset` value conflicts with the prop's JSDoc. The interface documents `Default 8`, but the destructured default is `inset = 0`. Consumers relying on the documented default of 8 will get 0 an…

#### `src/client/components/motion/smooth-scroll.tsx`
- [ ] `src/client/components/motion/smooth-scroll.tsx:222-227` — [other · low] Spelling/grammar nit on the Lenis comment: "preventDefault()s" should be "preventDefaults" or rephrased — "calls preventDefault() on every wheel event" is clearer.

#### `src/client/components/motion/stepped-slider.tsx`
- [ ] `src/client/components/motion/stepped-slider.tsx:153-159, 161-171` — [bug · low] `onPointerMove` does `setDragValue` even if the snapped value hasn't changed (e.g., pointer stays within the same step cell). Combined with the per-frame motion value downstream, this triggers re-ren…  _(+1 related in cluster: bug·low)_
- [ ] `src/client/components/motion/stepped-slider.tsx:201-211` — [bug · low] Touch-action: the track has `touch-none` in classes, but the track has `overflow-hidden` and the inner animated fill is `absolute inset-y-0 left-0 overflow-hidden` — on iOS Safari, dragging on the li…
- [ ] `src/client/components/motion/stepped-slider.tsx:294-318` — [bug · low] `steps.length > 0` rendering block uses `isFirst`/`isLast` to anchor end labels at the edges, but those labels are placed inside `absolute inset-x-2 inset-y-0` and don't compensate for the `-translat…

#### `src/client/components/motion/tabs.tsx`
- [ ] `src/client/components/motion/tabs.tsx:125-126` — [bug · low] `mix-blend-exclusion` is referenced in the inline comment but never actually applied to the trigger text. The comment is misleading: in the pill/segment branches the active label uses `text-primary-f…

#### `src/client/components/shared/bitbucket-mark.tsx`
- [ ] `src/client/components/shared/bitbucket-mark.tsx:10-10` — [accessibility · low] The component hardcodes `aria-hidden="true"`, which is correct for the current call sites (each one renders the mark next to a visible "Bitbucket" label, e.g. in `login.tsx` and `repos.tsx`). However…

#### `src/client/components/shared/empty-state.tsx`
- [ ] `src/client/components/shared/empty-state.tsx:68-77` — [bug · low] When `linkAction` is provided without `href`, the rendered button uses `linkAction.onClick`, but the type marks `onClick` as optional. A caller passing `{ label, href: undefined, onClick: undefined }…

#### `src/client/components/shared/jobs-table.tsx`
- [ ] `src/client/components/shared/jobs-table.tsx:32-39` — [bug · low] Column ordering in `DEFAULT_COLUMNS` is `['pr', 'repo', 'status', 'verdict', 'created', 'action']` (PR first, then repo), but the `COLUMN_CLASSES` and `COLUMN_HEADERS` records are keyed identically.…
- [ ] `src/client/components/shared/jobs-table.tsx:86-107` — [bug · low] `formatRelativeDate` rounds `diffSeconds / seconds` using `Math.round`, which can pick the wrong unit boundary for edge cases. For example, a date 29 days ago: absSeconds (2,505,600) >= 604,800 (week…
- [ ] `src/client/components/shared/jobs-table.tsx:267-270` — [accessibility · medium] The mobile card is a single `<Link>` wrapping the entire card content (including nested text and the arrow). The desktop row also has multiple nested `<Link>` elements (`to={`/jobs/${job.id}`}`) insi…

#### `src/client/components/shared/opencodra-logo.tsx`
- [ ] `src/client/components/shared/opencodra-logo.tsx:13-18` — [accessibility · medium] Accessibility: the inner SVG is correctly marked `aria-hidden="true"`, but the outer lockup has no accessible name. Screen readers will read "OpenCodra" without any indication it is a logo/brand mark…

#### `src/client/components/shared/skeleton.tsx`
- [ ] `src/client/components/shared/skeleton.tsx:22-27, 23-25, 33-42` — [bug · low] Style spread order silently overrides consumer-provided styles. Because `...style` is spread last, any explicit `width`, `height`, or `borderRadius` the caller passes via the `style` prop will win ov…  _(+2 related in cluster: bug·low)_

#### `src/client/components/ui/alert.tsx`
- [ ] `src/client/components/ui/alert.tsx:40-57, 45-45, 57-57` — [other · medium] Accessibility — role="alert" on mount: `role="alert"` implies an assertive aria-live region and screen readers will announce the contents as soon as the component mounts. If this Alert is used transi…  _(+2 related in cluster: bug·low, other·medium, other·low)_

#### `src/client/components/ui/badge.tsx`
- [ ] `src/client/components/ui/badge.tsx:62-75` — [bug · low] Inconsistent display casing: `'partial'` is hardcoded and already lowercase/Title-cased via `capitalize`, while every other status is rendered via `label.replace(/_/g, ' ')`. This means labels the ca…

#### `src/client/components/ui/card.tsx`
- [ ] `src/client/components/ui/card.tsx:29-34` — [bug · low] CardDescription is rendered as a <p> element, which makes it invalid HTML to nest it inside another <p> or block that already contains block-level content. Forwarding arbitrary HTMLAttributes includi…

#### `src/client/components/ui/confirm-dialog.tsx`
- [ ] `src/client/components/ui/confirm-dialog.tsx:30-30` — [bug · low] Missing focus management: Radix's `Dialog.Content` should receive focus on open. Because the Confirm button is the last focusable element and the Cancel button uses `Dialog.Close asChild`, focus rest…

#### `src/client/components/ui/dropdown-menu.tsx`
- [ ] `src/client/components/ui/dropdown-menu.tsx:47-52` — [bug · low] Outside-click listener fires on `pointerdown`, but the trigger's own `onClick` toggles state. On touch devices, `pointerdown` on the trigger fires before `click`, the listener bails out (target is in…
- [ ] `src/client/components/ui/dropdown-menu.tsx:124-127` — [bug · low] `getItems()` queries by `[role="menuitem"]` and excludes `aria-disabled="true"` and `disabled`, but WAI-ARIA also allows `aria-hidden` subtree items; nested submenus or groups using `role="presentati…
- [ ] `src/client/components/ui/dropdown-menu.tsx:278-281, 281-284` — [bug · low] `asChild` items render a `Slot` but `DropdownMenuItem` does not forward the menuitem role/tabindex via props; Radix Slot merges props, but the trigger sets `role="menuitem"` here only — when wrapped…  _(+1 related in cluster: bug·low)_

#### `src/client/components/ui/input.tsx`
- [ ] `src/client/components/ui/input.tsx:9-10` — [bug · low] No defensive handling for the `type` prop: this wrapper spreads arbitrary `type` values directly onto the native `<input>`. If a consumer passes an unexpected string (e.g., a typo like `'emial'` or `…

#### `src/client/components/ui/select.tsx`
- [ ] `src/client/components/ui/select.tsx:93-96, 102-107` — [bug · low] Scroll-into-view effect lists `highlightedIndex` as a dependency, but its body only depends on the listbox being open and the highlighted index changing. Listing only `[open, highlightedIndex]` is co…  _(+2 related in cluster: bug·low)_
- [ ] `src/client/components/ui/select.tsx:116-124` — [bug · low] Stale `height` state causes the open animation to start from a wrong value if the option list contents change while closed. The `ResizeObserver` is mounted in a `useLayoutEffect` whose body does not…
- [ ] `src/client/components/ui/select.tsx:350-359` — [a11y · low] The listbox `<motion.ul>` element has no `aria-activedescendant` plumbing on itself; active-descendant is implemented via the trigger's `aria-activedescendant`. That's fine for a listbox opened by th…

#### `src/client/hooks/use-job-detail.ts`
- [ ] `src/client/hooks/use-job-detail.ts:7-17` — [bug · low] No `let`/`var` issues, no `==`/`!=`, no `any`, no nested ternaries, no `innerHTML`, no `eval`, no `document.write`, no prototype mutation — passes the listed rules. Good.
- [ ] `src/client/hooks/use-job-detail.ts:62-69` — [bug · low] The first effect resets `etag.current` and `latestJob.current` when `id` changes, but does not reset `error`. If a previous job for another id had a stored error, switching ids will display that stal…
- [ ] `src/client/hooks/use-job-detail.ts:137-143` — [bug · low] `handleDelete` only clears `isDeleting` in the catch branch, which works because the success path navigates away. However, if `navigate` is somehow blocked or `api.deleteJob` returns without throwing…

#### `src/client/lib/api.ts`
- [ ] `src/client/lib/api.ts:173-173` — [bug · low] A conditional GET may legitimately return 304 without repeating validators; this currently replaces the caller's known ETag with `null`. If the consumer stores this result, subsequent requests lose c…

#### `src/client/lib/ease.ts`
- [ ] `src/client/lib/ease.ts:1-1` — [other · low] Naming: `EASE_OUT` is a very generic identifier. The numeric values correspond to the well-known "expoOut" curve (cubic-bezier(0.16, 1, 0.3, 1)). Consider a more descriptive name such as `EASE_EXPO_O…

#### `src/client/lib/utils.ts`
- [ ] `src/client/lib/utils.ts:18-20, 27-29` — [bug · low] Sub-minute durations always render one decimal, so 0–99ms values display as '0.0s' / '0.1s'. If the intent is to surface sub-second work precisely, fine; otherwise consider a minimum-display threshol…  _(+1 related in cluster: bug·low)_

#### `src/client/pages/dashboard.tsx`
- [ ] `src/client/pages/dashboard.tsx:44-44` — [bug · low] Stale closure risk in `load` and `usePolling` deps: `load` is recreated on every render (it's a fresh `async` function literal) and closes over the current `days`. Looking at `usePolling`, the effect…

#### `src/client/pages/jobs.tsx`
- [ ] `src/client/pages/jobs.tsx:61-70` — [bug · low] Manual Refresh button always starts from page 1 and ignores current filters on retry: clicking Refresh calls `load(true)` which is fine, but if a previous request failed and `error` is set, `loading`…
- [ ] `src/client/pages/jobs.tsx:82-89` — [accessibility · low] Search input has no associated label or accessible name other than the wrapping `<label>` which uses `htmlFor="pr-search"`—that part is fine. However, the input lacks an `aria-label`/`type="search"`…

#### `src/client/pages/landing.tsx`
- [ ] `src/client/pages/landing.tsx:34-38` — [bug · low] Async session probe has no `.catch`/try-catch at the call site. `probeSession` currently swallows errors internally (returns `null` on failure), so this is safe today — but if that contract ever chan…

#### `src/client/pages/not-found.tsx`
- [ ] `src/client/pages/not-found.tsx:7-7, 14-19` — [other · low] This route-level error page has no explicit document title or semantic landmark/announcement. Users of screen readers may not be informed that navigation resulted in a not-found state, and the browse…  _(+1 related in cluster: other·low)_

#### `src/client/pages/repos.tsx`
- [ ] `src/client/pages/repos.tsx:70-78` — [bug · low] `getStoredRepoRoute` returns `repo.sizeOverrides ?? []` but the schema likely stores `sizeOverrides` as a possibly-null array. `routesEqual`/`tiersEqual` will treat `[]` as equal to the empty global…
- [ ] `src/client/pages/repos.tsx:193-195` — [bug · low] `describeModelRoute(route, modelOptions)` may produce strings containing `owner`/`repo` content from the route metadata. This string is rendered as text content (safe), but is used only as a tooltip-…
- [ ] `src/client/pages/repos.tsx:267-279` — [bug · low] `handleApply` saves the strategy, but if `route` equals the current `globalRoute` (user edited to match globals), the saved record is custom yet identical to global. The subsequent `hasMeaningfulCust…
- [ ] `src/client/pages/repos.tsx:424-424` — [bug · low] `loadRepos` is declared with `function` inside the component body, recreating a new function reference each render. While acceptable, the `useEffect(() => { loadRepos(); }, [])` only runs once becaus…

#### `src/client/pages/repos/add-bitbucket.tsx`
- [ ] `src/client/pages/repos/add-bitbucket.tsx:48-50` — [bug · low] Duplicate user-facing error reporting: failures are surfaced both via the inline Alert (`setError`) and `toast.error`. This creates redundant notifications on every failure. Pick one channel (typical…
- [ ] `src/client/pages/repos/add-bitbucket.tsx:151-151` — [bug · low] The "Back to repositories" button calls `navigate('/repos')` even when `submitting` is true but is disabled. While the disabled guard prevents the click, using a plain `onClick` rather than relying o…

#### `src/client/pages/stats.tsx`
- [ ] `src/client/pages/stats.tsx:141-149` — [bug · low] `statusTotal` is floored to `1` even when `stats.statuses` is empty, but unlike `repoMax`/`modelMax` (which are used only for percentage widths and thus benefit from the floor), `statusTotal` also fe…

#### `src/client/pages/vcs-credentials.tsx`
- [ ] `src/client/pages/vcs-credentials.tsx:234-234` — [bug · low] Stale closure in useEffect: `load()` is defined inside the component and captured by the empty-deps `useEffect(() => { load(); }, [])`. ESLint react-hooks/exhaustive-deps will flag this. Either inclu…
- [ ] `src/client/pages/vcs-credentials.tsx:246-250` — [bug · low] No cleanup of `tid` if component unmounts mid-save: if the user navigates away while a save is in-flight, `toast.loading('Saving credential…')` and `toast.loading('Removing credential…')` will resolv…
- [ ] `src/client/pages/vcs-credentials.tsx:268-270, 274-277` — [bug · low] handleSubmit: when mode is 'add' the comparison `currentExpiry !== originalExpiry` is skipped (correct) but `editingCredential` is still read for the original value computation, which is fine. Howeve…  _(+1 related in cluster: bug·low)_
- [ ] `src/client/pages/vcs-credentials.tsx:301-307` — [bug · low] Delete does not validate that the row still exists: after `setDeleteTarget(null)` closes the dialog (currently in finally — see other finding) and a successful delete, the local list is filtered. But…
- [ ] `src/client/pages/vcs-credentials.tsx:454-458` — [accessibility · low] ConfirmDialog 'description' can be empty string when `deleteTarget` is null: the dialog stays open only when deleteTarget is non-null (controlled by `open={deleteTarget !== null}`), so the empty desc…

#### `src/server/core/config.ts`
- [ ] `src/server/core/config.ts:40-48` — [bug · low] `getGlobalConfig` returns the cached value even when schema validation fails: when `repoConfigSchema.shape.model.safeParse(cached)` fails (e.g., a poisoned global value), the function silently falls…

#### `src/server/core/crypto.ts`
- [ ] `src/server/core/crypto.ts:41-45` — [bug · low] Tag length is implicit on both encrypt and decrypt. WebCrypto's default is 128 bits, but relying on the default makes the contract brittle across runtimes and complicates the format migration story.…

#### `src/server/core/diff.ts`
- [ ] `src/server/core/diff.ts:131-135` — [bug · low] 'GIT binary patch' is also emitted for binary patches of new (non-empty) files, not just empty additions — but the previous 'new file mode' handler does not set `isBinary`. The combined check `Binary…
- [ ] `src/server/core/diff.ts:227-227` — [bug · low] `getValidNewLines` filters `line.kind !== 'del' && line.newLineNumber !== undefined` and then does `line.newLineNumber as number` — this non-null assertion is necessary because TypeScript cannot narr…
- [ ] `src/server/core/diff.ts:242-252` — [bug · low] `findPositionForLine` iterates all hunks and breaks on the first matching position, but it iterates *all* lines within a hunk rather than breaking early inside the inner loop. For files with many com…
- [ ] `src/server/core/diff.ts:341-352` — [bug · low] Inside `chunkFileDiff`'s `while`, when `roomInChunk <= 0`, the code pushes the *current* chunk then resets. If the freshly-pushed chunk has `currentLines === 0` (e.g. the previous iteration filled ex…

#### `src/server/core/github-oauth.ts`
- [ ] `src/server/core/github-oauth.ts:41-45` — [other · low] `exchangeGitHubOAuthCode` ignores the `scope` field in the OAuth response. If your app enforces requested scopes, the returned `scope` should be validated against expectations before issuing a sessio…

#### `src/server/core/http.ts`
- [ ] `src/server/core/http.ts:6-7` — [bug · low] Silent default on missing Accept header: `?? ''` collapses absent and empty Accept headers into the same value, so `wantsHtml` cannot distinguish "no header" from "Accept: ". If callers depend on thi…

#### `src/server/core/model-output.ts`
- [ ] `src/server/core/model-output.ts:179-184` — [bug · low] normalizeFinding's line_range construction uses `(start ?? end!)` which will throw at runtime if `start` is undefined and `end` is undefined (the `??` short-circuits to `end` which is undefined, then…
- [ ] `src/server/core/model-output.ts:386-393` — [bug · low] Severity mapping drops invalid priorities silently. When `finding.priority` is outside 0-3 (or negative), `normalizeFinding` clamps it to [0,3] before parsing — that's fine — but `priorityMap[finding…

#### `src/server/core/review.ts`
- [ ] `src/server/core/review.ts:423-435` — [bug · low] Lease release gap for `'superseded'` race during continuation-ceiling degrade: `continueOrFailWedgedJob` is called from inside `runReviewJob`'s catch block. On the `phase === 'review'` degrade branch…
- [ ] `src/server/core/review.ts:444-447` — [bug · low] `continueOrFailWedgedJob` argument order: `leaseOwner` is passed BEFORE `phase` (line 482-487), then `phase`, `delaySeconds`, `reason`. Verify all callers -- line 444 passes `..., leaseOwner, phase,…
- [ ] `src/server/core/review.ts:480-489, 496-500` — [bug · low] `continueOrFailWedgedJob` calls `markJobContinuationQueued(env, job.id, delaySeconds)` BEFORE checking the ceiling. On the `phase === 'review'` degrade branch (continuation > MAX_JOB_CONTINUATIONS),…  _(+1 related in cluster: bug·low)_
- [ ] `src/server/core/review.ts:866-874` — [bug · low] `existingReview` passed to `reviewAndPersistFile` after an `awaitingReview.poll failed` is the same `existingReview` whose `transient_error_count` was used to compute `compactPrompt`. But the row is…
- [ ] `src/server/core/review.ts:1186-1190` — [bug · low] `Object.defineProperty` is called on the same `error` object that may already have a non-configurable `retryAfterSeconds` property (e.g., from a lower-layer model service that defined it once). Becau…

#### `src/server/db/jobs.ts`
- [ ] `src/server/db/jobs.ts:620-621` — [bug · low] `markJobContinuationQueued`'s `delaySeconds` default is `0` and the SQL uses `WHEN $2::int > 0` to branch. `delaySeconds=0` falls into the `ELSE now()` branch, which sets `last_queue_message_at = now…
- [ ] `src/server/db/jobs.ts:893-902` — [concurrency · low] `findExistingJobForHead` uses a `WHERE r.vcs_provider = $6` predicate with a parameter binding, but the binding's name `vcsProvider` shadows the optional input param's name. If a future caller passes…
- [ ] `src/server/db/jobs.ts:978-983` — [concurrency · medium] `recoverExpiredJobLeases` runs the `requeued` and `failed` CTEs as two independent statements. Between them, a worker can call `heartbeatJobLease`, re-extending `lease_expires_at` and clearing the `W…
- [ ] `src/server/db/jobs.ts:1085-1097` — [bug · low] `getTerminalJobsNeedingCheckRunCompletion` ORDER BY uses `COALESCE(j.finished_at, j.started_at, j.created_at) ASC` — this picks the oldest *terminal* job first, which is good. But for jobs that are t…

#### `src/server/db/model-configs.ts`
- [ ] `src/server/db/model-configs.ts:138-147` — [bug · low] `findLlmProviderByName` performs a case-insensitive comparison via `lower(name) = lower($1)`, but `listLlmProviders` and other queries `ORDER BY name` without a functional index. If provider names mu…
- [ ] `src/server/db/model-configs.ts:167-184` — [bug · low] `updateLlmProvider` does not validate `id` exists: if `$1` matches no row, `row` is `undefined` and `mapProvider(row)` will throw. Return `null` (matching the documented return type) on no match inst…
- [ ] `src/server/db/model-configs.ts:247-253` — [bug · low] `getResolvedModelConfig` does an INNER JOIN with `llm_providers`, so if a `model_configs` row exists whose `provider_id` points at a deleted provider, this returns `null` instead of the orphaned mode…

#### `src/server/db/repo-configs.ts`
- [ ] `src/server/db/repo-configs.ts:57-70` — [bug · n/a] Partial-update semantics for enabled flag may be incorrect. `enabled = COALESCE($6, repo_configs.enabled)` preserves the existing value when `$6` is NULL. However, callers explicitly passing `enabled…
- [ ] `src/server/db/repo-configs.ts:82-108, 115-140` — [bug · n/a] Race condition between syncRepoConfig and upsertRepoConfig: if a user has just upserted a custom config and a sync runs concurrently, the ON CONFLICT DO NOTHING in syncRepoConfig won't overwrite, but…  _(+1 related in cluster: bug·n/a)_
- [ ] `src/server/db/repo-configs.ts:231-232` — [bug · n/a] getRepoConfigRecord returns `null` when no row found, but mapRepo assumes all required fields exist. If a partial row exists (e.g., due to schema drift or left join returning unexpected nulls), `repo…

#### `src/server/db/repositories.ts`
- [ ] `src/server/db/repositories.ts:25-25, 27-28` — [other · high] `vcsProvider` is typed as an arbitrary string, but every value other than the exact literal `bitbucket` takes the GitHub SQL path. A typo or unsupported provider is therefore silently persisted (for…  _(+1 related in cluster: other·high, other·medium)_
- [ ] `src/server/db/repositories.ts:43-44` — [other · medium] The Bitbucket UPSERT uses `(vcs_provider, workspace, repo)` as its conflict key but updates `owner` from the latest caller. If the same canonical Bitbucket identity is supplied with different owner v…
- [ ] `src/server/db/repositories.ts:81-85` — [other · medium] This lookup depends on every caller lowercasing both values, but the repository layer itself performs no normalization. A missed normalization silently returns `null`, causing webhook handling to tre…

#### `src/server/db/vcs-credentials.ts`
- [ ] `src/server/db/vcs-credentials.ts:0-0` — [bug · low] bulk-inherited files set in currentReviews are the parent's review objects (still pointing at the parent's job_id semantics), but they were retrieved from allExistingReviews where the filter ensures…  _(+3 related in cluster: bug·low)_

#### `src/server/db/webhook-deliveries.ts`
- [ ] `src/server/db/webhook-deliveries.ts:66-66` — [bug · low] Read-path type safety: `parseJsonColumn` only parses strings; if `fetch_types` is ever flipped on (or a future migration changes the column type) the jsonb value would already be an object and `parse…

#### `src/server/middleware/auth.ts`
- [ ] `src/server/middleware/auth.ts:0-0` — [bug · low] Cookie attribute drift: `createSession` sets `secure: true, sameSite: 'Lax', httpOnly: true, path: '/'`. `destroySession` only sets `path: '/'`. Browsers match deletion cookies on (name, domain, path…
- [ ] `src/server/middleware/auth.ts:9-11` — [bug · low] Open redirect risk: `c.redirect('/login')` is safe here because the target is a static literal, but the codebase likely uses `c.redirect(...)` elsewhere with user-controlled `next` parameters. Worth…

#### `src/server/models/anthropic.ts`
- [ ] `src/server/models/anthropic.ts:55-55` — [bug · low] Two concurrent errors can be swallowed by the same guard. If `response.json()` rejects (e.g., truncated body, invalid JSON), the thrown error has no user-friendly context — it propagates as a bare pa…

#### `src/server/models/catalog.ts`
- [ ] `src/server/models/catalog.ts:220-227` — [bug · low] The 'cf/' → '@cf/' branch in `normalizeCloudflareModelId` is dead code: every caller applies a `.startsWith('@cf/')` filter immediately after normalization, so any string starting with 'cf/' that get…

#### `src/server/models/cloudflare.ts`
- [ ] `src/server/models/cloudflare.ts:150-150` — [bug · low] Missing null guard before `choice.finish_reason ?? choice.stop_reason` access. While the prior line guards `isRecord(result)` and `Array.isArray(result.choices)`, it does *not* assert `choice` is a r…
- [ ] `src/server/models/cloudflare.ts:305-305` — [bug · low] Variable `lastError` is declared but never read on success paths, and on the failure path it is `throw lastError` — but `lastError` is only assigned in the catch, so if the loop never entered the cat…

#### `src/server/models/google.ts`
- [ ] `src/server/models/google.ts:32-45` — [bug · low] Non-JSON retry-after values silently fall back: When `retry-after` is an HTTP-date, `Date.parse` returns `NaN` for invalid strings — handled. But `retryAfterDelayMs` returns `null` only if both branc…
- [ ] `src/server/models/google.ts:113-115` — [bug · low] `incrementSubrequests` is incremented even when the fetch throws: The call `tracker.incrementSubrequests(1)` happens before `fetch`, which is fine for accounting — but if the fetch throws and the loo…
- [ ] `src/server/models/google.ts:152-157` — [bug · low] `retryAfterDelayMs` comment mismatch: The variable name and JSDoc above imply milliseconds handling, but the function returns either `seconds * 1000` (when `value` parses as a number) or a date delta…

#### `src/server/models/limits.ts`
- [ ] `src/server/models/limits.ts:72-79` — [bug · low] `acquire()` increments `active` before returning the resolved promise. This is fine for the hot path but creates a subtle re-entrancy hazard: if a caller synchronously invokes another `acquire()` fro…

#### `src/server/prompts/file-review.ts`
- [ ] `src/server/prompts/file-review.ts:48-48` — [bug · low] Persona clobbers system prompt. `buildFileReviewSystemPrompt` prepends `'You are a world-class professional senior code reviewer...'` and then concatenates the entire `fileReviewSystemPromptBase` (wh…

#### `src/server/routes/api/jobs.ts`
- [ ] `src/server/routes/api/jobs.ts:94-96` — [bug · low] Returning the value of `c.json(...)` directly from a handler works in Hono, but mixing it with `return c.json({ error: ... }, 400)` here in the early-rejection branch can cause the caller's `return c…
- [ ] `src/server/routes/api/jobs.ts:105-108` — [bug · low] The `as any` cast on the parsed query (line 51) also defeats null/undefined checks at the DB layer; combined with the swallowed `loadRepoConfig` failure (line 106) the entire `startReplacementJob` is…
- [ ] `src/server/routes/api/jobs.ts:172-186` — [bug · n/a] `/stop` and `/rerun` check `status` from a freshly-loaded row and then act on it without re-checking under a transaction. If the row transitions between `getJobForProcessing` and `cancelJob`/`insertJ…

#### `src/server/routes/api/models.ts`
- [ ] `src/server/routes/api/models.ts:52-65` — [bug · low] `fallbacks` in `globalModelConfigSchema` is `.nullable().default([])` and `size_overrides.fallbacks` is `.array(...).optional()` — this asymmetry leads to runtime code needing to handle `null`, `unde…
- [ ] `src/server/routes/api/models.ts:173-173` — [bug · low] Route ordering: `app.post('/sync')` is declared before any `/:id` handlers in the same router. Because Hono registers routes in order, this works correctly today, but a future maintainer adding `app.…
- [ ] `src/server/routes/api/models.ts:199-202, 204-208, 204-220` — [bug · low] Error responses swallow validation details: every Zod parse failure returns `'Invalid provider config.'` / `'Invalid global model config.'` with no field path or issue information. Callers cannot tel…  _(+2 related in cluster: bug·low)_

#### `src/server/routes/api/settings.ts`
- [ ] `src/server/routes/api/settings.ts:28-32` — [bug · low] The PATCH body parser collapses every parse failure — empty body, malformed JSON, schema violation, or unknown extra keys — into a single generic 'Invalid review settings.' 400. In practice this is a…

#### `src/server/routes/api/vcs-credentials.ts`
- [ ] `src/server/routes/api/vcs-credentials.ts:87-90` — [bug · low] TOCTOU race between `getVcsCredentialSecrets` and `upsertVcsCredential`. Two concurrent POSTs for the same brand-new key can both observe `!existing`, both pass the "both secrets required" gate, and…
- [ ] `src/server/routes/api/vcs-credentials.ts:112-115` — [bug · low] DELETE handler does not validate `workspace` / `repoSlug` length or minimum length before issuing the DB query. A request to `/api/vcs-credentials/bitbucket//some-slug` (empty workspace) reaches `del…

#### `src/server/routes/auth.ts`
- [ ] `src/server/routes/auth.ts:27-30` — [bug · low] Loose equality / truthiness on `c.req.query('error')`: a GitHub-supplied `error` string of `'0'`, `'false'`, or an empty string would currently bypass the error branch (`if (error)`). GitHub's docume…

#### `src/server/routes/webhook-bitbucket.ts`
- [ ] `src/server/routes/webhook-bitbucket.ts:192-210` — [bug · low] Step 12 mutates the response contract: if `recordWebhookDelivery` returns `null` (interpreted as a duplicate), the handler reports `{ ok: true, duplicate: true, eventName: xEventKey }` with status 20…
- [ ] `src/server/routes/webhook-bitbucket.ts:223-223` — [bug · low] `baseSha` falls back to `''` for `destination.commit.hash`, but the schemas use `.passthrough()` and `commitSchema.hash: z.string().min(1)` — so the schema should never let an empty hash through. If…
- [ ] `src/server/routes/webhook-bitbucket.ts:290-296` — [bug · low] Possible encoding drift: `JSON.parse(rawBody)` and the Zod object spread assume `parsedBody` is a JSON object (a mapping of string keys to values), but `typeof [] === 'object'` and `typeof null === '…

#### `src/server/services/formatter.ts`
- [ ] `src/server/services/formatter.ts:32-36` — [bug · low] `img(...)` assumes `alt` is safe to embed in an HTML attribute, but it is currently called with raw severity labels derived from `ParsedReviewComment['severity']`. Today the input is constrained to `…
- [ ] `src/server/services/formatter.ts:57-65` — [bug · low] Title-dedup logic compares lowercased strings but the body is sliced using the *original-case* `firstLine.length`. If the case differs (e.g. title `'Foo'`, first line `'foo bar'`), the prefix check m…

#### `src/server/services/model.ts`
- [ ] `src/server/services/model.ts:227-229` — [bug · low] resolveModel's `pending.catch(() => this.resolvedModelCache.delete(normalized))` deletes the cache entry from the rejection handler, but `pending` is already stored in the map and may still be awaite…

#### `src/server/vcs/bitbucket.ts`
- [ ] `src/server/vcs/bitbucket.ts:282-285` — [bug · low] `findExistingReviewForCommit` matches on a substring of `item.body` containing both `BITBUCKET_REVIEW_MARKER` and `shortSha`, but `String.prototype.includes` is a plain substring check. If a previous…

#### `src/server/vcs/types.ts`
- [ ] `src/server/vcs/types.ts:66-71` — [bug · low] `VcsSubmitReviewInput.jobIdHint` is provider-asymmetric (Bitbucket uses it; GitHub ignores it). A future GitHub call site that accidentally passes `jobIdHint` will compile and silently no-op. Conside…

#### `src/shared/github.ts`
- [ ] `src/shared/github.ts:5-7` — [bug · low] `isSupportedGitHubWebhookEvent` does not validate that the input is actually a string before calling `.includes`. If a caller passes `null`, `undefined`, or a non-string value, this will throw a `Typ…

#### `src/shared/schema.ts`
- [ ] `src/shared/schema.ts:390-392` — [bug · low] `repoConfigSchema.model.size_overrides` is declared as `.nullable().optional()` inside the model default block, but `normalizeRepoModelConfig` handles `=== null OR === undefined` AND the empty-array…

#### `test/github-fetch-mock.ts`
- [ ] `test/github-fetch-mock.ts:82-86` — [bug · low] The reviewResponses script is consumed only by the POST .../reviews handler. If GitHubClient first does a GET .../reviews (which always returns [] here) before each POST, that GET is silently answere…

#### `test/helpers.ts`
- [ ] `test/helpers.ts:14-21` — [bug · low] MemoryKV.get silently returns null for missing keys without simulating KV's eventual-consistency/expiration behavior. If production code distinguishes between "key never existed" and "key was deleted…

#### `tsconfig.json`
- [ ] `tsconfig.json:4-4` — [bug · low] Spelling error: `"lib"` entries should be `"ES2024"` and `"DOM.Iterable"` — however `"ES2024"` is invalid. The correct TypeScript lib name is `"ES2024"` is acceptable, but verify; more importantly, `…

#### `vitest.config.ts`
- [ ] `vitest.config.ts:19-19` — [other · low] Missing 'rootDir' on the workspace projects: Without an explicit rootDir per project (and at the workspace level), Vitest infers it from each project's include patterns. This can produce surprising b…

## Excluded (verified)

### Path-excluded globs

| Glob | Count | Reason |
| ---- | ----- | ------ |
| `.claude/**` | 1825 | Not tracked product source — GSD tooling / planning artifacts (gitignored). |
| `.design-sync/previews/**` | 34 | Generated design-preview artifacts, not tracked product source. |
| **Total path-excluded** | 1859 | |

### Verified false-positive findings

Finding-specific exclusions (co-located real findings in the same file were KEPT).

| File:lines | Tag | Reason |
| ---------- | --- | ------ |
| `scripts/setup-cloudflare.js:130-130` | security · high | FP — alleged command-arg injection via dbUrl. Operator-run local setup script; input is the operator's own connection string, not an external trust boundary. |
| `scripts/setup-cloudflare.js:240-240` | security · high | FP — alleged arg-injection via resolvedId/dbUrl. Operator-run local setup script; no external attacker surface. |
| `scripts/setup-cloudflare.js:284-284` | security · high | FP — alleged shell injection via secretName in exec(). Operator-run local setup script; secretName is a fixed internal identifier. |
| `db/migrations/005_bitbucket_repo_identity.sql:58-65` | bug · critical | FP — alleged UNIQUE constraint-name collision. Guard/constraint names are byte-identical to today's schema. |
| `db/migrations/003_vcs_provider_foundation.sql:10-18` | bug · critical | FP — alleged dup (owner,repo) breaking migration. 001 UNIQUE(owner,repo) already guarantees no duplicate pairs. |
| `db/migrations/001_initial.sql:347-350` | bug · critical | FP — real gap but a frozen one-time migration path; not editable to effect (already applied). |
| `db/migrations/001_initial.sql:490-496` | bug · critical | FP — real gap but a frozen one-time migration path; not editable to effect (already applied). |
| `db/migrations/001_initial.sql:579-586` | security · high | FP — real gap but a frozen one-time migration path; not editable to effect (already applied). |
| `src/server/core/verify.ts:50-50` | security · high | FP — webhook body re-encoding via TextEncoder is a byte-identical round-trip; signature verification is unaffected. |
| `src/server/routes/api/auth.ts:4-7` | security · high | FP — "missing authentication middleware". requireSession middleware is mounted globally at app.ts:35-36. |
| `src/server/routes/api/models.ts:173-180` | bug · medium | FP — "/sync has no auth check". Covered by global requireSession + requireCsrfHeader (app.ts:35-36). |
| `src/server/routes/api/models.ts:162-163` | bug · critical | FP — "no auth on router". Global requireSession + requireCsrfHeader at app.ts:35-36 gate every /api/* route. |
| `src/server/routes/api/repos.ts:50-53` | security · high | FP — "router exposes repo inventory/mutation without session check". Global requireSession + requireCsrfHeader (app.ts:35-36) enforce auth. |
| `src/client/components/features/job-detail/file-finding.tsx:52-52` | security · high | FP — client XSS. rehypeSanitize runs last in the plugin chain (safeRehypePlugins), sanitizing rehypeRaw output. |
| `src/client/pages/repos.tsx:282-282` | security · high | FP — toast XSS. Sonner escapes the description; interpolated repo fields are rendered as text, not HTML. |

> Note on no-auth FPs: the OCR dump's no-auth-on-router findings that global `requireSession` + `requireCsrfHeader` (app.ts:35-36) neutralize live at `models.ts:162-163`, `models.ts:173-180`, `repos.ts:50-53`, and `routes/api/auth.ts:4-7`. Co-located real items are retained: `stats.ts:9-11` (days range-check), `jobs.ts:147-154` (retry/rerun/stop/delete TOCTOU), `vcs-credentials.ts:71-73` (error-message leakage).
